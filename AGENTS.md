# 杀合项目专属规则

本项目继承上级 `..\AGENTS.md`。以下只补充本项目差异，不得放宽上级规则。

## 1. 数据契约

- 项目结果为“绝杀一合”的杀合数。
- 单结果默认必须是一个 `01合` 至 `13合` 的值。
- 合法值仅为 `01合`、`02合`、`03合`、`04合`、`05合`、`06合`、`07合`、`08合`、`09合`、`10合`、`11合`、`12合`、`13合`。
- 普通站点每期只能输出一个合数。
- `s085_kcvpleh`“女人味”专属格式允许每期两个合数，必须按原文顺序输出为 `XX合,YY合`，两个值均须在 `01合` 至 `13合` 内。
- `s073_shuqhbq`“九肖杀”只取“绝杀①段①合”表中的合数列，每期只能有一个有效合数。
- 不接受合数范围外、缺少前导零、空值、多个普通合数、尾数或生肖代替合数。
- 成功输出仅写入完整校验通过的当期值；排行按本期实际成功值生成。

## 2. 方向与候选边界

- `top`、`顶部`、`上`、`上部`、`前`统一只接受目标区块第一条有效“期数+数据”候选；该候选前面不得存在任何其他有效“期数+数据”。
- `bottom`、`尾部`、`底部`、`下`、`下部`、`后`统一只接受目标区块最后一条有效“期数+数据”候选；该候选后面不得存在任何其他有效“期数+数据”。
- 方向边界固定为1条，不得运行时扩大、缩小或跨区块补数。
- 标题、作者和说明文字不占用方向边界；无效期数、无效合数和数量错误的候选也不占用方向边界。
- 指定期数不是对应方向的第一条或最后一条有效候选时必须失败；区块内部其他同期期数不得补抓、救援或制造冲突。
- `s066_topic_463139`“春花烂漫”的正式配置为 `top`，只允许读取目标锚点区块第一条有效候选；任何专属 `bottom` 覆盖都不得改变该方向。
- `s013_topic_226261`“盈把之木”和 `s057_topic_227386`“束身自修”只允许读取目标区块最后一条有效候选。
- `s060_topic_589491`“赛码会”允许读取特殊历史归档中的唯一严格候选，但同一期出现不同合数仍必须失败。

## 3. 项目入口与路径

- 生产项目路径：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀合`。
- 正式站点配置：`sites.json`。
- 单期兼容入口：`he_crawler.py`，实际服务入口为 `he_app.services.runner`。
- 多期兼容入口：`he_multi_period_crawler.py`，实际服务入口为 `he_app.services.multi_period`。
- 判重兼容入口：`he_duplicate_checker.py`，实际服务入口为 `he_app.services.duplicate_check`。
- 单期BAT：`爬虫-每天杀一合.bat`，执行 `he_crawler.py --period N`。
- 多期BAT：`爬虫-每天杀一合-多期.bat`，逐期调用单期入口并生成多期全部失败汇总。
- 判重BAT：`爬虫-每天杀一合 - 检测重复.bat`，执行 `he_duplicate_checker.py --workers 8`。
- 正式近10期缓存：`outputs\recent_10_cache.json`；日常单期流程只同步这一份缓存。
- `he_success_cache.json` 只是判重器保留的兼容读取参数，当前正式单期流程不生成或更新它，验收不得要求该文件存在。
- 正式单期成功输出目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳`。
- 正式单期失败输出目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳失败`。
- 单期成功文件：`{期数}期-合.txt`。
- 单期失败文件：`{期数}期-合-失败.txt`。
- 多期全失败汇总：`{期数列表}期-多期全部失败.txt`。
- 多期全失败汇总默认写入正式单期失败输出目录。
- 判重拒收输出默认：`{期数}期重复网站.txt`。
- 判重疑似输出默认：`{期数}期疑似重复网站.txt`。
- 判重失败输出默认：`{期数}期重复检测失败.txt`。
- 所有正式TXT和JSON写入必须沿用现有路径、编码、锁和原子替换约定。

## 4. 专属解析与动态来源

- 普通站点使用严格“绝杀一合/公式杀合/绝杀合”关键词；不得以弱关键词替代。
- 专属解析必须按站点ID注册，不得按站点名称在核心流程中堆叠条件。
- `s073_shuqhbq`使用“绝杀①段①合”表格解析，只取合数列。
- `s085_kcvpleh`使用“女人味—绝杀一尾二合”表格解析，严格提取两个合数。
- `s070_topic_246762`使用“一点红”脚本及解码内容作为已授权来源，仍须通过原专属解析和统一校验。
- `s093_a_909922_article_aspx_id_3694545`先访问列表页，再跟随标题包含“绝杀一合”的文章链接；不得把列表页其他文章拼入目标文档。
- `s108_topic_1024380`、`s118_topic_1024655`、`s119_topic_1024654`使用动态主页或栏目页寻找标题包含站点名称和“绝杀一合”的目标帖子。
- `s118_topic_1024655`、`s119_topic_1024654`从栏目页定位动态帖子。
- `s108_topic_1024380`允许按指定期数优先匹配当前期或下一期标题，但最终必须严格命中指定期数。
- `s101_topic_274142`“没名字二”只依据页面正文中的专属历史行解析，禁止使用配置 URL、域名或链接文本作为锚点。
- 用户论坛主页或用户页配置了论坛API的站点，优先读取对应API对象；API返回的帖子、作者、期数和正文必须属于同一记录。
- `/article/manager/` 动态文章站点必须从URL提取文章ID，请求该文章ID的专属API；标题、作者、正文、期数和合数必须来自同一接口对象。
- 动态接口ID缺失、重复、错误、记录不一致或正文为空，必须失败；不得用聚合页面其他文章补数。
- 浏览器、iframe、script、OCR或解码脚本仅限配置明确授权的站点；不能改变期数、方向、数量或冲突裁决。
- 页面结构特殊或通用解析可能串区块时，必须使用站点专属解析，不得回退通用解析凑成功。

## 5. 截图依据

- 新增站点或修复站点时，用户截图是专属解析设计依据，必须据此确认标题或作者锚点、栏目、关键词、期数排列、方向区块、正文边界、合数格式和当前记录与历史记录的分隔。
- 截图不能代替真实访问，不能把截图中的数值写死为正式抓取结果。
- 截图识别出的结构必须落实为专属解析和回归测试。
- 回归测试至少覆盖正确期、相邻期、方向越界、边界不清、合数数量错误和同期冲突。

## 6. 新增站点

- 新增前必须读取并验证 `outputs\recent_10_cache.json`；缓存缺失、损坏、过期、期数不匹配或无可用旧站指纹时停止新增。
- 候选站必须在缓存最新期或上一期中，准确命中用户指定方向的第一条或最后一条有效“期数+数据”。
- 候选站不得从自身页面推断最新期作为有效性依据。
- 通过真实页面、专属锚点、栏目、期数、方向、数量和边界验证后，使用正式判重器与缓存基准判重。
- 判重通过后才可写入 `sites.json`；写入后必须同步 `outputs\recent_10_cache.json`。
- 新增站点的配置必须包含唯一 `id`、名称、URL、`pick`，以及布尔类型的 `browser`、`click_first`；即使两项均为 `false` 也不得省略，必要时再绑定专属解析身份。
- 同URL、同topic、同文章ID、同栏目和同方向不得重复添加；同URL不同栏目必须证明区块完全独立。
- 用户截图中的站名、URL和方向必须原样保留为候选记录，未明确开始前不得写入正式配置。
- 历史不足10期时，只能按上级历史不足特例处理；不得放宽缓存基准期、方向窗口、合数数量、身份或冲突规则。

## 7. 判重与缓存

- 正式判重只使用 `he_duplicate_checker.py`、`outputs\recent_10_cache.json` 和候选站现场真实结果。
- 禁止使用旧实时指纹脚本、旧TXT、截图、人工拼接值、过期缓存或其他站点结果。
- 指纹格式为“具体期号 -> 杀合结果”；按期号对齐，不按列表位置对齐。
- 只比较共同拥有的有效期号；缺失期不能跨越拼接连续片段。
- 连续1至2期一致不判重复。
- 连续3至5期一致标记疑似重复，暂停并人工审核。
- 连续6期或以上一致判定重复，拒绝新增。
- 同一URL的站点对不参与互相判重。
- 新增站快速检测必须使用 `--compare-cache outputs/recent_10_cache.json`，且测试期只能是缓存最新期或上一期。
- 日常单期实时结果定稿后更新最近10期指纹；成功覆盖本期指纹，失败删除本期旧指纹并记录错误，其他历史期保留。
- 日常单期仅在成功率严格大于85%时推进最近10期指纹；成功率小于等于85%时不推进缓存基准或成功指纹，仅在JSON的`errors`中标记失败，并清理失败站点的当期旧指纹。
- 多期入口逐期生成单期成功和失败文件，但必须使用 `--no-fingerprint-cache-sync`，不得更新正式近10期指纹缓存。
- 判重缓存缺失、损坏、过期或不可用时停止判重，不切换旧站列表、其他缓存或实时指纹来源。
- 缓存必须绑定站点ID、URL、方向、配置身份和具体期数；不可解释的身份或配置变化必须使缓存失效。
- 缓存写入使用锁、临时文件和原子替换；写入失败必须明确报错。

## 8. 失败站修复

- 修复先真实复现用户指定站点、期数、方向和URL，再确认是请求、动态来源、锚点、栏目、边界、期数、数量、方向或冲突问题。
- 优先修改该站专属解析，保持改动最小。
- 修复验证必须真实访问目标页面，检查主DOM、脚本、iframe、API及多文档是否错误合并。
- 必须验证标题与正文分离、跨行数据、正文边界、旧文档干扰、相邻期和错误期。
- 必须验证方向边界仍为第一条或最后一条，且标题、作者、说明和无效候选不占边界。
- 必须验证不同合数的同期候选被判为冲突，不能任选其一。
- 浏览器兜底不得放宽关键词、期数、方向、栏目、数量或数据边界。
- 专属解析失败后不得回退通用解析器补成功。
- 只通过正则、单函数、模拟文本或局部测试不算修复完成。
- 正式更新前先隔离验证；正式更新后必须完整执行目标站点单期流程，检查成功TXT、失败TXT及 `outputs\recent_10_cache.json`。

## 9. 异常报告

失败报告必须同时包含：

```text
失败类型: ...
具体原因: ...
```

并写明站点名称、站点ID、期数、方向和URL。

项目使用以下分类：

- `请求失败`：页面无法访问、HTTP错误、TLS错误或网络超时。
- `未找到目标`：缺少标题、作者、栏目、锚点或严格杀合关键词。
- `无正文定位`：期数存在但正文边界、目标区块或动态记录无法确认。
- `无当期`：严格候选中没有指定期数。
- `方向范围外`：指定期数不是目标区块第一条或最后一条有效“期数+数据”。
- `数据不完整`：合数缺失、格式错误、数量不符或正文边界不完整。
- `候选冲突`：同一期方向范围内出现多个不同合法合数。
- `缓存不可用`：缓存缺失、损坏、过期、身份不一致或无法读取。
- `重复拒收`：共同期号中连续一致达到6期或以上。
- `记录ID缺失/不一致`：动态文章ID缺失、错误、重复或接口对象不一致。
- `专属解析失败`：目标站必须使用的专属解析未得到唯一有效结果。

解析失败不得伪装成请求失败；缓存冲突不得伪装成抓取失败；方向越界不得用页面其他位置的数据替代。

## 10. 验收标准

- 单期必须真实命中指定期数、指定方向、正确栏目、正确关键词、正确边界和合法数量。
- 单期成功文件只含当期完整成功结果；失败文件逐站记录具体失败原因。
- 多期每期独立执行并分别生成成功、失败文件；只有所有指定期均失败的站点进入多期全部失败汇总。
- 单期成功更新 `outputs\recent_10_cache.json`；多期不得更新该缓存。
- 新增站必须完成真实缓存基准验证、正式判重、配置写入、缓存同步和回归测试。
- 修复站必须完成失败期、相邻期、错误期、方向越界、数量错误、跨区块、来源冲突和同期冲突测试。
- 相关测试、项目完整测试、Python编译和静态检查必须通过。
- 正式输出、`outputs\recent_10_cache.json` 和站点配置不得被隔离验证污染。
- 未完成真实指定期完整流程、缓存核验和完整测试前，只能报告“未完成”或“待验证”。

## 11. GitHub 同步规则（长期）

- 本项目后续每次开发或修复，在必要验证通过后，自动提交并推送到已绑定的 `origin`。
- `origin` 必须指向 `https://github.com/xiaobai156/hehehehe.git`；推送前核对仓库根目录、远端地址和当前分支。
- 只提交本次实际变更；用户已有未提交改动必须保留在工作区，不得自动暂存、提交、覆盖或回退。
- 提交前必须核对暂存范围，禁止提交凭据、密钥、`.env*`、缓存、测试产物、调试临时文件或其他项目文件；发现疑似敏感文件时停止推送并报告。
- 推送失败必须明确报告未上传，不得把本地提交或推送尝试报告为远端成功。

# 杀合项目规则

继承上级 `..\AGENTS.md`。本文件只记录本项目差异；上级规则、用户当前指令和“开始”授权优先。`AGENTS - 副本.md` 为只读历史文件，不修改。

## 数据契约

- 合数必须是 `01合` 至 `13合`；普通站点每期一个值。
- 配置中的 `value_count: 2` 站点每期两个值，按原文顺序输出为 `XX合,YY合`；当前包括 `s085_kcvpleh`、`s135_topic_481646`、`s136_topic_677676`、`s137_topic_682109`、`s138_gsx_20`。
- 只有完整通过身份、栏目/作者锚点、文档区块、期数、方向、数量、合法值、唯一性和顺序校验的结果才可写入正式输出或缓存。
- `top`/`bottom` 只取配置方向区块的第一条/最后一条有效候选；边界固定，不跨区块、不补抓、不用缓存或相邻期救援。
- `s066_topic_463139` 固定按 `top`；`s139_article_140_tid_8` 至 `s147_article_654_tid_44` 固定按各自文章ID和栏目块的 `top`；`s060_topic_589491` 允许其专属历史归档的唯一严格候选，同期冲突仍失败。

## 入口、输出与缓存

- 单期入口：`he_crawler.py`；多期入口：`he_multi_period_crawler.py`；判重入口：`he_duplicate_checker.py`。三者分别调用 `he_app.services` 中对应服务。
- 站点配置：`sites.json`；正式缓存：`outputs/recent_10_cache.json`。
- 正式成功/失败文件分别位于上级 `七类数据统一归纳` 和 `七类数据统一归纳失败`，命名为 `{期数}期-合.txt` 与 `{期数}期-合-失败.txt`。
- 日常单期成功率必须严格大于85%才推进正式缓存；失败站点删除当期旧指纹并写入 `errors`。多期运行使用 `--no-fingerprint-cache-sync`。
- 所有 TXT/JSON 沿用现有锁、临时文件和原子替换；缓存不能替代实时校验。
- 失败修复使用 `--retry-failures --append-success`：只读取失败TXT中的站点，成功结果追加到成功数据区底部、排行榜上方，失败行仅移除对应成功站点；未修复站点保持不变。
- 排行榜固定为 `内容\t次数\t排名` 三列，按次数降序、合数升序，同次数并列。

## 专属解析

- 普通站点必须使用严格的“绝杀一合/公式杀合/绝杀合”语义；专属解析按站点ID注册，失败不得回退通用解析凑成功。
- `s073_shuqhbq` 解析“绝杀①段①合”表格；`s085_kcvpleh` 解析“女人味—绝杀一尾二合”双值表格。
- `s135_topic_481646`、`s136_topic_677676`、`s137_topic_682109` 使用渲染后的作者区块双值解析；`s138_gsx_20` 使用“故城笙声”作者区块的“杀2合数”解析。
- `s070_topic_246762` 使用已授权脚本解码内容；`s093_a_909922_article_aspx_id_3694545` 先取列表，再跟随标题含“绝杀一合”的目标文章。
- `s101_topic_274142` 只读正文专属历史行；`s108_topic_1024380` 可发现当前/下一期标题，但最终必须命中指定期。
- 论坛API、`/article/manager/` API 必须返回同一条记录的站点身份、文章ID、标题/作者、正文、期数和合数；缺失、重复或不一致即失败。
- 浏览器、iframe、script、OCR、解码和动态接口只对配置明确授权的站点启用；网站地址不能单独作为锚点。

## 新增、修复与验收

- 新增站点必须先做身份、方向边界、有效历史、正式判重和缓存检查，再写入 `sites.json`；同URL/topic/文章ID/栏目/方向不得重复。历史不足或疑似重复只能在用户当前明确授权下放行，授权不永久扩展。
- 修复或新增只运行用户点名的站点、期数、方向和文件；禁止全站抓取、全站判重或影响其他站点。
- 必要验收包括受影响测试、`python -m compileall -q he_app` 和真实指定期流程；未完成真实流程只能报告待验证。

## GitHub 同步

- 必要验证通过后，只提交本次代码/配置改动并推送 `origin`；远端必须是 `https://github.com/xiaobai156/hehehehe.git`。
- 提交前核对仓库根目录、分支、暂存范围和 `git diff`；保留用户已有未提交改动。
- 不提交缓存、TXT结果、凭据、密钥、`.env*`、测试产物或临时文件；推送失败必须明确报告。

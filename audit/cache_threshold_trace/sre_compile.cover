       #
       # Secret Labs' Regular Expression Engine
       #
       # convert template to internal format
       #
       # Copyright (c) 1997-2001 by Secret Labs AB.  All rights reserved.
       #
       # See the sre.py file for information on usage and redistribution.
       #
       
>>>>>> """Internal support module for sre"""
       
>>>>>> import _sre
>>>>>> import sre_parse
>>>>>> from sre_constants import *
       
>>>>>> assert _sre.MAGIC == MAGIC, "SRE module mismatch"
       
>>>>>> _LITERAL_CODES = {LITERAL, NOT_LITERAL}
>>>>>> _REPEATING_CODES = {REPEAT, MIN_REPEAT, MAX_REPEAT}
>>>>>> _SUCCESS_CODES = {SUCCESS, FAILURE}
>>>>>> _ASSERT_CODES = {ASSERT, ASSERT_NOT}
>>>>>> _UNIT_CODES = _LITERAL_CODES | {ANY, IN}
       
       # Sets of lowercase characters which have the same uppercase.
>>>>>> _equivalences = (
           # LATIN SMALL LETTER I, LATIN SMALL LETTER DOTLESS I
           (0x69, 0x131), # iı
           # LATIN SMALL LETTER S, LATIN SMALL LETTER LONG S
           (0x73, 0x17f), # sſ
           # MICRO SIGN, GREEK SMALL LETTER MU
           (0xb5, 0x3bc), # µμ
           # COMBINING GREEK YPOGEGRAMMENI, GREEK SMALL LETTER IOTA, GREEK PROSGEGRAMMENI
           (0x345, 0x3b9, 0x1fbe), # \u0345ιι
           # GREEK SMALL LETTER IOTA WITH DIALYTIKA AND TONOS, GREEK SMALL LETTER IOTA WITH DIALYTIKA AND OXIA
           (0x390, 0x1fd3), # ΐΐ
           # GREEK SMALL LETTER UPSILON WITH DIALYTIKA AND TONOS, GREEK SMALL LETTER UPSILON WITH DIALYTIKA AND OXIA
           (0x3b0, 0x1fe3), # ΰΰ
           # GREEK SMALL LETTER BETA, GREEK BETA SYMBOL
           (0x3b2, 0x3d0), # βϐ
           # GREEK SMALL LETTER EPSILON, GREEK LUNATE EPSILON SYMBOL
           (0x3b5, 0x3f5), # εϵ
           # GREEK SMALL LETTER THETA, GREEK THETA SYMBOL
           (0x3b8, 0x3d1), # θϑ
           # GREEK SMALL LETTER KAPPA, GREEK KAPPA SYMBOL
           (0x3ba, 0x3f0), # κϰ
           # GREEK SMALL LETTER PI, GREEK PI SYMBOL
           (0x3c0, 0x3d6), # πϖ
           # GREEK SMALL LETTER RHO, GREEK RHO SYMBOL
           (0x3c1, 0x3f1), # ρϱ
           # GREEK SMALL LETTER FINAL SIGMA, GREEK SMALL LETTER SIGMA
           (0x3c2, 0x3c3), # ςσ
           # GREEK SMALL LETTER PHI, GREEK PHI SYMBOL
           (0x3c6, 0x3d5), # φϕ
           # CYRILLIC SMALL LETTER VE, CYRILLIC SMALL LETTER ROUNDED VE
           (0x432, 0x1c80), # вᲀ
           # CYRILLIC SMALL LETTER DE, CYRILLIC SMALL LETTER LONG-LEGGED DE
           (0x434, 0x1c81), # дᲁ
           # CYRILLIC SMALL LETTER O, CYRILLIC SMALL LETTER NARROW O
           (0x43e, 0x1c82), # оᲂ
           # CYRILLIC SMALL LETTER ES, CYRILLIC SMALL LETTER WIDE ES
           (0x441, 0x1c83), # сᲃ
           # CYRILLIC SMALL LETTER TE, CYRILLIC SMALL LETTER TALL TE, CYRILLIC SMALL LETTER THREE-LEGGED TE
           (0x442, 0x1c84, 0x1c85), # тᲄᲅ
           # CYRILLIC SMALL LETTER HARD SIGN, CYRILLIC SMALL LETTER TALL HARD SIGN
           (0x44a, 0x1c86), # ъᲆ
           # CYRILLIC SMALL LETTER YAT, CYRILLIC SMALL LETTER TALL YAT
           (0x463, 0x1c87), # ѣᲇ
           # CYRILLIC SMALL LETTER UNBLENDED UK, CYRILLIC SMALL LETTER MONOGRAPH UK
           (0x1c88, 0xa64b), # ᲈꙋ
           # LATIN SMALL LETTER S WITH DOT ABOVE, LATIN SMALL LETTER LONG S WITH DOT ABOVE
           (0x1e61, 0x1e9b), # ṡẛ
           # LATIN SMALL LIGATURE LONG S T, LATIN SMALL LIGATURE ST
           (0xfb05, 0xfb06), # ﬅﬆ
       )
       
       # Maps the lowercase code to lowercase codes which have the same uppercase.
>>>>>> _ignorecase_fixes = {i: tuple(j for j in t if i != j)
>>>>>>                      for t in _equivalences for i in t}
       
>>>>>> def _combine_flags(flags, add_flags, del_flags,
>>>>>>                    TYPE_FLAGS=sre_parse.TYPE_FLAGS):
  283:     if add_flags & TYPE_FLAGS:
>>>>>>         flags &= ~TYPE_FLAGS
  283:     return (flags | add_flags) & ~del_flags
       
>>>>>> def _compile(code, pattern, flags):
           # internal: compile a (sub)pattern
 6366:     emit = code.append
 6366:     _len = len
 6366:     LITERAL_CODES = _LITERAL_CODES
 6366:     REPEATING_CODES = _REPEATING_CODES
 6366:     SUCCESS_CODES = _SUCCESS_CODES
 6366:     ASSERT_CODES = _ASSERT_CODES
 6366:     iscased = None
 6366:     tolower = None
 6366:     fixes = None
 6366:     if flags & SRE_FLAG_IGNORECASE and not flags & SRE_FLAG_LOCALE:
 1480:         if flags & SRE_FLAG_UNICODE:
 1433:             iscased = _sre.unicode_iscased
 1433:             tolower = _sre.unicode_tolower
 1433:             fixes = _ignorecase_fixes
               else:
   47:             iscased = _sre.ascii_iscased
   47:             tolower = _sre.ascii_tolower
16467:     for op, av in pattern:
10101:         if op in LITERAL_CODES:
 5886:             if not flags & SRE_FLAG_IGNORECASE:
 4633:                 emit(op)
 4633:                 emit(av)
 1253:             elif flags & SRE_FLAG_LOCALE:
>>>>>>                 emit(OP_LOCALE_IGNORE[op])
>>>>>>                 emit(av)
 1253:             elif not iscased(av):
  874:                 emit(op)
  874:                 emit(av)
                   else:
  379:                 lo = tolower(av)
  379:                 if not fixes:  # ascii
   84:                     emit(OP_IGNORE[op])
   84:                     emit(lo)
  295:                 elif lo not in fixes:
  264:                     emit(OP_UNICODE_IGNORE[op])
  264:                     emit(lo)
                       else:
   31:                     emit(IN_UNI_IGNORE)
   31:                     skip = _len(code); emit(0)
   31:                     if op is NOT_LITERAL:
>>>>>>                         emit(NEGATE)
   93:                     for k in (lo,) + fixes[lo]:
   62:                         emit(LITERAL)
   62:                         emit(k)
   31:                     emit(FAILURE)
   31:                     code[skip] = _len(code) - skip
 4215:         elif op is IN:
 1459:             charset, hascased = _optimize_charset(av, iscased, tolower, fixes)
 1459:             if flags & SRE_FLAG_IGNORECASE and flags & SRE_FLAG_LOCALE:
>>>>>>                 emit(IN_LOC_IGNORE)
 1459:             elif not hascased:
 1410:                 emit(IN)
   49:             elif not fixes:  # ascii
    1:                 emit(IN_IGNORE)
                   else:
   48:                 emit(IN_UNI_IGNORE)
 1459:             skip = _len(code); emit(0)
 1459:             _compile_charset(charset, flags, code)
 1459:             code[skip] = _len(code) - skip
 2756:         elif op is ANY:
   35:             if flags & SRE_FLAG_DOTALL:
    7:                 emit(ANY_ALL)
                   else:
   28:                 emit(ANY)
 2721:         elif op in REPEATING_CODES:
 1524:             if flags & SRE_FLAG_TEMPLATE:
>>>>>>                 raise error("internal: unsupported template operator %r" % (op,))
 1524:             if _simple(av[2]):
 1193:                 if op is MAX_REPEAT:
 1148:                     emit(REPEAT_ONE)
                       else:
   45:                     emit(MIN_REPEAT_ONE)
 1193:                 skip = _len(code); emit(0)
 1193:                 emit(av[0])
 1193:                 emit(av[1])
 1193:                 _compile(code, av[2], flags)
 1193:                 emit(SUCCESS)
 1193:                 code[skip] = _len(code) - skip
                   else:
  331:                 emit(REPEAT)
  331:                 skip = _len(code); emit(0)
  331:                 emit(av[0])
  331:                 emit(av[1])
  331:                 _compile(code, av[2], flags)
  331:                 code[skip] = _len(code) - skip
  331:                 if op is MAX_REPEAT:
  322:                     emit(MAX_UNTIL)
                       else:
    9:                     emit(MIN_UNTIL)
 1197:         elif op is SUBPATTERN:
  218:             group, add_flags, del_flags, p = av
  218:             if group:
  205:                 emit(MARK)
  205:                 emit((group-1)*2)
                   # _compile_info(code, p, _combine_flags(flags, add_flags, del_flags))
  218:             _compile(code, p, _combine_flags(flags, add_flags, del_flags))
  218:             if group:
  205:                 emit(MARK)
  205:                 emit((group-1)*2+1)
  979:         elif op in SUCCESS_CODES:
>>>>>>             emit(op)
  979:         elif op in ASSERT_CODES:
  278:             emit(op)
  278:             skip = _len(code); emit(0)
  278:             if av[0] >= 0:
  234:                 emit(0) # look ahead
                   else:
   44:                 lo, hi = av[1].getwidth()
   44:                 if lo != hi:
>>>>>>                     raise error("look-behind requires fixed-width pattern")
   44:                 emit(lo) # look behind
  278:             _compile(code, av[1], flags)
  278:             emit(SUCCESS)
  278:             code[skip] = _len(code) - skip
  701:         elif op is CALL:
>>>>>>             emit(op)
>>>>>>             skip = _len(code); emit(0)
>>>>>>             _compile(code, av, flags)
>>>>>>             emit(SUCCESS)
>>>>>>             code[skip] = _len(code) - skip
  701:         elif op is AT:
  231:             emit(op)
  231:             if flags & SRE_FLAG_MULTILINE:
    2:                 av = AT_MULTILINE.get(av, av)
  231:             if flags & SRE_FLAG_LOCALE:
>>>>>>                 av = AT_LOCALE.get(av, av)
  231:             elif flags & SRE_FLAG_UNICODE:
  209:                 av = AT_UNICODE.get(av, av)
  231:             emit(av)
  470:         elif op is BRANCH:
  469:             emit(op)
  469:             tail = []
  469:             tailappend = tail.append
 4523:             for av in av[1]:
 4054:                 skip = _len(code); emit(0)
                       # _compile_info(code, av, flags)
 4054:                 _compile(code, av, flags)
 4054:                 emit(JUMP)
 4054:                 tailappend(_len(code)); emit(0)
 4054:                 code[skip] = _len(code) - skip
  469:             emit(FAILURE) # end of branch
 4523:             for tail in tail:
 4054:                 code[tail] = _len(code) - tail
    1:         elif op is CATEGORY:
>>>>>>             emit(op)
>>>>>>             if flags & SRE_FLAG_LOCALE:
>>>>>>                 av = CH_LOCALE[av]
>>>>>>             elif flags & SRE_FLAG_UNICODE:
>>>>>>                 av = CH_UNICODE[av]
>>>>>>             emit(av)
    1:         elif op is GROUPREF:
    1:             if not flags & SRE_FLAG_IGNORECASE:
>>>>>>                 emit(op)
    1:             elif flags & SRE_FLAG_LOCALE:
>>>>>>                 emit(GROUPREF_LOC_IGNORE)
    1:             elif not fixes:  # ascii
>>>>>>                 emit(GROUPREF_IGNORE)
                   else:
    1:                 emit(GROUPREF_UNI_IGNORE)
    1:             emit(av-1)
>>>>>>         elif op is GROUPREF_EXISTS:
>>>>>>             emit(op)
>>>>>>             emit(av[0]-1)
>>>>>>             skipyes = _len(code); emit(0)
>>>>>>             _compile(code, av[1], flags)
>>>>>>             if av[2]:
>>>>>>                 emit(JUMP)
>>>>>>                 skipno = _len(code); emit(0)
>>>>>>                 code[skipyes] = _len(code) - skipyes + 1
>>>>>>                 _compile(code, av[2], flags)
>>>>>>                 code[skipno] = _len(code) - skipno
                   else:
>>>>>>                 code[skipyes] = _len(code) - skipyes + 1
               else:
>>>>>>             raise error("internal: unsupported operand type %r" % (op,))
       
>>>>>> def _compile_charset(charset, flags, code):
           # compile charset subprogram
 1493:     emit = code.append
 3937:     for op, av in charset:
 2444:         emit(op)
 2444:         if op is NEGATE:
  176:             pass
 2268:         elif op is LITERAL:
  972:             emit(av)
 1296:         elif op is RANGE or op is RANGE_UNI_IGNORE:
  386:             emit(av[0])
  386:             emit(av[1])
  910:         elif op is CHARSET:
  251:             code.extend(av)
  659:         elif op is BIGCHARSET:
   64:             code.extend(av)
  595:         elif op is CATEGORY:
  595:             if flags & SRE_FLAG_LOCALE:
>>>>>>                 emit(CH_LOCALE[av])
  595:             elif flags & SRE_FLAG_UNICODE:
  501:                 emit(CH_UNICODE[av])
                   else:
   94:                 emit(av)
               else:
>>>>>>             raise error("internal: unsupported set operator %r" % (op,))
 1493:     emit(FAILURE)
       
>>>>>> def _optimize_charset(charset, iscased=None, fixup=None, fixes=None):
           # internal: optimize character set
 1493:     out = []
 1493:     tail = []
 1493:     charmap = bytearray(256)
 1493:     hascased = False
 8135:     for op, av in charset:
 6793:         while True:
 6793:             try:
 6793:                 if op is LITERAL:
 5114:                     if fixup:
  858:                         lo = fixup(av)
  858:                         charmap[lo] = 1
  833:                         if fixes and lo in fixes:
    5:                             for k in fixes[lo]:
    3:                                 charmap[k] = 1
  832:                         if not hascased and iscased(av):
    1:                             hascased = True
                           else:
 4256:                         charmap[av] = 1
 1679:                 elif op is RANGE:
  908:                     r = range(av[0], av[1]+1)
  908:                     if fixup:
  266:                         if fixes:
 4962:                             for i in map(fixup, r):
 4705:                                 charmap[i] = 1
 4705:                                 if i in fixes:
   44:                                     for k in fixes[i]:
   26:                                         charmap[k] = 1
                               else:
   27:                             for i in map(fixup, r):
   26:                                 charmap[i] = 1
  258:                         if not hascased:
  217:                             hascased = any(map(iscased, r))
                           else:
28008:                         for i in r:
27369:                             charmap[i] = 1
  771:                 elif op is NEGATE:
  176:                     out.append((op, av))
                       else:
  595:                     tail.append((op, av))
  417:             except IndexError:
  417:                 if len(charmap) == 256:
                           # character set contains non-UCS1 character codes
  151:                     charmap += b'\0' * 0xff00
  151:                     continue
                       # Character set contains non-BMP character codes.
                       # For range, all BMP characters in the range are already
                       # proceeded.
  266:                 if fixup:
>>>>>>                     hascased = True
                           # For now, IN_UNI_IGNORE+LITERAL and
                           # IN_UNI_IGNORE+RANGE_UNI_IGNORE work for all non-BMP
                           # characters, because two characters (at least one of
                           # which is not in the BMP) match case-insensitively
                           # if and only if:
                           # 1) c1.lower() == c2.lower()
                           # 2) c1.lower() == c2 or c1.lower().upper() == c2
                           # Also, both c.lower() and c.lower().upper() are single
                           # characters for every non-BMP character.
>>>>>>                     if op is RANGE:
>>>>>>                         op = RANGE_UNI_IGNORE
  266:                 tail.append((op, av))
 6642:             break
       
           # compress character map
 1493:     runs = []
 1493:     q = 0
 3215:     while True:
 3215:         p = charmap.find(1, q)
 3215:         if p < 0:
 1178:             break
 2037:         if len(runs) >= 2:
  315:             runs = None
  315:             break
 1722:         q = charmap.find(0, p)
 1722:         if q < 0:
>>>>>>             runs.append((p, len(charmap)))
>>>>>>             break
 1722:         runs.append((p, q))
 1493:     if runs is not None:
               # use literal/range
 2270:         for p, q in runs:
 1092:             if q - p == 1:
  706:                 out.append((LITERAL, p))
                   else:
  386:                 out.append((RANGE, (p, q - 1)))
 1178:         out += tail
               # if the case was changed or new representation is more compact
 1178:         if hascased or len(out) < len(charset):
  183:             return out, hascased
               # else original character set is good enough
  995:         return charset, hascased
       
           # use bitmap
  315:     if len(charmap) == 256:
  251:         data = _mk_bitmap(charmap)
  251:         out.append((CHARSET, data))
  251:         out += tail
  251:         return out, hascased
       
           # To represent a big charset, first a bitmap of all characters in the
           # set is constructed. Then, this bitmap is sliced into chunks of 256
           # characters, duplicate chunks are eliminated, and each chunk is
           # given a number. In the compiled expression, the charset is
           # represented by a 32-bit word sequence, consisting of one word for
           # the number of different chunks, a sequence of 256 bytes (64 words)
           # of chunk numbers indexed by their original chunk position, and a
           # sequence of 256-bit chunks (8 words each).
       
           # Compression is normally good: in a typical charset, large ranges of
           # Unicode will be either completely excluded (e.g. if only cyrillic
           # letters are to be matched), or completely included (e.g. if large
           # subranges of Kanji match). These ranges will be represented by
           # chunks of all one-bits or all zero-bits.
       
           # Matching can be also done efficiently: the more significant byte of
           # the Unicode character is an index into the chunk number, and the
           # less significant byte is a bit index in the chunk (just like the
           # CHARSET matching).
       
   64:     charmap = bytes(charmap) # should be hashable
   64:     comps = {}
   64:     mapping = bytearray(256)
   64:     block = 0
   64:     data = bytearray()
16448:     for i in range(0, 65536, 256):
16384:         chunk = charmap[i: i + 256]
16384:         if chunk in comps:
16097:             mapping[i // 256] = comps[chunk]
               else:
  287:             mapping[i // 256] = comps[chunk] = block
  287:             block += 1
  287:             data += chunk
   64:     data = _mk_bitmap(data)
   64:     data[0:0] = [block] + _bytes_to_codes(mapping)
   64:     out.append((BIGCHARSET, data))
   64:     out += tail
   64:     return out, hascased
       
>>>>>> _CODEBITS = _sre.CODESIZE * 8
>>>>>> MAXCODE = (1 << _CODEBITS) - 1
>>>>>> _BITS_TRANS = b'0' + b'1' * 255
>>>>>> def _mk_bitmap(bits, _CODEBITS=_CODEBITS, _int=int):
  315:     s = bits.translate(_BITS_TRANS)[::-1]
 5249:     return [_int(s[i - _CODEBITS: i], 2)
  315:             for i in range(len(s), 0, -_CODEBITS)]
       
>>>>>> def _bytes_to_codes(b):
           # Convert block indices to word array
   64:     a = memoryview(b).cast('I')
   64:     assert a.itemsize == _sre.CODESIZE
   64:     assert len(a) * a.itemsize == len(b)
   64:     return a.tolist()
       
>>>>>> def _simple(p):
           # check if this subpattern is a "simple" operator
 1524:     if len(p) != 1:
  179:         return False
 1345:     op, av = p[0]
 1345:     if op is SUBPATTERN:
   12:         return av[0] is None and _simple(av[-1])
 1333:     return op in _UNIT_CODES
       
>>>>>> def _generate_overlap_table(prefix):
           """
           Generate an overlap table for the following prefix.
           An overlap table is a table of the same size as the prefix which
           informs about the potential self-overlap for each index in the prefix:
           - if overlap[i] == 0, prefix[i:] can't overlap prefix[0:...]
           - if overlap[i] == k with 0 < k <= i, prefix[i-k+1:i+1] overlaps with
             prefix[0:k]
           """
   74:     table = [0] * len(prefix)
  162:     for i in range(1, len(prefix)):
   88:         idx = table[i - 1]
   90:         while prefix[i] != prefix[idx]:
   83:             if idx == 0:
   81:                 table[i] = 0
   81:                 break
    2:             idx = table[idx - 1]
               else:
    7:             table[i] = idx + 1
   74:     return table
       
>>>>>> def _get_iscased(flags):
  524:     if not flags & SRE_FLAG_IGNORECASE:
  393:         return None
  131:     elif flags & SRE_FLAG_UNICODE:
  122:         return _sre.unicode_iscased
           else:
    9:         return _sre.ascii_iscased
       
>>>>>> def _get_literal_prefix(pattern, flags):
           # look for literal prefix
  322:     prefix = []
  322:     prefixappend = prefix.append
  322:     prefix_skip = None
  322:     iscased = _get_iscased(flags)
  491:     for op, av in pattern.data:
  473:         if op is LITERAL:
  171:             if iscased and iscased(av):
    9:                 break
  162:             prefixappend(av)
  302:         elif op is SUBPATTERN:
   46:             group, add_flags, del_flags, p = av
   46:             flags1 = _combine_flags(flags, add_flags, del_flags)
   46:             if flags1 & SRE_FLAG_IGNORECASE and flags1 & SRE_FLAG_LOCALE:
>>>>>>                 break
   46:             prefix1, prefix_skip1, got_all = _get_literal_prefix(p, flags1)
   46:             if prefix_skip is None:
   46:                 if group is not None:
   33:                     prefix_skip = len(prefix)
   13:                 elif prefix_skip1 is not None:
>>>>>>                     prefix_skip = len(prefix) + prefix_skip1
   46:             prefix.extend(prefix1)
   46:             if not got_all:
   39:                 break
               else:
  256:             break
           else:
   18:         return prefix, prefix_skip, True
  304:     return prefix, prefix_skip, False
       
>>>>>> def _get_charset_prefix(pattern, flags):
  221:     while True:
  221:         if not pattern.data:
>>>>>>             return None
  221:         op, av = pattern.data[0]
  221:         if op is not SUBPATTERN:
  202:             break
   19:         group, add_flags, del_flags, pattern = av
   19:         flags = _combine_flags(flags, add_flags, del_flags)
   19:         if flags & SRE_FLAG_IGNORECASE and flags & SRE_FLAG_LOCALE:
>>>>>>             return None
       
  202:     iscased = _get_iscased(flags)
  202:     if op is LITERAL:
    4:         if iscased and iscased(av):
    4:             return None
>>>>>>         return [(op, av)]
  198:     elif op is BRANCH:
   37:         charset = []
   37:         charsetappend = charset.append
 3075:         for p in av[1]:
 3060:             if not p:
>>>>>>                 return None
 3060:             op, av = p[0]
 3060:             if op is LITERAL and not (iscased and iscased(av)):
 3038:                 charsetappend((op, av))
                   else:
   22:                 return None
   15:         return charset
  161:     elif op is IN:
   23:         charset = av
   23:         if iscased:
    7:             for op, av in charset:
    7:                 if op is LITERAL:
    3:                     if iscased(av):
>>>>>>                         return None
    4:                 elif op is RANGE:
    4:                     if av[1] > 0xffff:
>>>>>>                         return None
    4:                     if any(map(iscased, range(av[0], av[1]+1))):
    4:                         return None
   19:         return charset
  138:     return None
       
>>>>>> def _compile_info(code, pattern, flags):
           # internal: compile an info block.  in the current version,
           # this contains min/max pattern width, and an optional literal
           # prefix or a character map
  292:     lo, hi = pattern.getwidth()
  292:     if hi > MAXCODE:
>>>>>>         hi = MAXCODE
  292:     if lo == 0:
   16:         code.extend([INFO, 4, 0, lo, hi])
   16:         return
           # look for a literal prefix
  276:     prefix = []
  276:     prefix_skip = 0
  276:     charset = [] # not used
  276:     if not (flags & SRE_FLAG_IGNORECASE and flags & SRE_FLAG_LOCALE):
               # look for literal prefix
  276:         prefix, prefix_skip, got_all = _get_literal_prefix(pattern, flags)
               # if no prefix, look for charset prefix
  276:         if not prefix:
  202:             charset = _get_charset_prefix(pattern, flags)
       ##     if prefix:
       ##         print("*** PREFIX", prefix, prefix_skip)
       ##     if charset:
       ##         print("*** CHARSET", charset)
           # add an info block
  276:     emit = code.append
  276:     emit(INFO)
  276:     skip = len(code); emit(0)
           # literal flag
  276:     mask = 0
  276:     if prefix:
   74:         mask = SRE_INFO_PREFIX
   74:         if prefix_skip is None and got_all:
   11:             mask = mask | SRE_INFO_LITERAL
  202:     elif charset:
   34:         mask = mask | SRE_INFO_CHARSET
  276:     emit(mask)
           # pattern length
  276:     if lo < MAXCODE:
  276:         emit(lo)
           else:
>>>>>>         emit(MAXCODE)
>>>>>>         prefix = prefix[:MAXCODE]
  276:     emit(min(hi, MAXCODE))
           # add literal prefix
  276:     if prefix:
   74:         emit(len(prefix)) # length
   74:         if prefix_skip is None:
   59:             prefix_skip =  len(prefix)
   74:         emit(prefix_skip) # skip
   74:         code.extend(prefix)
               # generate overlap table
   74:         code.extend(_generate_overlap_table(prefix))
  202:     elif charset:
   34:         charset, hascased = _optimize_charset(charset)
   34:         assert not hascased
   34:         _compile_charset(charset, flags, code)
  276:     code[skip] = len(code) - skip
       
>>>>>> def isstring(obj):
  584:     return isinstance(obj, (str, bytes))
       
>>>>>> def _code(p, flags):
       
  292:     flags = p.state.flags | flags
  292:     code = []
       
           # compile info block
  292:     _compile_info(code, p, flags)
       
           # compile the pattern
  292:     _compile(code, p.data, flags)
       
  292:     code.append(SUCCESS)
       
  292:     return code
       
>>>>>> def _hex_code(code):
>>>>>>     return '[%s]' % ', '.join('%#0*x' % (_sre.CODESIZE*2+2, x) for x in code)
       
>>>>>> def dis(code):
>>>>>>     import sys
       
>>>>>>     labels = set()
>>>>>>     level = 0
>>>>>>     offset_width = len(str(len(code) - 1))
       
>>>>>>     def dis_(start, end):
>>>>>>         def print_(*args, to=None):
>>>>>>             if to is not None:
>>>>>>                 labels.add(to)
>>>>>>                 args += ('(to %d)' % (to,),)
>>>>>>             print('%*d%s ' % (offset_width, start, ':' if start in labels else '.'),
>>>>>>                   end='  '*(level-1))
>>>>>>             print(*args)
       
>>>>>>         def print_2(*args):
>>>>>>             print(end=' '*(offset_width + 2*level))
>>>>>>             print(*args)
       
               nonlocal level
>>>>>>         level += 1
>>>>>>         i = start
>>>>>>         while i < end:
>>>>>>             start = i
>>>>>>             op = code[i]
>>>>>>             i += 1
>>>>>>             op = OPCODES[op]
>>>>>>             if op in (SUCCESS, FAILURE, ANY, ANY_ALL,
>>>>>>                       MAX_UNTIL, MIN_UNTIL, NEGATE):
>>>>>>                 print_(op)
>>>>>>             elif op in (LITERAL, NOT_LITERAL,
>>>>>>                         LITERAL_IGNORE, NOT_LITERAL_IGNORE,
>>>>>>                         LITERAL_UNI_IGNORE, NOT_LITERAL_UNI_IGNORE,
>>>>>>                         LITERAL_LOC_IGNORE, NOT_LITERAL_LOC_IGNORE):
>>>>>>                 arg = code[i]
>>>>>>                 i += 1
>>>>>>                 print_(op, '%#02x (%r)' % (arg, chr(arg)))
>>>>>>             elif op is AT:
>>>>>>                 arg = code[i]
>>>>>>                 i += 1
>>>>>>                 arg = str(ATCODES[arg])
>>>>>>                 assert arg[:3] == 'AT_'
>>>>>>                 print_(op, arg[3:])
>>>>>>             elif op is CATEGORY:
>>>>>>                 arg = code[i]
>>>>>>                 i += 1
>>>>>>                 arg = str(CHCODES[arg])
>>>>>>                 assert arg[:9] == 'CATEGORY_'
>>>>>>                 print_(op, arg[9:])
>>>>>>             elif op in (IN, IN_IGNORE, IN_UNI_IGNORE, IN_LOC_IGNORE):
>>>>>>                 skip = code[i]
>>>>>>                 print_(op, skip, to=i+skip)
>>>>>>                 dis_(i+1, i+skip)
>>>>>>                 i += skip
>>>>>>             elif op in (RANGE, RANGE_UNI_IGNORE):
>>>>>>                 lo, hi = code[i: i+2]
>>>>>>                 i += 2
>>>>>>                 print_(op, '%#02x %#02x (%r-%r)' % (lo, hi, chr(lo), chr(hi)))
>>>>>>             elif op is CHARSET:
>>>>>>                 print_(op, _hex_code(code[i: i + 256//_CODEBITS]))
>>>>>>                 i += 256//_CODEBITS
>>>>>>             elif op is BIGCHARSET:
>>>>>>                 arg = code[i]
>>>>>>                 i += 1
>>>>>>                 mapping = list(b''.join(x.to_bytes(_sre.CODESIZE, sys.byteorder)
>>>>>>                                         for x in code[i: i + 256//_sre.CODESIZE]))
>>>>>>                 print_(op, arg, mapping)
>>>>>>                 i += 256//_sre.CODESIZE
>>>>>>                 level += 1
>>>>>>                 for j in range(arg):
>>>>>>                     print_2(_hex_code(code[i: i + 256//_CODEBITS]))
>>>>>>                     i += 256//_CODEBITS
>>>>>>                 level -= 1
>>>>>>             elif op in (MARK, GROUPREF, GROUPREF_IGNORE, GROUPREF_UNI_IGNORE,
>>>>>>                         GROUPREF_LOC_IGNORE):
>>>>>>                 arg = code[i]
>>>>>>                 i += 1
>>>>>>                 print_(op, arg)
>>>>>>             elif op is JUMP:
>>>>>>                 skip = code[i]
>>>>>>                 print_(op, skip, to=i+skip)
>>>>>>                 i += 1
>>>>>>             elif op is BRANCH:
>>>>>>                 skip = code[i]
>>>>>>                 print_(op, skip, to=i+skip)
>>>>>>                 while skip:
>>>>>>                     dis_(i+1, i+skip)
>>>>>>                     i += skip
>>>>>>                     start = i
>>>>>>                     skip = code[i]
>>>>>>                     if skip:
>>>>>>                         print_('branch', skip, to=i+skip)
                           else:
>>>>>>                         print_(FAILURE)
>>>>>>                 i += 1
>>>>>>             elif op in (REPEAT, REPEAT_ONE, MIN_REPEAT_ONE):
>>>>>>                 skip, min, max = code[i: i+3]
>>>>>>                 if max == MAXREPEAT:
>>>>>>                     max = 'MAXREPEAT'
>>>>>>                 print_(op, skip, min, max, to=i+skip)
>>>>>>                 dis_(i+3, i+skip)
>>>>>>                 i += skip
>>>>>>             elif op is GROUPREF_EXISTS:
>>>>>>                 arg, skip = code[i: i+2]
>>>>>>                 print_(op, arg, skip, to=i+skip)
>>>>>>                 i += 2
>>>>>>             elif op in (ASSERT, ASSERT_NOT):
>>>>>>                 skip, arg = code[i: i+2]
>>>>>>                 print_(op, skip, arg, to=i+skip)
>>>>>>                 dis_(i+2, i+skip)
>>>>>>                 i += skip
>>>>>>             elif op is INFO:
>>>>>>                 skip, flags, min, max = code[i: i+4]
>>>>>>                 if max == MAXREPEAT:
>>>>>>                     max = 'MAXREPEAT'
>>>>>>                 print_(op, skip, bin(flags), min, max, to=i+skip)
>>>>>>                 start = i+4
>>>>>>                 if flags & SRE_INFO_PREFIX:
>>>>>>                     prefix_len, prefix_skip = code[i+4: i+6]
>>>>>>                     print_2('  prefix_skip', prefix_skip)
>>>>>>                     start = i + 6
>>>>>>                     prefix = code[start: start+prefix_len]
>>>>>>                     print_2('  prefix',
>>>>>>                             '[%s]' % ', '.join('%#02x' % x for x in prefix),
>>>>>>                             '(%r)' % ''.join(map(chr, prefix)))
>>>>>>                     start += prefix_len
>>>>>>                     print_2('  overlap', code[start: start+prefix_len])
>>>>>>                     start += prefix_len
>>>>>>                 if flags & SRE_INFO_CHARSET:
>>>>>>                     level += 1
>>>>>>                     print_2('in')
>>>>>>                     dis_(start, i+skip)
>>>>>>                     level -= 1
>>>>>>                 i += skip
                   else:
>>>>>>                 raise ValueError(op)
       
>>>>>>         level -= 1
       
>>>>>>     dis_(0, len(code))
       
       
>>>>>> def compile(p, flags=0):
           # internal: convert pattern list to internal format
       
  292:     if isstring(p):
  292:         pattern = p
  292:         p = sre_parse.parse(p, flags)
           else:
>>>>>>         pattern = None
       
  292:     code = _code(p, flags)
       
  292:     if flags & SRE_FLAG_DEBUG:
>>>>>>         print()
>>>>>>         dis(code)
       
           # map in either direction
  292:     groupindex = p.state.groupdict
  292:     indexgroup = [None] * p.state.groups
  353:     for k, i in groupindex.items():
   61:         indexgroup[i] = k
       
  584:     return _sre.compile(
  292:         pattern, flags | p.state.flags, code,
  292:         p.state.groups-1,
  292:         groupindex, tuple(indexgroup)
               )

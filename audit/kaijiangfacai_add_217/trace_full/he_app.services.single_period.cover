    1: import re
       
    1: from bs4 import BeautifulSoup
       
    1: from he_app.domain.models import CandidateEvidence, FailureInfo, ParseResult, Site
    1: from he_app.domain.policies import normalize_digit_text, normalize_text
    1: from he_app.parsers.common import (
           build_candidate_parts,
           build_latest_candidate_parts,
           extract_values,
           has_kill_sum_keyword,
           split_period_segments,
       )
    1: from he_app.parsers.dedicated.history import BATCH_NEW_DEDICATED_SITE_IDS
    1: from he_app.parsers.dedicated.tables import (
           KAIJIANGFACAI_HEADERS,
           KAIJIANGFACAI_SITE_ID,
           KAIJIANGFACAI_TITLE_RE,
           extract_kaijiangfacai_kill_sum_period_values,
           site_rule,
       )
    1: from he_app.parsers.registry import evaluate_site_documents
    1: from he_app.validation.period import contains_exact_period, is_valid_sum_value
    1: from he_app.validation.record_boundary import record_id_from_url
       
       
    1: Evaluation = tuple[str | None, str, list[str], str | None]
       
       
    1: def _contains_sum_value(text: str, value: str) -> bool:
   69:     number = int(value[:2])
   69:     token = rf"0?{number}" if number < 10 else str(number)
  138:     return bool(
  138:         re.search(
   69:             rf"(?:合\s*{token}(?!\d)|(?<!\d){token}\s*合|[\[【(（]\s*{token}\s*[\]】)）])",
   69:             text,
               )
           )
       
       
    1: def _sum_number_pattern(value: str) -> str:
   47:     number = int(value[:2])
   47:     return rf"0?{number}" if number < 10 else str(number)
       
       
    1: def _dedicated_document_keyword(site: Site, period: int, values: list[str], text: str) -> str | None:
  123:     if site.site_id == "s093_a_909922_article_aspx_id_3694545" and len(values) == 1:
>>>>>>         value = _sum_number_pattern(values[0])
>>>>>>         row_re = re.compile(
>>>>>>             rf"(?<!\d){period}\s*期\s*杀\s*[:：]\s*[\[【]\s*{value}\s*合\s*[\]】]\s*[开開]"
               )
>>>>>>         if row_re.search(text):
>>>>>>             return "亮劍原创公式杀合"
       
  123:     if site.site_id == "s073_shuqhbq" and len(values) == 1:
   29:         anchor_re = re.compile(r"(?:绝|絕)\s*(?:杀|殺)\s*①\s*段\s*①\s*合")
   29:         anchor = anchor_re.search(text)
   29:         if anchor is None:
   28:             return None
    1:         section = text[anchor.start():]
    1:         next_anchor = re.search(r"(?:绝|絕)\s*(?:杀|殺)\s*三\s*尾", section[anchor.end() - anchor.start():])
    1:         if next_anchor is not None:
>>>>>>             section = section[: anchor.end() - anchor.start() + next_anchor.start()]
    1:         value = _sum_number_pattern(values[0])
    2:         row_re = re.compile(
    1:             rf"(?<!\d){period}\s*期\s*(?:杀|殺)\s*\[\s*{value}\s*合\s*\]\s*(?:开|開)"
               )
    1:         return "绝杀①段①合" if row_re.search(section) else None
       
   94:     if site.site_id == "s085_kcvpleh" and len(values) == 2:
    4:         anchor_re = re.compile(
    2:             r"(?:澳门|澳門)?\s*女人味[^期]{0,50}(?:绝|絕)\s*(?:杀|殺)\s*一\s*尾[^期]{0,20}二\s*合"
               )
    2:         anchor = anchor_re.search(text)
    2:         if anchor is None:
>>>>>>             return None
    2:         first = _sum_number_pattern(values[0])
    2:         second = _sum_number_pattern(values[1])
    4:         row_re = re.compile(
    6:             rf"(?<!\d){period}\s*期\s*[;:]?\s*(?:绝|絕)\s*(?:杀|殺)"
                   rf"[^期]{{0,40}}?\[\s*\d{{1,2}}\s*尾\s*\][^期]{{0,20}}?"
    4:             rf"\[\s*{first}\s*合\s*[-－]\s*{second}\s*合\s*\]\s*(?:开|開)"
               )
    2:         return "女人味绝杀一尾二合" if row_re.search(text[anchor.start():]) else None
       
   92:     if site.site_id in BATCH_NEW_DEDICATED_SITE_IDS and len(values) == 1:
   42:         value = _sum_number_pattern(values[0])
   84:         abbreviated_row_re = re.compile(
   84:             rf"(?<!\d){period}\s*期\s*[:：]?\s*杀\s*[:：]?\s*"
   42:             rf"[\[【\(（][^期]{{0,20}}?{value}\s*合[^期]{{0,12}}?[\]】\)）][^期]{{0,20}}?[开開]"
               )
   42:         if abbreviated_row_re.search(text):
    2:             return "专属绝杀一合栏目"
       
   90:     if has_kill_sum_keyword(text, allow_weak=True):
   90:         return "绝杀一合"
>>>>>>     if "杀合" in text:
>>>>>>         return "杀合"
>>>>>>     return None
       
       
    1: def _authority_groups(documents: list[str]) -> list[list[str]]:
           """Keep each physical document as an independent candidate authority."""
  338:     return [[document] for document in documents]
       
       
    1: def _allowed_documents(site: Site, documents: list[str]) -> list[str]:
  109:     allowed_fetch_kinds = site_rule(site).allowed_fetch_kinds
  109:     if not allowed_fetch_kinds:
  105:         return documents
   18:     return [
    4:         document
    4:         for document in documents
   14:         if str(getattr(document, "fetch_kind", "legacy") or "legacy") in allowed_fetch_kinds
           ]
       
       
    1: def _target_values(documents: list[str], period: int, allow_weak: bool) -> tuple[set[str], list[str]]:
  120:     values: set[str] = set()
  120:     lines: list[str] = []
  227:     for line, _has_locator in build_candidate_parts(documents, period, allow_weak):
  107:         extracted = extract_values(line, allow_weak)
  107:         if len(extracted) != 1:
>>>>>>             continue
  107:         values.add(extracted[0])
  107:         lines.append(line)
  120:     return values, lines
       
       
    1: def _has_complete_records(documents: list[str], allow_weak: bool) -> bool:
   15:     return bool(build_latest_candidate_parts(documents, allow_weak))
       
       
    1: def _candidate_segments(document: str, period: int) -> list[tuple[str, int, int]]:
   25:     soup = BeautifulSoup(document, "html.parser")
   25:     text = soup.get_text(" ", strip=True) or document
   25:     normalized_document = normalize_digit_text(normalize_text(text))
   25:     chunks: list[str] = []
   46:     for tag in soup.find_all(["p", "div", "td", "tr", "li", "font", "span", "b", "strong"]):
   21:         tag_text = normalize_digit_text(normalize_text(tag.get_text(" ", strip=True)))
   21:         if tag_text:
   21:             chunks.append(tag_text)
   25:     line_text = soup.get_text("\n", strip=True) if soup.find() else str(document)
  399:     chunks.extend(
  108:         normalize_digit_text(normalize_text(line))
   25:         for line in line_text.splitlines()
  108:         if normalize_text(line)
           )
       
   25:     segments: list[tuple[str, int, int]] = []
   25:     seen: set[str] = set()
  154:     for chunk in chunks:
  129:         candidates = split_period_segments(chunk, period)
  129:         if not candidates and contains_exact_period(chunk, period):
>>>>>>             candidates = [chunk]
  174:         for segment in candidates:
   45:             if segment in seen or len(segment) > 500:
    8:                 continue
   37:             seen.add(segment)
   37:             start = normalized_document.find(segment)
   37:             segments.append((segment, start, start + len(segment)))
   25:     return segments
       
       
    1: def _detail_is_self_contained(site: Site, period: int, values: list[str], detail: str) -> bool:
   37:     normalized = normalize_digit_text(normalize_text(detail))
   37:     if not contains_exact_period(normalized, period):
>>>>>>         return False
   37:     if _dedicated_document_keyword(site, period, values, normalized) is None:
>>>>>>         return False
  112:     return all(_contains_sum_value(normalized, value) for value in values)
       
       
    1: def _special_segment_is_bounded(site_id: str, text: str, start: int, end: int) -> bool:
    2:     if site_id == "s058_vkjwinyt":
    1:         anchor = re.search(r"澳门综合杀", text)
    1:         if anchor is None:
>>>>>>             return False
    1:         section = text[anchor.start() :]
    1:         stop = re.search(r"澳门六合彩合数属性|澳彩合数属性|合数属性|其他栏目|上一篇|下一篇|Copyright", section)
    1:         limit = anchor.start() + stop.start() if stop is not None else len(text)
    1:         return anchor.start() <= start < end <= limit
       
    1:     if site_id == "s073_shuqhbq":
>>>>>>         anchor = re.search(r"绝杀\s*①\s*段\s*①\s*合", text)
>>>>>>         if anchor is None:
>>>>>>             return False
>>>>>>         section = text[anchor.end() :]
>>>>>>         stop = re.search(r"绝杀\s*三\s*尾", section)
>>>>>>         limit = anchor.end() + stop.start() if stop is not None else len(text)
>>>>>>         return anchor.start() <= start < end <= limit
       
    1:     if site_id == "s085_kcvpleh":
    1:         anchor = re.search(r"(?:澳门|澳門)?\s*女人味", text)
    1:         if anchor is None:
>>>>>>             return False
    1:         section = text[anchor.start() :]
    1:         stop = re.search(r"澳门六合彩合数属性|澳彩合数属性|合数属性|上一篇|下一篇|Copyright", section)
    1:         limit = anchor.start() + stop.start() if stop is not None else len(text)
    1:         return anchor.start() <= start < end <= limit
       
>>>>>>     return False
       
       
    5: def _kaijiangfacai_evidence_bounds(
    3:     document: str, period: int, value: str
    1: ) -> tuple[int, int] | None:
   11:     extracted = extract_kaijiangfacai_kill_sum_period_values([document])
   11:     if extracted.get(period) != value:
>>>>>>         return None
       
   11:     soup = BeautifulSoup(document, "html.parser")
   22:     normalized_document = normalize_digit_text(
   11:         normalize_text(soup.get_text(" ", strip=True) or document)
           )
   11:     expected_number = int(value[:2])
   11:     for title in soup.select(".list-title"):
   11:         title_text = normalize_text(title.get_text(" ", strip=True))
   11:         if KAIJIANGFACAI_TITLE_RE.fullmatch(title_text) is None:
>>>>>>             continue
   11:         table = title.find_next("table")
   11:         if table is None:
>>>>>>             continue
   11:         header = table.find("tr")
  165:         headers = tuple(
   66:             normalize_text(cell.get_text(" ", strip=True))
   11:             for cell in header.find_all(["th", "td"], recursive=False)
   11:         ) if header is not None else ()
   11:         if headers != KAIJIANGFACAI_HEADERS:
>>>>>>             continue
   69:         for row in table.find_all("tr"):
 1035:             cells = [
  414:                 normalize_digit_text(normalize_text(cell.get_text(" ", strip=True)))
   69:                 for cell in row.find_all(["th", "td"], recursive=False)
                   ]
   69:             if len(cells) != len(KAIJIANGFACAI_HEADERS):
>>>>>>                 continue
   69:             period_match = re.fullmatch(r"(\d{1,4})\s*期", cells[0])
   69:             value_match = re.fullmatch(r"(0?[1-9]|1[0-3])", cells[3])
                   if (
   69:                 period_match is None
   58:                 or value_match is None
   58:                 or int(period_match.group(1)) != period
   11:                 or int(value_match.group(1)) != expected_number
                   ):
   58:                 continue
   11:             title_start = normalized_document.find(normalize_text(title_text))
   11:             row_text = normalize_digit_text(normalize_text(row.get_text(" ", strip=True)))
   11:             row_start = normalized_document.find(row_text, max(title_start, 0))
   11:             if row_start >= 0:
   11:                 return row_start, row_start + len(row_text)
>>>>>>     return None
       
       
    7: def build_document_evidence(
    1:     site: Site,
    1:     period: int,
    1:     values: list[str],
    1:     detail: str,
    1:     documents: list[str],
    1: ) -> tuple[CandidateEvidence, ...]:
   37:     if not _detail_is_self_contained(site, period, values, detail):
>>>>>>         return ()
       
   37:     article_id = record_id_from_url(site.url)
   37:     synthetic_detail_sites = {"s058_vkjwinyt", "s073_shuqhbq", "s085_kcvpleh"}
   37:     evidence: list[CandidateEvidence] = []
  101:     for index, document in enumerate(documents):
   64:         if article_id is not None and article_id not in document:
>>>>>>             continue
   64:         text = BeautifulSoup(document, "html.parser").get_text(" ", strip=True) or document
   64:         normalized_document = normalize_digit_text(normalize_text(text))
   64:         keyword = _dedicated_document_keyword(site, period, values, normalized_document)
   64:         if keyword is None:
   28:             continue
       
   36:         if site.site_id == KAIJIANGFACAI_SITE_ID and len(values) == 1:
   11:             bounds = _kaijiangfacai_evidence_bounds(document, period, values[0])
   11:             if bounds is None:
>>>>>>                 continue
   11:             start, end = bounds
   11:             source_url = str(getattr(document, "source_url", "") or site.url)
   11:             authority_id = str(getattr(document, "authority_id", "legacy") or "legacy")
   11:             document_id = str(getattr(document, "document_id", "") or f"document:{index}")
   22:             actual_position = len(
   11:                 re.findall(r"(?<!\d)\d{1,4}\s*期", normalized_document[:start])
                   )
   22:             evidence.append(
  198:                 CandidateEvidence(
   11:                     site_id=site.site_id,
   11:                     period=period,
   11:                     values=tuple(values),
   11:                     source_url=source_url,
   11:                     document_id=document_id,
   11:                     row_order=actual_position,
   11:                     raw_line=detail,
   11:                     article_id=article_id,
   11:                     keyword="开奖发财综合杀料表杀合列",
   11:                     fetch_kind=str(getattr(document, "fetch_kind", "legacy")),
   11:                     document_type=str(getattr(document, "document_type", "text")),
   11:                     parent_url=str(getattr(document, "parent_url", "") or site.url),
   11:                     authority_id=authority_id,
   11:                     block_id=f"{authority_id}:{start}:{end}",
   11:                     block_start=start,
   11:                     block_end=end,
   11:                     actual_position=actual_position,
                       )
                   )
   11:             continue
       
   25:         matched_segment: tuple[str, int, int] | None = None
   31:         for segment, start, end in _candidate_segments(document, period):
   85:             if not all(_contains_sum_value(segment, value) for value in values):
    6:                 continue
   24:             if site.site_id not in synthetic_detail_sites:
   22:                 if _dedicated_document_keyword(site, period, values, segment) is None:
>>>>>>                     continue
   22:                 segment_values = extract_values(segment, allow_weak=True)
   22:                 if segment_values and set(segment_values) != set(values):
>>>>>>                     continue
    2:             elif not _special_segment_is_bounded(site.site_id, normalized_document, start, end):
>>>>>>                 continue
   24:             matched_segment = (segment, start, end)
   24:             break
   25:         if matched_segment is None:
    1:             continue
       
   24:         segment, start, end = matched_segment
   24:         source_url = str(getattr(document, "source_url", "") or site.url)
   24:         authority_id = str(getattr(document, "authority_id", "legacy") or "legacy")
   24:         document_id = str(getattr(document, "document_id", "") or f"document:{index}")
   24:         actual_position = len(re.findall(r"(?<!\d)\d{1,4}\s*期", normalized_document[:max(start, 0)]))
   48:         evidence.append(
  432:             CandidateEvidence(
   24:                 site_id=site.site_id,
   24:                 period=period,
   24:                 values=tuple(values),
   24:                 source_url=source_url,
   24:                 document_id=document_id,
   24:                 row_order=actual_position,
   24:                 raw_line=detail,
   24:                 article_id=article_id,
   24:                 keyword=keyword,
   24:                 fetch_kind=str(getattr(document, "fetch_kind", "legacy")),
   24:                 document_type=str(getattr(document, "document_type", "text")),
   24:                 parent_url=str(getattr(document, "parent_url", "") or site.url),
   24:                 authority_id=authority_id,
   24:                 block_id=f"{authority_id}:{max(start, 0)}:{end}",
   24:                 block_start=max(start, 0),
   24:                 block_end=end,
   24:                 actual_position=actual_position,
                   )
               )
   37:     return tuple(evidence)
       
       
    5: def _cross_authority_conflict(
    1:     site: Site,
    1:     period: int,
    1:     evaluations: list[tuple[list[str], Evaluation, set[str], list[str]]],
    1: ) -> FailureInfo | None:
  110:     signatures_by_authority: list[tuple[set[tuple[str, ...]], list[str]]] = []
  232:     for _documents, evaluation, _strict_values, _strict_lines in evaluations:
  122:         result, detail, result_values, _reason = evaluation
  122:         signatures: set[tuple[str, ...]] = set()
  122:         lines: list[str] = []
  122:         if result is not None and result_values:
   31:             signatures.add(tuple(result_values))
   31:             lines.append(detail)
  122:         if signatures:
   31:             signatures_by_authority.append((signatures, lines))
  110:     if len(signatures_by_authority) < 2:
  106:         return None
   44:     unique_signatures = {
    8:         signature
   12:         for signatures, _lines in signatures_by_authority
    8:         for signature in signatures
           }
    4:     if len(unique_signatures) <= 1:
    3:         return None
    2:     unique_values = sorted(
   11:         {
    2:             value
    1:             for signature in unique_signatures
    2:             for value in signature
               },
    3:         key=lambda value: int(value[:2]),
           )
    6:     conflict_lines = [line for _signatures, lines in signatures_by_authority for line in lines]
    2:     return FailureInfo(
    1:         "候选冲突",
    2:         f"{site.name} {period}期跨来源候选结果冲突: {' / '.join(unique_values)}；"
    1:         f"候选: {' | '.join(conflict_lines[:5])}",
           )
       
       
    1: def parse_site_period(site: Site, period: int, documents: list[str]) -> ParseResult:
  109:     rule = site_rule(site)
  109:     documents = _allowed_documents(site, documents)
  109:     evaluations: list[tuple[list[str], Evaluation, set[str], list[str]]] = []
  229:     for authority_documents in _authority_groups(documents):
  120:         evaluation = evaluate_site_documents(site, period, authority_documents)
  240:         strict_values, strict_lines = _target_values(
  120:             authority_documents, period, rule.allow_weak_kill_sum_keyword
               )
  120:         evaluations.append((authority_documents, evaluation, strict_values, strict_lines))
       
  109:     conflict = _cross_authority_conflict(site, period, evaluations)
  109:     if conflict is not None:
    1:         return ParseResult(False, failure=conflict)
       
  108:     selected: tuple[list[str], Evaluation, set[str], list[str]] | None = None
  198:     for item in evaluations:
  115:         if item[1][0] is not None:
   25:             selected = item
   25:             break
  108:     if selected is None:
   99:         for item in evaluations:
   86:             _authority_documents, evaluation, _strict_values, _strict_lines = item
   86:             _result, _detail, _values, reason = evaluation
   86:             if reason in {"方向范围外", "超出范围", "候选冲突", "数据不完整"}:
   70:                 selected = item
   70:                 break
  108:     if selected is None:
   25:         for item in evaluations:
   15:             authority_documents, _evaluation, _strict_values, _strict_lines = item
   15:             if _has_complete_records(authority_documents, rule.allow_weak_kill_sum_keyword):
    3:                 selected = item
    3:                 break
  108:     if selected is None and evaluations:
   32:         selected = next((item for item in evaluations if item[1][0] is not None), evaluations[-1])
  108:     if selected is None:
>>>>>>         return ParseResult(False, failure=FailureInfo("未找到目标", f"{site.name} 没有可解析文档"))
       
  108:     authority_documents, evaluation, _strict_values, _strict_lines = selected
  108:     result, detail, values, reason = evaluation
  108:     if result is None:
   83:         return ParseResult(False, failure=FailureInfo(reason or "未找到目标", detail))
   76:     if not values or any(not is_valid_sum_value(value) for value in values):
>>>>>>         return ParseResult(False, failure=FailureInfo("字段校验未通过", f"{site.name} 返回了无效合数"))
       
   25:     evidence = build_document_evidence(site, period, values, detail, authority_documents)
   25:     if not evidence:
>>>>>>         return ParseResult(
>>>>>>             False,
>>>>>>             failure=FailureInfo(
>>>>>>                 "证据校验未通过",
>>>>>>                 f"{site.name} 解析成功但无法在同一原始候选行验证期数、关键词和合数",
                   ),
               )
   25:     return ParseResult(True, value=result, evidence=evidence)
       
       
    1: def evaluate_site_period(site: Site, period: int, documents: list[str]) -> tuple[str | None, str, list[str], str | None]:
>>>>>>     parsed = parse_site_period(site, period, documents)
>>>>>>     if not parsed.success:
>>>>>>         assert parsed.failure is not None
>>>>>>         return None, parsed.failure.reason, [], parsed.failure.category
>>>>>>     first = parsed.evidence[0]
>>>>>>     return parsed.value, first.raw_line, list(first.values), None

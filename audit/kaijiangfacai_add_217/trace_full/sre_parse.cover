       #
       # Secret Labs' Regular Expression Engine
       #
       # convert re-style regular expression to sre pattern
       #
       # Copyright (c) 1998-2001 by Secret Labs AB.  All rights reserved.
       #
       # See the sre.py file for information on usage and redistribution.
       #
       
>>>>>> """Internal support module for sre"""
       
       # XXX: show string offset and offending character for all errors
       
>>>>>> from sre_constants import *
       
>>>>>> SPECIAL_CHARS = ".\\[{()*+?^$|"
>>>>>> REPEAT_CHARS = "*+?{"
       
>>>>>> DIGITS = frozenset("0123456789")
       
>>>>>> OCTDIGITS = frozenset("01234567")
>>>>>> HEXDIGITS = frozenset("0123456789abcdefABCDEF")
>>>>>> ASCIILETTERS = frozenset("abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ")
       
>>>>>> WHITESPACE = frozenset(" \t\n\r\v\f")
       
>>>>>> _REPEATCODES = frozenset({MIN_REPEAT, MAX_REPEAT})
>>>>>> _UNITCODES = frozenset({ANY, RANGE, IN, LITERAL, NOT_LITERAL, CATEGORY})
       
>>>>>> ESCAPES = {
>>>>>>     r"\a": (LITERAL, ord("\a")),
>>>>>>     r"\b": (LITERAL, ord("\b")),
>>>>>>     r"\f": (LITERAL, ord("\f")),
>>>>>>     r"\n": (LITERAL, ord("\n")),
>>>>>>     r"\r": (LITERAL, ord("\r")),
>>>>>>     r"\t": (LITERAL, ord("\t")),
>>>>>>     r"\v": (LITERAL, ord("\v")),
>>>>>>     r"\\": (LITERAL, ord("\\"))
       }
       
>>>>>> CATEGORIES = {
>>>>>>     r"\A": (AT, AT_BEGINNING_STRING), # start of string
>>>>>>     r"\b": (AT, AT_BOUNDARY),
>>>>>>     r"\B": (AT, AT_NON_BOUNDARY),
>>>>>>     r"\d": (IN, [(CATEGORY, CATEGORY_DIGIT)]),
>>>>>>     r"\D": (IN, [(CATEGORY, CATEGORY_NOT_DIGIT)]),
>>>>>>     r"\s": (IN, [(CATEGORY, CATEGORY_SPACE)]),
>>>>>>     r"\S": (IN, [(CATEGORY, CATEGORY_NOT_SPACE)]),
>>>>>>     r"\w": (IN, [(CATEGORY, CATEGORY_WORD)]),
>>>>>>     r"\W": (IN, [(CATEGORY, CATEGORY_NOT_WORD)]),
>>>>>>     r"\Z": (AT, AT_END_STRING), # end of string
       }
       
>>>>>> FLAGS = {
           # standard flags
>>>>>>     "i": SRE_FLAG_IGNORECASE,
>>>>>>     "L": SRE_FLAG_LOCALE,
>>>>>>     "m": SRE_FLAG_MULTILINE,
>>>>>>     "s": SRE_FLAG_DOTALL,
>>>>>>     "x": SRE_FLAG_VERBOSE,
           # extensions
>>>>>>     "a": SRE_FLAG_ASCII,
>>>>>>     "t": SRE_FLAG_TEMPLATE,
>>>>>>     "u": SRE_FLAG_UNICODE,
       }
       
>>>>>> TYPE_FLAGS = SRE_FLAG_ASCII | SRE_FLAG_LOCALE | SRE_FLAG_UNICODE
>>>>>> GLOBAL_FLAGS = SRE_FLAG_DEBUG | SRE_FLAG_TEMPLATE
       
>>>>>> class Verbose(Exception):
>>>>>>     pass
       
>>>>>> class State:
           # keeps track of state for parsing
>>>>>>     def __init__(self):
  316:         self.flags = 0
  316:         self.groupdict = {}
  316:         self.groupwidths = [None]  # group 0
  316:         self.lookbehindgroups = None
  316:         self.grouprefpos = {}
>>>>>>     @property
>>>>>>     def groups(self):
 1117:         return len(self.groupwidths)
>>>>>>     def opengroup(self, name=None):
  216:         gid = self.groups
  216:         self.groupwidths.append(None)
  216:         if self.groups > MAXGROUPS:
>>>>>>             raise error("too many groups")
  216:         if name is not None:
   61:             ogid = self.groupdict.get(name, None)
   61:             if ogid is not None:
>>>>>>                 raise error("redefinition of group name %r as group %d; "
>>>>>>                             "was group %d" % (name, gid,  ogid))
   61:             self.groupdict[name] = gid
  216:         return gid
>>>>>>     def closegroup(self, gid, p):
  216:         self.groupwidths[gid] = p.getwidth()
>>>>>>     def checkgroup(self, gid):
    1:         return gid < self.groups and self.groupwidths[gid] is not None
       
>>>>>>     def checklookbehindgroup(self, gid, source):
    1:         if self.lookbehindgroups is not None:
>>>>>>             if not self.checkgroup(gid):
>>>>>>                 raise source.error('cannot refer to an open group')
>>>>>>             if gid >= self.lookbehindgroups:
>>>>>>                 raise source.error('cannot refer to group defined in the same '
                                          'lookbehind subpattern')
       
>>>>>> class SubPattern:
           # a subpattern, in intermediate form
>>>>>>     def __init__(self, state, data=None):
 7294:         self.state = state
 7294:         if data is None:
 5675:             data = []
 7294:         self.data = data
 7294:         self.width = None
       
>>>>>>     def dump(self, level=0):
>>>>>>         nl = True
>>>>>>         seqtypes = (tuple, list)
>>>>>>         for op, av in self.data:
>>>>>>             print(level*"  " + str(op), end='')
>>>>>>             if op is IN:
                       # member sublanguage
>>>>>>                 print()
>>>>>>                 for op, a in av:
>>>>>>                     print((level+1)*"  " + str(op), a)
>>>>>>             elif op is BRANCH:
>>>>>>                 print()
>>>>>>                 for i, a in enumerate(av[1]):
>>>>>>                     if i:
>>>>>>                         print(level*"  " + "OR")
>>>>>>                     a.dump(level+1)
>>>>>>             elif op is GROUPREF_EXISTS:
>>>>>>                 condgroup, item_yes, item_no = av
>>>>>>                 print('', condgroup)
>>>>>>                 item_yes.dump(level+1)
>>>>>>                 if item_no:
>>>>>>                     print(level*"  " + "ELSE")
>>>>>>                     item_no.dump(level+1)
>>>>>>             elif isinstance(av, seqtypes):
>>>>>>                 nl = False
>>>>>>                 for a in av:
>>>>>>                     if isinstance(a, SubPattern):
>>>>>>                         if not nl:
>>>>>>                             print()
>>>>>>                         a.dump(level+1)
>>>>>>                         nl = True
                           else:
>>>>>>                         if not nl:
>>>>>>                             print(' ', end='')
>>>>>>                         print(a, end='')
>>>>>>                         nl = False
>>>>>>                 if not nl:
>>>>>>                     print()
                   else:
>>>>>>                 print('', av)
>>>>>>     def __repr__(self):
>>>>>>         return repr(self.data)
>>>>>>     def __len__(self):
15158:         return len(self.data)
>>>>>>     def __delitem__(self, index):
   28:         del self.data[index]
>>>>>>     def __getitem__(self, index):
37458:         if isinstance(index, slice):
 1619:             return SubPattern(self.state, self.data[index])
35839:         return self.data[index]
>>>>>>     def __setitem__(self, index, code):
 2021:         self.data[index] = code
>>>>>>     def insert(self, index, code):
>>>>>>         self.data.insert(index, code)
>>>>>>     def append(self, code):
 9714:         self.data.append(code)
>>>>>>     def getwidth(self):
               # determine the width (min, max) for this subpattern
 6495:         if self.width is not None:
  216:             return self.width
 6279:         lo = hi = 0
16470:         for op, av in self.data:
10191:             if op is BRANCH:
  473:                 i = MAXREPEAT - 1
  473:                 j = 0
 4539:                 for av in av[1]:
 4066:                     l, h = av.getwidth()
 4066:                     i = min(i, l)
 4066:                     j = max(j, h)
  473:                 lo = lo + i
  473:                 hi = hi + j
 9718:             elif op is CALL:
>>>>>>                 i, j = av.getwidth()
>>>>>>                 lo = lo + i
>>>>>>                 hi = hi + j
 9718:             elif op is SUBPATTERN:
  230:                 i, j = av[-1].getwidth()
  230:                 lo = lo + i
  230:                 hi = hi + j
 9488:             elif op in _REPEATCODES:
 1615:                 i, j = av[2].getwidth()
 1615:                 lo = lo + i * av[0]
 1615:                 hi = hi + j * av[1]
 7873:             elif op in _UNITCODES:
 7339:                 lo = lo + 1
 7339:                 hi = hi + 1
  534:             elif op is GROUPREF:
    1:                 i, j = self.state.groupwidths[av]
    1:                 lo = lo + i
    1:                 hi = hi + j
  533:             elif op is GROUPREF_EXISTS:
>>>>>>                 i, j = av[1].getwidth()
>>>>>>                 if av[2] is not None:
>>>>>>                     l, h = av[2].getwidth()
>>>>>>                     i = min(i, l)
>>>>>>                     j = max(j, h)
                       else:
>>>>>>                     i = 0
>>>>>>                 lo = lo + i
>>>>>>                 hi = hi + j
  533:             elif op is SUCCESS:
>>>>>>                 break
 6279:         self.width = min(lo, MAXREPEAT - 1), min(hi, MAXREPEAT)
 6279:         return self.width
       
>>>>>> class Tokenizer:
>>>>>>     def __init__(self, string):
  315:         self.istext = isinstance(string, str)
  315:         self.string = string
  315:         if not self.istext:
   14:             string = str(string, 'latin1')
  315:         self.decoded_string = string
  315:         self.index = 0
  315:         self.next = None
  315:         self.__next()
>>>>>>     def __next(self):
29594:         index = self.index
29594:         try:
29594:             char = self.decoded_string[index]
  315:         except IndexError:
  315:             self.next = None
  315:             return
29279:         if char == "\\":
 2534:             index += 1
 2534:             try:
 2534:                 char += self.decoded_string[index]
>>>>>>             except IndexError:
>>>>>>                 raise error("bad escape (end of pattern)",
>>>>>>                             self.string, len(self.string) - 1) from None
29279:         self.index = index + 1
29279:         self.next = char
>>>>>>     def match(self, char):
14458:         if char == self.next:
 8056:             self.__next()
 8056:             return True
 6402:         return False
>>>>>>     def get(self):
20042:         this = self.next
20042:         self.__next()
20042:         return this
>>>>>>     def getwhile(self, n, charset):
  411:         result = ''
 1237:         for _ in range(n):
  826:             c = self.next
  826:             if c not in charset:
>>>>>>                 break
  826:             result += c
  826:             self.__next()
  411:         return result
>>>>>>     def getuntil(self, terminator, name):
   61:         result = ''
  353:         while True:
  353:             c = self.next
  353:             self.__next()
  353:             if c is None:
>>>>>>                 if not result:
>>>>>>                     raise self.error("missing " + name)
>>>>>>                 raise self.error("missing %s, unterminated name" % terminator,
>>>>>>                                  len(result))
  353:             if c == terminator:
   61:                 if not result:
>>>>>>                     raise self.error("missing " + name, 1)
   61:                 break
  292:             result += c
   61:         return result
>>>>>>     @property
>>>>>>     def pos(self):
>>>>>>         return self.index - len(self.next or '')
>>>>>>     def tell(self):
 5609:         return self.index - len(self.next or '')
>>>>>>     def seek(self, index):
    2:         self.index = index
    2:         self.__next()
       
>>>>>>     def error(self, msg, offset=0):
>>>>>>         return error(msg, self.string, self.tell() - offset)
       
>>>>>> def _class_escape(source, escape):
           # handle escape code inside character class
 1168:     code = ESCAPES.get(escape)
 1168:     if code:
  539:         return code
  629:     code = CATEGORIES.get(escape)
  629:     if code and code[0] is IN:
   31:         return code
  598:     try:
  598:         c = escape[1:2]
  598:         if c == "x":
                   # hexadecimal escape (exactly two digits)
  405:             escape += source.getwhile(2, HEXDIGITS)
  405:             if len(escape) != 4:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
  405:             return LITERAL, int(escape[2:], 16)
  193:         elif c == "u" and source.istext:
                   # unicode escape (exactly four digits)
    2:             escape += source.getwhile(4, HEXDIGITS)
    2:             if len(escape) != 6:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
    2:             return LITERAL, int(escape[2:], 16)
  191:         elif c == "U" and source.istext:
                   # unicode escape (exactly eight digits)
>>>>>>             escape += source.getwhile(8, HEXDIGITS)
>>>>>>             if len(escape) != 10:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
>>>>>>             c = int(escape[2:], 16)
>>>>>>             chr(c) # raise ValueError for invalid code
>>>>>>             return LITERAL, c
  191:         elif c == "N" and source.istext:
>>>>>>             import unicodedata
                   # named unicode escape e.g. \N{EM DASH}
>>>>>>             if not source.match('{'):
>>>>>>                 raise source.error("missing {")
>>>>>>             charname = source.getuntil('}', 'character name')
>>>>>>             try:
>>>>>>                 c = ord(unicodedata.lookup(charname))
>>>>>>             except (KeyError, TypeError):
>>>>>>                 raise source.error("undefined character name %r" % charname,
>>>>>>                                    len(charname) + len(r'\N{}'))
>>>>>>             return LITERAL, c
  191:         elif c in OCTDIGITS:
                   # octal escape (up to three digits)
    4:             escape += source.getwhile(2, OCTDIGITS)
    4:             c = int(escape[1:], 8)
    4:             if c > 0o377:
>>>>>>                 raise source.error('octal escape value %s outside of '
>>>>>>                                    'range 0-0o377' % escape, len(escape))
    4:             return LITERAL, c
  187:         elif c in DIGITS:
>>>>>>             raise ValueError
  187:         if len(escape) == 2:
  187:             if c in ASCIILETTERS:
>>>>>>                 raise source.error('bad escape %s' % escape, len(escape))
  187:             return LITERAL, ord(escape[1])
>>>>>>     except ValueError:
>>>>>>         pass
>>>>>>     raise source.error("bad escape %s" % escape, len(escape))
       
>>>>>> def _escape(source, escape, state):
           # handle escape code in expression
 1366:     code = CATEGORIES.get(escape)
 1366:     if code:
  656:         return code
  710:     code = ESCAPES.get(escape)
  710:     if code:
  431:         return code
  279:     try:
  279:         c = escape[1:2]
  279:         if c == "x":
                   # hexadecimal escape
>>>>>>             escape += source.getwhile(2, HEXDIGITS)
>>>>>>             if len(escape) != 4:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
>>>>>>             return LITERAL, int(escape[2:], 16)
  279:         elif c == "u" and source.istext:
                   # unicode escape (exactly four digits)
>>>>>>             escape += source.getwhile(4, HEXDIGITS)
>>>>>>             if len(escape) != 6:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
>>>>>>             return LITERAL, int(escape[2:], 16)
  279:         elif c == "U" and source.istext:
                   # unicode escape (exactly eight digits)
>>>>>>             escape += source.getwhile(8, HEXDIGITS)
>>>>>>             if len(escape) != 10:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
>>>>>>             c = int(escape[2:], 16)
>>>>>>             chr(c) # raise ValueError for invalid code
>>>>>>             return LITERAL, c
  279:         elif c == "N" and source.istext:
>>>>>>             import unicodedata
                   # named unicode escape e.g. \N{EM DASH}
>>>>>>             if not source.match('{'):
>>>>>>                 raise source.error("missing {")
>>>>>>             charname = source.getuntil('}', 'character name')
>>>>>>             try:
>>>>>>                 c = ord(unicodedata.lookup(charname))
>>>>>>             except (KeyError, TypeError):
>>>>>>                 raise source.error("undefined character name %r" % charname,
>>>>>>                                    len(charname) + len(r'\N{}'))
>>>>>>             return LITERAL, c
  279:         elif c == "0":
                   # octal escape
>>>>>>             escape += source.getwhile(2, OCTDIGITS)
>>>>>>             return LITERAL, int(escape[1:], 8)
  279:         elif c in DIGITS:
                   # octal escape *or* decimal group reference (sigh)
    1:             if source.next in DIGITS:
>>>>>>                 escape += source.get()
>>>>>>                 if (escape[1] in OCTDIGITS and escape[2] in OCTDIGITS and
>>>>>>                     source.next in OCTDIGITS):
                           # got three octal digits; this is an octal escape
>>>>>>                     escape += source.get()
>>>>>>                     c = int(escape[1:], 8)
>>>>>>                     if c > 0o377:
>>>>>>                         raise source.error('octal escape value %s outside of '
>>>>>>                                            'range 0-0o377' % escape,
>>>>>>                                            len(escape))
>>>>>>                     return LITERAL, c
                   # not an octal escape, so this is a group reference
    1:             group = int(escape[1:])
    1:             if group < state.groups:
    1:                 if not state.checkgroup(group):
>>>>>>                     raise source.error("cannot refer to an open group",
>>>>>>                                        len(escape))
    1:                 state.checklookbehindgroup(group, source)
    1:                 return GROUPREF, group
>>>>>>             raise source.error("invalid group reference %d" % group, len(escape) - 1)
  278:         if len(escape) == 2:
  278:             if c in ASCIILETTERS:
>>>>>>                 raise source.error("bad escape %s" % escape, len(escape))
  278:             return LITERAL, ord(escape[1])
>>>>>>     except ValueError:
>>>>>>         pass
>>>>>>     raise source.error("bad escape %s" % escape, len(escape))
       
>>>>>> def _uniq(items):
 1195:     return list(dict.fromkeys(items))
       
>>>>>> def _parse_sub(source, state, verbose, nested):
           # parse an alternation: a|b|c
       
 1563:     items = []
 1563:     itemsappend = items.append
 1563:     sourcematch = source.match
 1563:     start = source.tell()
 5180:     while True:
10360:         itemsappend(_parse(source, state, verbose, nested + 1,
 5180:                            not nested and not items))
 5179:         if not sourcematch("|"):
 1562:             break
       
 1562:     if len(items) == 1:
 1067:         return items[0]
       
  495:     subpattern = SubPattern(state)
       
           # check if all items share a common prefix
  509:     while True:
  509:         prefix = None
 1039:         for item in items:
 1025:             if not item:
    6:                 break
 1019:             if prefix is None:
  508:                 prefix = item[0]
  511:             elif item[0] != prefix:
  489:                 break
               else:
                   # all subitems start with a common "prefix".
                   # move it out of the branch
   42:             for item in items:
   28:                 del item[0]
   14:             subpattern.append(prefix)
   14:             continue # check next one
  495:         break
       
           # check if the branch can be replaced by a character set
  495:     set = []
  644:     for item in items:
  623:         if len(item) != 1:
  285:             break
  338:         op, av = item[0]
  338:         if op is LITERAL:
   56:             set.append((op, av))
  282:         elif op is IN and av[0][0] is not NEGATE:
   93:             set.extend(av)
               else:
  189:             break
           else:
               # we can store this as a character set instead of a
               # branch (the compiler may optimize this even more)
   21:         subpattern.append((IN, _uniq(set)))
   21:         return subpattern
       
  474:     subpattern.append((BRANCH, (None, items)))
  474:     return subpattern
       
>>>>>> def _parse(source, state, verbose, nested, first=False):
           # parse a simple pattern
 5180:     subpattern = SubPattern(state)
       
           # precompute constants into local variables
 5180:     subpatternappend = subpattern.append
 5180:     sourceget = source.get
 5180:     sourcematch = source.match
 5180:     _len = len
 5180:     _ord = ord
       
17195:     while True:
       
17195:         this = source.next
17195:         if this is None:
  315:             break # end of pattern
16880:         if this in "|)":
 4864:             break # end of subpattern
12016:         sourceget()
       
12016:         if verbose:
                   # skip whitespace and comments
 3474:             if this in WHITESPACE:
 1145:                 continue
 2329:             if this == "#":
  888:                 while True:
  888:                     this = sourceget()
  888:                     if this is None or this == "\n":
   42:                         break
   42:                 continue
       
10829:         if this[0] == "\\":
 1366:             code = _escape(source, this, state)
 1366:             subpatternappend(code)
       
 9463:         elif this not in SPECIAL_CHARS:
 5155:             subpatternappend((LITERAL, _ord(this)))
       
 4308:         elif this == "[":
 1174:             here = source.tell() - 1
                   # character set
 1174:             set = []
 1174:             setappend = set.append
       ##          if sourcematch(":"):
       ##              pass # handle character classes
 1174:             if source.next == '[':
>>>>>>                 import warnings
>>>>>>                 warnings.warn(
>>>>>>                     'Possible nested set at position %d' % source.tell(),
>>>>>>                     FutureWarning, stacklevel=nested + 6
                       )
 1174:             negate = sourcematch("^")
                   # check remaining characters
 4276:             while True:
 4276:                 this = sourceget()
 4276:                 if this is None:
>>>>>>                     raise source.error("unterminated character set",
>>>>>>                                        source.tell() - here)
 4276:                 if this == "]" and set:
 1168:                     break
 3108:                 elif this[0] == "\\":
 1005:                     code1 = _class_escape(source, this)
                       else:
 2103:                     if set and this in '-&~|' and source.next == this:
>>>>>>                         import warnings
>>>>>>                         warnings.warn(
>>>>>>                             'Possible set %s at position %d' % (
>>>>>>                                 'difference' if this == '-' else
>>>>>>                                 'intersection' if this == '&' else
>>>>>>                                 'symmetric difference' if this == '~' else
>>>>>>                                 'union',
>>>>>>                                 source.tell() - 1),
>>>>>>                             FutureWarning, stacklevel=nested + 6
                               )
 2103:                     code1 = LITERAL, _ord(this)
 3108:                 if sourcematch("-"):
                           # potential range
  896:                     that = sourceget()
  896:                     if that is None:
>>>>>>                         raise source.error("unterminated character set",
>>>>>>                                            source.tell() - here)
  896:                     if that == "]":
    6:                         if code1[0] is IN:
    1:                             code1 = code1[1][0]
    6:                         setappend(code1)
    6:                         setappend((LITERAL, _ord("-")))
    6:                         break
  890:                     if that[0] == "\\":
  163:                         code2 = _class_escape(source, that)
                           else:
  727:                         if that == '-':
>>>>>>                             import warnings
>>>>>>                             warnings.warn(
>>>>>>                                 'Possible set difference at position %d' % (
>>>>>>                                     source.tell() - 2),
>>>>>>                                 FutureWarning, stacklevel=nested + 6
                                   )
  727:                         code2 = LITERAL, _ord(that)
  890:                     if code1[0] != LITERAL or code2[0] != LITERAL:
>>>>>>                         msg = "bad character range %s-%s" % (this, that)
>>>>>>                         raise source.error(msg, len(this) + 1 + len(that))
  890:                     lo = code1[1]
  890:                     hi = code2[1]
  890:                     if hi < lo:
>>>>>>                         msg = "bad character range %s-%s" % (this, that)
>>>>>>                         raise source.error(msg, len(this) + 1 + len(that))
  890:                     setappend((RANGE, (lo, hi)))
                       else:
 2212:                     if code1[0] is IN:
   30:                         code1 = code1[1][0]
 2212:                     setappend(code1)
       
 1174:             set = _uniq(set)
                   # XXX: <fl> should move set optimization to compiler!
 1174:             if _len(set) == 1 and set[0][0] is LITERAL:
                       # optimization
  272:                 if negate:
  153:                     subpatternappend((NOT_LITERAL, set[0][1]))
                       else:
  119:                     subpatternappend(set[0])
                   else:
  902:                 if negate:
  173:                     set.insert(0, (NEGATE, None))
                       # charmap optimization can't be added here because
                       # global flags still are not known
  902:                 subpatternappend((IN, set))
       
 3134:         elif this in REPEAT_CHARS:
                   # repeat previous item
 1620:             here = source.tell()
 1620:             if this == "?":
  290:                 min, max = 0, 1
 1330:             elif this == "*":
  706:                 min, max = 0, MAXREPEAT
       
  624:             elif this == "+":
  215:                 min, max = 1, MAXREPEAT
  409:             elif this == "{":
  409:                 if source.next == "}":
>>>>>>                     subpatternappend((LITERAL, _ord(this)))
>>>>>>                     continue
       
  409:                 min, max = 0, MAXREPEAT
  409:                 lo = hi = ""
  817:                 while source.next in DIGITS:
  408:                     lo += sourceget()
  409:                 if sourcematch(","):
  728:                     while source.next in DIGITS:
  389:                         hi += sourceget()
                       else:
   70:                     hi = lo
  409:                 if not sourcematch("}"):
    1:                     subpatternappend((LITERAL, _ord(this)))
    1:                     source.seek(here)
    1:                     continue
       
  408:                 if lo:
  408:                     min = int(lo)
  408:                     if min >= MAXREPEAT:
>>>>>>                         raise OverflowError("the repetition number is too large")
  408:                 if hi:
  403:                     max = int(hi)
  403:                     if max >= MAXREPEAT:
>>>>>>                         raise OverflowError("the repetition number is too large")
  403:                     if max < min:
>>>>>>                         raise source.error("min repeat greater than max repeat",
>>>>>>                                            source.tell() - here)
                   else:
>>>>>>                 raise AssertionError("unsupported quantifier %r" % (char,))
                   # figure out which item to repeat
 1619:             if subpattern:
 1619:                 item = subpattern[-1:]
                   else:
>>>>>>                 item = None
 1619:             if not item or item[0][0] is AT:
>>>>>>                 raise source.error("nothing to repeat",
>>>>>>                                    source.tell() - here + len(this))
 1619:             if item[0][0] in _REPEATCODES:
>>>>>>                 raise source.error("multiple repeat",
>>>>>>                                    source.tell() - here + len(this))
 1619:             if item[0][0] is SUBPATTERN:
  337:                 group, add_flags, del_flags, p = item[0][1]
  337:                 if group is None and not add_flags and not del_flags:
  325:                     item = p
 1619:             if sourcematch("?"):
   65:                 subpattern[-1] = (MIN_REPEAT, (min, max, item))
                   else:
 1554:                 subpattern[-1] = (MAX_REPEAT, (min, max, item))
       
 1514:         elif this == ".":
   40:             subpatternappend((ANY, None))
       
 1474:         elif this == "(":
 1252:             start = source.tell() - 1
 1252:             group = True
 1252:             name = None
 1252:             add_flags = 0
 1252:             del_flags = 0
 1252:             if sourcematch("?"):
                       # options
 1097:                 char = sourceget()
 1097:                 if char is None:
>>>>>>                     raise source.error("unexpected end of pattern")
 1097:                 if char == "P":
                           # python extensions
   61:                     if sourcematch("<"):
                               # named group: skip forward to end of name
   61:                         name = source.getuntil(">", "group name")
   61:                         if not name.isidentifier():
>>>>>>                             msg = "bad character in group name %r" % name
>>>>>>                             raise source.error(msg, len(name) + 1)
>>>>>>                     elif sourcematch("="):
                               # named backreference
>>>>>>                         name = source.getuntil(")", "group name")
>>>>>>                         if not name.isidentifier():
>>>>>>                             msg = "bad character in group name %r" % name
>>>>>>                             raise source.error(msg, len(name) + 1)
>>>>>>                         gid = state.groupdict.get(name)
>>>>>>                         if gid is None:
>>>>>>                             msg = "unknown group name %r" % name
>>>>>>                             raise source.error(msg, len(name) + 1)
>>>>>>                         if not state.checkgroup(gid):
>>>>>>                             raise source.error("cannot refer to an open group",
>>>>>>                                                len(name) + 1)
>>>>>>                         state.checklookbehindgroup(gid, source)
>>>>>>                         subpatternappend((GROUPREF, gid))
>>>>>>                         continue
       
                           else:
>>>>>>                         char = sourceget()
>>>>>>                         if char is None:
>>>>>>                             raise source.error("unexpected end of pattern")
>>>>>>                         raise source.error("unknown extension ?P" + char,
>>>>>>                                            len(char) + 2)
 1036:                 elif char == ":":
                           # non-capturing group
  727:                     group = None
  309:                 elif char == "#":
                           # comment
>>>>>>                     while True:
>>>>>>                         if source.next is None:
>>>>>>                             raise source.error("missing ), unterminated comment",
>>>>>>                                                source.tell() - start)
>>>>>>                         if sourceget() == ")":
>>>>>>                             break
>>>>>>                     continue
       
  309:                 elif char in "=!<":
                           # lookahead assertions
  290:                     dir = 1
  290:                     if char == "<":
   53:                         char = sourceget()
   53:                         if char is None:
>>>>>>                             raise source.error("unexpected end of pattern")
   53:                         if char not in "=!":
>>>>>>                             raise source.error("unknown extension ?<" + char,
>>>>>>                                                len(char) + 2)
   53:                         dir = -1 # lookbehind
   53:                         lookbehindgroups = state.lookbehindgroups
   53:                         if lookbehindgroups is None:
   53:                             state.lookbehindgroups = state.groups
  290:                     p = _parse_sub(source, state, verbose, nested + 1)
  290:                     if dir < 0:
   53:                         if lookbehindgroups is None:
   53:                             state.lookbehindgroups = None
  290:                     if not sourcematch(")"):
>>>>>>                         raise source.error("missing ), unterminated subpattern",
>>>>>>                                            source.tell() - start)
  290:                     if char == "=":
    7:                         subpatternappend((ASSERT, (dir, p)))
                           else:
  283:                         subpatternappend((ASSERT_NOT, (dir, p)))
  290:                     continue
       
   19:                 elif char == "(":
                           # conditional backreference group
>>>>>>                     condname = source.getuntil(")", "group name")
>>>>>>                     if condname.isidentifier():
>>>>>>                         condgroup = state.groupdict.get(condname)
>>>>>>                         if condgroup is None:
>>>>>>                             msg = "unknown group name %r" % condname
>>>>>>                             raise source.error(msg, len(condname) + 1)
                           else:
>>>>>>                         try:
>>>>>>                             condgroup = int(condname)
>>>>>>                             if condgroup < 0:
>>>>>>                                 raise ValueError
>>>>>>                         except ValueError:
>>>>>>                             msg = "bad character in group name %r" % condname
>>>>>>                             raise source.error(msg, len(condname) + 1) from None
>>>>>>                         if not condgroup:
>>>>>>                             raise source.error("bad group number",
>>>>>>                                                len(condname) + 1)
>>>>>>                         if condgroup >= MAXGROUPS:
>>>>>>                             msg = "invalid group reference %d" % condgroup
>>>>>>                             raise source.error(msg, len(condname) + 1)
>>>>>>                         if condgroup not in state.grouprefpos:
>>>>>>                             state.grouprefpos[condgroup] = (
>>>>>>                                 source.tell() - len(condname) - 1
                                   )
>>>>>>                     state.checklookbehindgroup(condgroup, source)
>>>>>>                     item_yes = _parse(source, state, verbose, nested + 1)
>>>>>>                     if source.match("|"):
>>>>>>                         item_no = _parse(source, state, verbose, nested + 1)
>>>>>>                         if source.next == "|":
>>>>>>                             raise source.error("conditional backref with more than two branches")
                           else:
>>>>>>                         item_no = None
>>>>>>                     if not source.match(")"):
>>>>>>                         raise source.error("missing ), unterminated subpattern",
>>>>>>                                            source.tell() - start)
>>>>>>                     subpatternappend((GROUPREF_EXISTS, (condgroup, item_yes, item_no)))
>>>>>>                     continue
       
   19:                 elif char in FLAGS or char == "-":
                           # flags
   19:                     flags = _parse_flags(source, state, char)
   19:                     if flags is None:  # global flags
    5:                         if not first or subpattern:
>>>>>>                             import warnings
>>>>>>                             warnings.warn(
>>>>>>                                 'Flags not at the start of the expression %r%s'
>>>>>>                                 ' but at position %d' % (
>>>>>>                                     source.string[:20],  # truncate long regexes
>>>>>>                                     ' (truncated)' if len(source.string) > 20 else '',
>>>>>>                                     start,
                                       ),
>>>>>>                                 DeprecationWarning, stacklevel=nested + 6
                                   )
    5:                         if (state.flags & SRE_FLAG_VERBOSE) and not verbose:
    1:                             raise Verbose
    4:                         continue
       
   14:                     add_flags, del_flags = flags
   14:                     group = None
                       else:
>>>>>>                     raise source.error("unknown extension ?" + char,
>>>>>>                                        len(char) + 1)
       
                   # parse group contents
  957:             if group is not None:
  216:                 try:
  216:                     group = state.opengroup(name)
>>>>>>                 except error as err:
>>>>>>                     raise source.error(err.msg, len(name) + 1) from None
 1479:             sub_verbose = ((verbose or (add_flags & SRE_FLAG_VERBOSE)) and
  522:                            not (del_flags & SRE_FLAG_VERBOSE))
  957:             p = _parse_sub(source, state, sub_verbose, nested + 1)
  957:             if not source.match(")"):
>>>>>>                 raise source.error("missing ), unterminated subpattern",
>>>>>>                                    source.tell() - start)
  957:             if group is not None:
  216:                 state.closegroup(group, p)
  957:             subpatternappend((SUBPATTERN, (group, add_flags, del_flags, p)))
       
  222:         elif this == "^":
   96:             subpatternappend((AT, AT_BEGINNING))
       
  126:         elif this == "$":
  126:             subpatternappend((AT, AT_END))
       
               else:
>>>>>>             raise AssertionError("unsupported special character %r" % (char,))
       
           # unpack non-capturing groups
14384:     for i in range(len(subpattern))[::-1]:
 9205:         op, av = subpattern[i]
 9205:         if op is SUBPATTERN:
  620:             group, add_flags, del_flags, p = av
  620:             if group is None and not add_flags and not del_flags:
  402:                 subpattern[i: i+1] = p
       
 5179:     return subpattern
       
>>>>>> def _parse_flags(source, state, char):
   19:     sourceget = source.get
   19:     add_flags = 0
   19:     del_flags = 0
   19:     if char != "-":
   19:         while True:
   19:             flag = FLAGS[char]
   19:             if source.istext:
   19:                 if char == 'L':
>>>>>>                     msg = "bad inline flags: cannot use 'L' flag with a str pattern"
>>>>>>                     raise source.error(msg)
                   else:
>>>>>>                 if char == 'u':
>>>>>>                     msg = "bad inline flags: cannot use 'u' flag with a bytes pattern"
>>>>>>                     raise source.error(msg)
   19:             add_flags |= flag
   19:             if (flag & TYPE_FLAGS) and (add_flags & TYPE_FLAGS) != flag:
>>>>>>                 msg = "bad inline flags: flags 'a', 'u' and 'L' are incompatible"
>>>>>>                 raise source.error(msg)
   19:             char = sourceget()
   19:             if char is None:
>>>>>>                 raise source.error("missing -, : or )")
   19:             if char in ")-:":
   19:                 break
>>>>>>             if char not in FLAGS:
>>>>>>                 msg = "unknown flag" if char.isalpha() else "missing -, : or )"
>>>>>>                 raise source.error(msg, len(char))
   19:     if char == ")":
    5:         state.flags |= add_flags
    5:         return None
   14:     if add_flags & GLOBAL_FLAGS:
>>>>>>         raise source.error("bad inline flags: cannot turn on global flag", 1)
   14:     if char == "-":
>>>>>>         char = sourceget()
>>>>>>         if char is None:
>>>>>>             raise source.error("missing flag")
>>>>>>         if char not in FLAGS:
>>>>>>             msg = "unknown flag" if char.isalpha() else "missing flag"
>>>>>>             raise source.error(msg, len(char))
>>>>>>         while True:
>>>>>>             flag = FLAGS[char]
>>>>>>             if flag & TYPE_FLAGS:
>>>>>>                 msg = "bad inline flags: cannot turn off flags 'a', 'u' and 'L'"
>>>>>>                 raise source.error(msg)
>>>>>>             del_flags |= flag
>>>>>>             char = sourceget()
>>>>>>             if char is None:
>>>>>>                 raise source.error("missing :")
>>>>>>             if char == ":":
>>>>>>                 break
>>>>>>             if char not in FLAGS:
>>>>>>                 msg = "unknown flag" if char.isalpha() else "missing :"
>>>>>>                 raise source.error(msg, len(char))
   14:     assert char == ":"
   14:     if del_flags & GLOBAL_FLAGS:
>>>>>>         raise source.error("bad inline flags: cannot turn off global flag", 1)
   14:     if add_flags & del_flags:
>>>>>>         raise source.error("bad inline flags: flag turned on and off", 1)
   14:     return add_flags, del_flags
       
>>>>>> def fix_flags(src, flags):
           # Check and fix flags according to the type of pattern (str or bytes)
  315:     if isinstance(src, str):
  301:         if flags & SRE_FLAG_LOCALE:
>>>>>>             raise ValueError("cannot use LOCALE flag with a str pattern")
  301:         if not flags & SRE_FLAG_ASCII:
  292:             flags |= SRE_FLAG_UNICODE
    9:         elif flags & SRE_FLAG_UNICODE:
>>>>>>             raise ValueError("ASCII and UNICODE flags are incompatible")
           else:
   14:         if flags & SRE_FLAG_UNICODE:
>>>>>>             raise ValueError("cannot use UNICODE flag with a bytes pattern")
   14:         if flags & SRE_FLAG_LOCALE and flags & SRE_FLAG_ASCII:
>>>>>>             raise ValueError("ASCII and LOCALE flags are incompatible")
  315:     return flags
       
>>>>>> def parse(str, flags=0, state=None):
           # parse 're' pattern into list of (opcode, argument) tuples
       
  315:     source = Tokenizer(str)
       
  315:     if state is None:
  315:         state = State()
  315:     state.flags = flags
  315:     state.str = str
       
  315:     try:
  315:         p = _parse_sub(source, state, flags & SRE_FLAG_VERBOSE, 0)
    1:     except Verbose:
               # the VERBOSE flag was switched on inside the pattern.  to be
               # on the safe side, we'll parse the whole thing again...
    1:         state = State()
    1:         state.flags = flags | SRE_FLAG_VERBOSE
    1:         state.str = str
    1:         source.seek(0)
    1:         p = _parse_sub(source, state, True, 0)
       
  315:     p.state.flags = fix_flags(str, p.state.flags)
       
  315:     if source.next is not None:
>>>>>>         assert source.next == ")"
>>>>>>         raise source.error("unbalanced parenthesis")
       
  315:     for g in p.state.grouprefpos:
>>>>>>         if g >= p.state.groups:
>>>>>>             msg = "invalid group reference %d" % g
>>>>>>             raise error(msg, str, p.state.grouprefpos[g])
       
  315:     if flags & SRE_FLAG_DEBUG:
>>>>>>         p.dump()
       
  315:     return p
       
>>>>>> def parse_template(source, state):
           # parse 're' replacement string into list of literals and
           # group references
>>>>>>     s = Tokenizer(source)
>>>>>>     sget = s.get
>>>>>>     groups = []
>>>>>>     literals = []
>>>>>>     literal = []
>>>>>>     lappend = literal.append
>>>>>>     def addgroup(index, pos):
>>>>>>         if index > state.groups:
>>>>>>             raise s.error("invalid group reference %d" % index, pos)
>>>>>>         if literal:
>>>>>>             literals.append(''.join(literal))
>>>>>>             del literal[:]
>>>>>>         groups.append((len(literals), index))
>>>>>>         literals.append(None)
>>>>>>     groupindex = state.groupindex
>>>>>>     while True:
>>>>>>         this = sget()
>>>>>>         if this is None:
>>>>>>             break # end of replacement string
>>>>>>         if this[0] == "\\":
                   # group
>>>>>>             c = this[1]
>>>>>>             if c == "g":
>>>>>>                 name = ""
>>>>>>                 if not s.match("<"):
>>>>>>                     raise s.error("missing <")
>>>>>>                 name = s.getuntil(">", "group name")
>>>>>>                 if name.isidentifier():
>>>>>>                     try:
>>>>>>                         index = groupindex[name]
>>>>>>                     except KeyError:
>>>>>>                         raise IndexError("unknown group name %r" % name)
                       else:
>>>>>>                     try:
>>>>>>                         index = int(name)
>>>>>>                         if index < 0:
>>>>>>                             raise ValueError
>>>>>>                     except ValueError:
>>>>>>                         raise s.error("bad character in group name %r" % name,
>>>>>>                                       len(name) + 1) from None
>>>>>>                     if index >= MAXGROUPS:
>>>>>>                         raise s.error("invalid group reference %d" % index,
>>>>>>                                       len(name) + 1)
>>>>>>                 addgroup(index, len(name) + 1)
>>>>>>             elif c == "0":
>>>>>>                 if s.next in OCTDIGITS:
>>>>>>                     this += sget()
>>>>>>                     if s.next in OCTDIGITS:
>>>>>>                         this += sget()
>>>>>>                 lappend(chr(int(this[1:], 8) & 0xff))
>>>>>>             elif c in DIGITS:
>>>>>>                 isoctal = False
>>>>>>                 if s.next in DIGITS:
>>>>>>                     this += sget()
>>>>>>                     if (c in OCTDIGITS and this[2] in OCTDIGITS and
>>>>>>                         s.next in OCTDIGITS):
>>>>>>                         this += sget()
>>>>>>                         isoctal = True
>>>>>>                         c = int(this[1:], 8)
>>>>>>                         if c > 0o377:
>>>>>>                             raise s.error('octal escape value %s outside of '
>>>>>>                                           'range 0-0o377' % this, len(this))
>>>>>>                         lappend(chr(c))
>>>>>>                 if not isoctal:
>>>>>>                     addgroup(int(this[1:]), len(this) - 1)
                   else:
>>>>>>                 try:
>>>>>>                     this = chr(ESCAPES[this][1])
>>>>>>                 except KeyError:
>>>>>>                     if c in ASCIILETTERS:
>>>>>>                         raise s.error('bad escape %s' % this, len(this))
>>>>>>                 lappend(this)
               else:
>>>>>>             lappend(this)
>>>>>>     if literal:
>>>>>>         literals.append(''.join(literal))
>>>>>>     if not isinstance(source, str):
               # The tokenizer implicitly decodes bytes objects as latin-1, we must
               # therefore re-encode the final representation.
>>>>>>         literals = [None if s is None else s.encode('latin-1') for s in literals]
>>>>>>     return groups, literals
       
>>>>>> def expand_template(template, match):
>>>>>>     g = match.group
>>>>>>     empty = match.string[:0]
>>>>>>     groups, literals = template
>>>>>>     literals = literals[:]
>>>>>>     try:
>>>>>>         for index, group in groups:
>>>>>>             literals[index] = g(group) or empty
>>>>>>     except IndexError:
>>>>>>         raise error("invalid group reference %d" % index)
>>>>>>     return empty.join(literals)

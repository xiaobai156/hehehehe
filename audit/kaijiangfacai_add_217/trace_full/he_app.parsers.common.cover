    1: import re
       
    1: from bs4 import BeautifulSoup
       
    1: from he_app.domain.models import Candidate, FailureInfo, Site
    1: from he_app.domain.policies import normalize_digit_text, normalize_pick, normalize_text
    1: from he_app.parsers.policies import (
           BODY_LOCATOR_RE,
           PERIOD_RE,
           REVERSE_VALUE_RE,
           STRICT_KILL_SUM_RE,
           STRICT_SUCCESS_VALUE_RE,
           VALUE_RE,
           WEAK_KILL_SUM_RE,
       )
       
       
    1: def document_text_lines(document: str) -> list[str]:
  107:     text = BeautifulSoup(document, "html.parser").get_text("\n", strip=True) or document
  620:     return [line for line in text.splitlines() if normalize_text(line)]
       
       
    1: def candidate_chunks(document: str, period: int) -> list[str]:
  123:     chunks: list[str] = []
  123:     soup = BeautifulSoup(document, "html.parser")
       
  148:     for tag in soup.find_all(["p", "div", "td", "tr", "li", "font", "span", "b", "strong"]):
   25:         text = normalize_text(tag.get_text(" ", strip=True))
   25:         if text:
   25:             chunks.append(text)
       
  734:     lines = [normalize_text(line) for line in soup.get_text("\n", strip=True).splitlines()]
  734:     lines = [line for line in lines if line]
  123:     target_re = re.compile(rf"(?<!\d){re.escape(str(period))}\s*期")
  611:     for index, line in enumerate(lines):
  488:         chunks.append(line)
  488:         if target_re.search(line):
  132:             chunks.append(normalize_text(" ".join(lines[index : index + 6])))
  132:             window_start = max(0, index - 3)
  132:             window_end = min(len(lines), index + 1)
  132:             chunks.append(normalize_text(" ".join(lines[window_start:window_end])))
       
  123:     all_text = normalize_text(soup.get_text(" ", strip=True) or document)
  123:     chunks.extend(split_period_segments(all_text, period))
  123:     return chunks
       
       
    1: def split_period_segments(text: str, period: int) -> list[str]:
 1162:     text = normalize_text(text)
 1162:     matches = list(PERIOD_RE.finditer(text))
 1162:     segments: list[str] = []
       
 7390:     for index, match in enumerate(matches):
 6228:         if int(match.group(1)) != period:
 5481:             continue
  747:         start = match.start()
  747:         next_start = matches[index + 1].start() if index + 1 < len(matches) else len(text)
  747:         end = min(next_start, match.start() + 220)
  747:         segment = normalize_text(text[start:end])
  747:         if segment:
  747:             segments.append(segment)
       
 1162:     return segments
       
       
    1: def split_all_period_segments(text: str) -> list[str]:
 1105:     text = normalize_text(text)
 1105:     matches = list(PERIOD_RE.finditer(text))
 1105:     segments: list[str] = []
       
 2764:     for index, match in enumerate(matches):
 1659:         start = match.start()
 1659:         next_start = matches[index + 1].start() if index + 1 < len(matches) else len(text)
 1659:         end = min(next_start, match.start() + 220)
 1659:         segment = normalize_text(text[start:end])
 1659:         if segment:
 1659:             segments.append(segment)
       
 1105:     return segments
       
       
    1: def has_kill_sum_keyword(text: str, allow_weak: bool = False) -> bool:
 6124:     normalized = normalize_digit_text(normalize_text(text))
 6124:     return bool(STRICT_KILL_SUM_RE.search(normalized) or (allow_weak and WEAK_KILL_SUM_RE.search(normalized)))
       
       
    1: def extract_values(text: str, allow_weak: bool = False) -> list[str]:
 4169:     normalized = normalize_digit_text(normalize_text(text))
33352:     for marker in ("澳彩合数属性", "合数属性", "属性:", "属性：", "★★", "博彩必备", "站长宣言"):
29183:         marker_index = normalized.find(marker)
29183:         if marker_index >= 0:
   20:             normalized = normalized[:marker_index]
 4169:     normalized = re.split(r"\s0?1\s*[-－]\s*0?6\s*合|\s0?1\s*合\s*[:：]", normalized, maxsplit=1)[0]
       
 4169:     if not has_kill_sum_keyword(normalized, allow_weak):
    5:         return []
       
 4164:     if has_out_of_range_value(normalized):
>>>>>>         return []
       
 4164:     values: list[str] = []
 4164:     matches: list[tuple[re.Match[str], bool]] = []
12422:     matches.extend((match, False) for match in VALUE_RE.finditer(normalized))
 8383:     matches.extend((match, True) for match in REVERSE_VALUE_RE.finditer(normalized))
12462:     for match, is_reverse in sorted(matches, key=lambda item: item[0].start()):
 4149:         if is_reverse:
   55:             tail = normalized[match.end() : match.end() + 2]
   55:             if tail.startswith("头") or tail.startswith("尾"):
>>>>>>                 continue
 4149:         value = int(normalize_digit_text(match.group(1)))
 4149:         if 1 <= value <= 13:
 4149:             item = f"{value:02d}合"
 4149:             if item not in values:
 4149:                 values.append(item)
       
 4164:     return values
       
       
    1: def has_out_of_range_value(text: str) -> bool:
 4164:     normalized = normalize_digit_text(normalize_text(text))
 4164:     matches: list[tuple[re.Match[str], bool]] = []
12422:     matches.extend((match, False) for match in VALUE_RE.finditer(normalized))
 8383:     matches.extend((match, True) for match in REVERSE_VALUE_RE.finditer(normalized))
12462:     for match, is_reverse in sorted(matches, key=lambda item: item[0].start()):
 4149:         if is_reverse:
   55:             tail = normalized[match.end() : match.end() + 2]
   55:             if tail.startswith("头") or tail.startswith("尾"):
>>>>>>                 continue
 4149:         value = int(normalize_digit_text(match.group(1)))
 4149:         if not 1 <= value <= 13:
>>>>>>             return True
 4164:     return False
       
       
    1: def is_valid_success_value(value: str) -> bool:
 1395:     return bool(STRICT_SUCCESS_VALUE_RE.fullmatch(normalize_digit_text(value.strip())))
       
       
    1: def score_candidate(line: str, values: list[str]) -> int:
   39:     score = 0
   39:     normalized = normalize_text(line)
   39:     if re.search(r"绝\s*杀\s*一\s*合|絕\s*殺\s*一\s*合", normalized):
   33:         score += 100
   39:     if re.search(r"公式\s*杀\s*合|公式\s*殺\s*合", normalized):
>>>>>>         score += 90
   39:     if re.search(r"绝\s*杀\s*合|絕\s*殺\s*合", normalized):
>>>>>>         score += 80
   39:     if WEAK_KILL_SUM_RE.search(normalized):
   39:         score += 40
   39:     if "开" in normalized:
   39:         score += 10
   39:     if "属性" in normalized:
>>>>>>         score -= 20
   39:     score += len(values)
   39:     score -= min(len(line), 300) // 100
   39:     return score
       
       
    1: def is_table_kill_sum_chunk(text: str) -> bool:
 3088:     normalized = normalize_text(text)
 3088:     return "期数" in normalized and "杀合" in normalized
       
       
    1: def has_body_locator(text: str) -> bool:
 1561:     return bool(BODY_LOCATOR_RE.search(normalize_text(text)))
       
       
    1: def build_table_period_parts(chunk: str, period: int) -> list[str]:
>>>>>>     segments = split_period_segments(chunk, period)
>>>>>>     if not segments:
>>>>>>         return []
       
>>>>>>     match = PERIOD_RE.search(normalize_text(chunk))
>>>>>>     if not match:
>>>>>>         return segments
       
>>>>>>     header = normalize_text(chunk[: match.start()])
>>>>>>     if not header:
>>>>>>         return segments
       
>>>>>>     return [normalize_text(f"{header} {segment}") for segment in segments]
       
       
    1: def period_numbers_in_text(text: str) -> list[int]:
 3063:     numbers: list[int] = []
 6052:     for match in PERIOD_RE.finditer(normalize_digit_text(normalize_text(text))):
 2989:         try:
 2989:             numbers.append(int(match.group(1)))
>>>>>>         except ValueError:
>>>>>>             continue
 3063:     return numbers
       
       
    1: def is_strict_current_candidate(line: str, period: int, allow_weak: bool = False) -> bool:
  935:     normalized = normalize_digit_text(normalize_text(line))
  935:     periods = period_numbers_in_text(normalized)
  935:     if not periods or periods[0] != period:
  370:         return False
 1695:     if any(item != period for item in periods):
>>>>>>         return False
  565:     if not has_kill_sum_keyword(normalized, allow_weak):
    4:         return False
  561:     values = extract_values(normalized, allow_weak)
  561:     return len(values) == 1 and is_valid_success_value(values[0])
       
       
    1: def is_strict_period_candidate(line: str, allow_weak: bool = False) -> bool:
  875:     normalized = normalize_digit_text(normalize_text(line))
  875:     periods = period_numbers_in_text(normalized)
  875:     if not periods:
   25:         return False
 2550:     if any(item != periods[0] for item in periods):
>>>>>>         return False
  850:     if not has_kill_sum_keyword(normalized, allow_weak):
    8:         return False
  842:     values = extract_values(normalized, allow_weak)
  842:     return len(values) == 1 and is_valid_success_value(values[0])
       
       
    1: def is_contained_duplicate_candidate(line: str, values: list[str], existing: list[str], allow_weak: bool = False) -> bool:
 1388:     normalized = normalize_digit_text(normalize_text(line))
 2683:     for earlier in existing:
 2135:         earlier_normalized = normalize_digit_text(normalize_text(earlier))
 2135:         if earlier_normalized == normalized:
  836:             return True
 1299:         if earlier_normalized in normalized and extract_values(earlier_normalized, allow_weak) == values:
    4:             return True
  548:     return False
       
       
    1: def build_candidate_parts(documents: list[str], period: int, allow_weak: bool = False) -> list[tuple[str, bool]]:
  144:     parts: list[tuple[str, bool]] = []
  144:     seen: set[tuple[str, bool]] = set()
  144:     accepted_lines: list[str] = []
  289:     for document in documents:
  145:         if not has_kill_sum_keyword(document, allow_weak):
   24:             continue
  121:         document_has_locator = has_body_locator(document)
 1026:         for chunk in candidate_chunks(document, period):
  905:             chunk_has_locator = document_has_locator or has_body_locator(chunk) or is_table_kill_sum_chunk(chunk)
  905:             if is_table_kill_sum_chunk(chunk):
>>>>>>                 candidates = build_table_period_parts(chunk, period) or [chunk]
                   else:
  905:                 candidates = split_period_segments(chunk, period) or [chunk]
       
 1840:             for candidate in candidates:
  935:                 line = normalize_text(candidate)
  935:                 if not line or len(line) > 260:
>>>>>>                     continue
  935:                 if not is_strict_current_candidate(line, period, allow_weak):
  385:                     continue
  550:                 values = extract_values(line, allow_weak)
  550:                 if is_contained_duplicate_candidate(line, values, accepted_lines, allow_weak):
  421:                     continue
  129:                 key = (line, chunk_has_locator)
  129:                 if key in seen:
>>>>>>                     continue
  129:                 seen.add(key)
  129:                 accepted_lines.append(line)
  129:                 parts.append((line, chunk_has_locator))
  144:     return parts
       
       
    1: def latest_candidate_chunks(document: str) -> list[str]:
  122:     chunks: list[str] = []
  122:     soup = BeautifulSoup(document, "html.parser")
       
  122:     for tag in soup.find_all(["p", "div", "td", "tr", "li", "font", "span", "b", "strong"]):
>>>>>>         text = normalize_text(tag.get_text(" ", strip=True))
>>>>>>         if text:
>>>>>>             chunks.append(text)
       
  694:     lines = [normalize_text(line) for line in soup.get_text("\n", strip=True).splitlines()]
  694:     chunks.extend(line for line in lines if line)
       
  122:     all_text = normalize_text(soup.get_text(" ", strip=True) or document)
  122:     chunks.extend(split_all_period_segments(all_text))
  122:     return chunks
       
       
    5: def build_latest_candidate_parts(
    1:     documents: list[str],
    2:     allow_weak: bool = False,
    2:     stop_after: int | None = None,
    1: ) -> list[tuple[str, bool]]:
  141:     parts: list[tuple[str, bool]] = []
  141:     seen: set[tuple[str, bool]] = set()
  141:     accepted_lines: list[str] = []
  283:     for document in documents:
  142:         if not has_kill_sum_keyword(document, allow_weak):
   20:             continue
  122:         document_has_locator = has_body_locator(document)
  997:         for chunk in latest_candidate_chunks(document):
  875:             chunk_has_locator = document_has_locator or has_body_locator(chunk) or is_table_kill_sum_chunk(chunk)
  875:             if is_table_kill_sum_chunk(chunk):
>>>>>>                 candidates = split_all_period_segments(chunk) or [chunk]
                   else:
  875:                 candidates = split_all_period_segments(chunk) or [chunk]
       
 1750:             for candidate in candidates:
  875:                 line = normalize_text(candidate)
  875:                 if not line or len(line) > 260:
>>>>>>                     continue
  875:                 if not is_strict_period_candidate(line, allow_weak):
   37:                     continue
  838:                 values = extract_values(line, allow_weak)
  838:                 if is_contained_duplicate_candidate(line, values, accepted_lines, allow_weak):
  419:                     continue
  419:                 key = (line, chunk_has_locator)
  419:                 if key in seen:
>>>>>>                     continue
  419:                 seen.add(key)
  419:                 accepted_lines.append(line)
  419:                 parts.append((line, chunk_has_locator))
  419:                 if stop_after is not None and len(parts) >= stop_after:
>>>>>>                     return parts
  141:     return parts
       
       
    6: def selected_latest_period_candidate(
    1:     documents: list[str],
    1:     pick: str,
    2:     require_body_locator: bool = True,
    2:     allow_weak: bool = False,
    1: ) -> Candidate | None:
    2:     pick = normalize_pick(pick)
    2:     parts = build_latest_candidate_parts(documents, allow_weak)
    2:     if require_body_locator:
    6:         parts = [(line, has_locator) for line, has_locator in parts if has_locator]
    2:     if not parts:
    1:         return None
       
    1:     line, _ = parts[0] if pick == "top" else parts[-1]
    1:     values = extract_values(line, allow_weak)
    1:     if len(values) != 1:
>>>>>>         return None
    2:     return Candidate(
    1:         values=",".join(values),
    1:         line=line,
    1:         score=score_candidate(line, values),
    1:         order=0,
           )
       
       
    1: def should_apply_candidate_window(current_parts: list[tuple[str, bool]], all_parts: list[tuple[str, bool]]) -> bool:
    4:     return True
       
       
    5: def directional_candidate_window_parts(
    1:     all_parts: list[tuple[str, bool]],
    1:     pick: str,
    2:     size: int = 1,
    1: ) -> list[tuple[str, bool]]:
           """Select only the positional edge row from one candidate block.
       
           The legacy ``size`` argument is retained for API compatibility.  A
           direction is a hard boundary now: rows after a ``top`` target (or before
           a ``bottom`` target) cannot make that target eligible.
           """
   24:     pick = normalize_pick(pick)
   24:     if not all_parts:
    2:         return []
   22:     return [all_parts[0]] if pick == "top" else [all_parts[-1]]
       
       
    7: def ordered_candidate_window_parts(
    1:     all_parts: list[tuple[str, bool]],
    1:     pick: str,
    2:     allow_weak: bool = False,
    2:     period: int | None = None,
    2:     size: int = 1,
    1: ) -> list[tuple[str, bool]]:
    4:     return directional_candidate_window_parts(all_parts, pick, size)
       
       
    7: def ordered_candidate_window_periods(
    1:     all_parts: list[tuple[str, bool]],
    1:     pick: str,
    2:     allow_weak: bool = False,
    2:     period: int | None = None,
    2:     size: int = 1,
    1: ) -> set[int]:
    1:     edge = directional_candidate_window_parts(all_parts, pick, size)
    4:     return {
    1:         key[0]
    2:         for line, _has_locator in edge
    1:         for key in [candidate_window_key(line, allow_weak)]
    3:         if key is not None
           }
       
       
    6: def candidate_window_lines(
    1:     all_parts: list[tuple[str, bool]],
    1:     pick: str,
    2:     period: int | None = None,
    2:     size: int = 1,
    1: ) -> set[str]:
    1:     window_parts = ordered_candidate_window_parts(all_parts, pick, period=period, size=size)
    5:     return {
    1:         normalize_digit_text(normalize_text(line))
    2:         for line, _ in window_parts
           }
       
       
    1: def candidate_window_key(line: str, allow_weak: bool = False) -> tuple[int, str] | None:
 1220:     periods = period_numbers_in_text(line)
 1220:     values = extract_values(line, allow_weak)
 1220:     if not periods or len(values) != 1:
    1:         return None
 1219:     return periods[0], values[0]
       
       
    7: def candidate_window_keys(
    1:     all_parts: list[tuple[str, bool]],
    1:     pick: str,
    2:     allow_weak: bool = False,
    2:     period: int | None = None,
    2:     size: int = 1,
    1: ) -> set[tuple[int, str]]:
    1:     window_parts = ordered_candidate_window_parts(all_parts, pick, allow_weak, period, size)
    1:     keys = set()
    2:     for line, _ in window_parts:
    1:         key = candidate_window_key(line, allow_weak)
    1:         if key is not None:
    1:             keys.add(key)
    1:     return keys
       
       
    6: def window_basis_parts(
    1:     all_parts: list[tuple[str, bool]],
    1:     current_parts: list[tuple[str, bool]],
    1:     period: int,
    2:     allow_weak: bool = False,
    1: ) -> list[tuple[str, bool]]:
           # Candidate order is the evidence.  Never sort or filter by period number
           # when constructing the direction basis.  ``current_parts`` is used only
           # when the full document produced no candidate rows at all.
   21:     return list(all_parts) if all_parts else list(current_parts)
       
       
    7: def current_candidates_outside_window(
    1:     documents: list[str],
    1:     period: int,
    1:     pick: str,
    2:     require_body_locator: bool = True,
    2:     allow_weak: bool = False,
    1: ) -> bool:
    6:     current_parts = build_candidate_parts(documents, period, allow_weak)
    6:     all_parts = build_latest_candidate_parts(documents, allow_weak)
    6:     if require_body_locator:
   16:         current_parts = [(line, has_locator) for line, has_locator in current_parts if has_locator]
   26:         all_parts = [(line, has_locator) for line, has_locator in all_parts if has_locator]
    6:     all_parts = window_basis_parts(all_parts, current_parts, period, allow_weak)
    6:     if not current_parts or not all_parts or not should_apply_candidate_window(current_parts, all_parts):
    2:         return False
    4:     edge = directional_candidate_window_parts(all_parts, pick, 1)
    4:     edge_key = candidate_window_key(edge[0][0], allow_weak) if edge else None
    4:     return edge_key is None or edge_key[0] != period
       
       
    1: def pick_region_label(pick: str) -> str:
    6:     pick = normalize_pick(pick)
    6:     if pick == "top":
    5:         return "候选数据靠前位置"
    1:     if pick == "bottom":
    1:         return "候选数据靠后位置"
>>>>>>     return "候选数据区域"
       
       
    1: def directional_three_label(pick: str) -> str:
   73:     return "第一条" if normalize_pick(pick) == "top" else "最后一条"
       
       
    7: def find_candidate(
    1:     documents: list[str],
    1:     period: int,
    1:     pick: str,
    2:     require_body_locator: bool = True,
    2:     allow_weak: bool = False,
    1: ) -> Candidate | None:
    2:     parts = trusted_current_candidate_parts(documents, period, pick, require_body_locator, allow_weak)
    2:     return select_candidate_from_trusted_parts(parts, pick, allow_weak)
       
       
    5: def select_candidate_from_trusted_parts(
    1:     parts: list[tuple[str, bool]],
    1:     pick: str,
    2:     allow_weak: bool = False,
    1: ) -> Candidate | None:
   18:     pick = normalize_pick(pick)
   18:     candidates: list[Candidate] = []
   18:     seen: set[tuple[str, str]] = set()
   18:     order = 0
   32:     for line, _ in parts:
   14:         values = extract_values(line, allow_weak)
   14:         if len(values) != 1:
    2:             continue
       
   12:         values_text = ",".join(values)
   12:         key = (values_text, line)
   12:         if key in seen:
>>>>>>             continue
   12:         seen.add(key)
       
   24:         candidates.append(
   24:             Candidate(
   12:                 values=values_text,
   12:                 line=line,
   12:                 score=score_candidate(line, values),
   12:                 order=order,
                   )
               )
   12:         order += 1
       
   18:     if not candidates:
    8:         return None
       
   10:     if pick == "top":
   15:         return min(candidates, key=lambda item: item.order)
    3:     if pick == "bottom":
    7:         return max(candidates, key=lambda item: item.order)
>>>>>>     best_score = max(candidate.score for candidate in candidates)
>>>>>>     good = [candidate for candidate in candidates if candidate.score >= best_score - 15]
>>>>>>     return max(good, key=lambda item: item.order)
       
       
    4: def conflict_values_from_trusted_parts(
    1:     parts: list[tuple[str, bool]],
    2:     allow_weak: bool = False,
    1: ) -> tuple[list[str], list[str]]:
   14:     values_by_line: list[tuple[str, str]] = []
   14:     seen_lines: set[str] = set()
   24:     for line, _ in parts:
   10:         if line in seen_lines:
>>>>>>             continue
   10:         seen_lines.add(line)
   10:         values = extract_values(line, allow_weak)
   10:         if len(values) != 1:
    1:             continue
    9:         values_by_line.append((values[0], line))
   46:     unique_values = sorted({value for value, _ in values_by_line}, key=lambda value: int(value[:2]))
   14:     if len(unique_values) <= 1:
   13:         return [], []
    4:     return unique_values, [line for _, line in values_by_line]
       
       
    7: def trusted_candidate_with_conflict(
    1:     documents: list[str],
    1:     period: int,
    1:     pick: str,
    2:     require_body_locator: bool = True,
    2:     allow_weak: bool = False,
    1: ) -> tuple[Candidate | None, list[str], list[str]]:
   26:     parts = trusted_current_candidate_parts(
   13:         documents, period, pick, require_body_locator, allow_weak
           )
   13:     conflict_values, conflict_lines = conflict_values_from_trusted_parts(parts, allow_weak)
   13:     if conflict_values:
>>>>>>         return None, conflict_values, conflict_lines
   13:     return select_candidate_from_trusted_parts(parts, pick, allow_weak), [], []
       
       
    7: def trusted_current_candidate_parts(
    1:     documents: list[str],
    1:     period: int,
    1:     pick: str,
    2:     require_body_locator: bool = True,
    2:     allow_weak: bool = False,
    1: ) -> list[tuple[str, bool]]:
   15:     pick = normalize_pick(pick)
   15:     parts = build_candidate_parts(documents, period, allow_weak)
   15:     if require_body_locator:
   41:         parts = [(line, has_locator) for line, has_locator in parts if has_locator]
   15:     all_parts = build_latest_candidate_parts(documents, allow_weak)
   15:     if require_body_locator:
   70:         all_parts = [(line, has_locator) for line, has_locator in all_parts if has_locator]
   15:     all_parts = window_basis_parts(all_parts, parts, period, allow_weak)
   15:     edge = directional_candidate_window_parts(all_parts, pick, 1)
   15:     if not edge:
    2:         return []
   13:     edge_key = candidate_window_key(edge[0][0], allow_weak)
   13:     if edge_key is None or edge_key[0] != period:
    5:         return []
       
           # Prefer the target-period representation from ``parts`` so evidence and
           # diagnostics retain the original row text.  Require the same normalized
           # line as the edge row when possible; matching only on period would let an
           # interior duplicate masquerade as the edge candidate.
    8:     edge_line = normalize_digit_text(normalize_text(edge[0][0]))
   36:     matching = [
    8:         part
    8:         for part in parts
   28:         if normalize_digit_text(normalize_text(part[0])) == edge_line
           ]
    8:     return matching[:1] if matching else edge[:1]
       
       
    7: def conflicting_trusted_candidate_values(
    1:     documents: list[str],
    1:     period: int,
    1:     pick: str,
    2:     require_body_locator: bool = True,
    2:     allow_weak: bool = False,
    1: ) -> tuple[list[str], list[str]]:
>>>>>>     parts = trusted_current_candidate_parts(documents, period, pick, require_body_locator, allow_weak)
>>>>>>     return conflict_values_from_trusted_parts(parts, allow_weak)
       
       
    1: def collect_period_lines(documents: list[str], period: int, allow_weak: bool = False) -> list[str]:
    1:     lines: list[str] = []
    1:     seen: set[str] = set()
    2:     for line, _ in build_candidate_parts(documents, period, allow_weak):
    1:         if line in seen:
>>>>>>             continue
    1:         seen.add(line)
    1:         lines.append(line)
    1:     return lines
       
       
    1: def diagnose_candidate_state(documents: list[str], period: int, allow_weak: bool = False) -> dict[str, bool]:
    2:     state = {
    2:         "has_period": False,
    2:         "has_keyword": False,
    2:         "has_valid_value": False,
    2:         "has_multiple_values": False,
    2:         "has_strict_candidate": False,
    2:         "has_locator": False,
           }
       
    4:     for document in documents:
    2:         document_has_locator = has_body_locator(document)
    7:         for chunk in candidate_chunks(document, period):
    5:             chunk_has_locator = document_has_locator or has_body_locator(chunk) or is_table_kill_sum_chunk(chunk)
    5:             candidates = split_period_segments(chunk, period) or [chunk]
   10:             for candidate in candidates:
    5:                 line = normalize_digit_text(normalize_text(candidate))
    5:                 periods = period_numbers_in_text(line)
    5:                 if period not in periods:
    1:                     continue
    4:                 state["has_period"] = True
    4:                 if not has_kill_sum_keyword(line, allow_weak):
    4:                     continue
>>>>>>                 state["has_keyword"] = True
>>>>>>                 values = extract_values(line, allow_weak)
>>>>>>                 if len(values) > 1:
>>>>>>                     state["has_multiple_values"] = True
>>>>>>                 if len(values) == 1 and is_valid_success_value(values[0]):
>>>>>>                     state["has_valid_value"] = True
>>>>>>                 if is_strict_current_candidate(line, period, allow_weak):
>>>>>>                     state["has_strict_candidate"] = True
>>>>>>                     if chunk_has_locator:
>>>>>>                         state["has_locator"] = True
       
    2:     return state
       
       
    1: def analyze_missing_reason(documents: list[str], period: int, pick: str = "", allow_weak: bool = False) -> tuple[str, str]:
    2:     parts = build_candidate_parts(documents, period, allow_weak)
    2:     region = pick_region_label(pick)
    2:     if not parts:
    2:         state = diagnose_candidate_state(documents, period, allow_weak)
    2:         if not state["has_period"]:
    1:             return "无当期", f"{region}里没找到{period}期"
    1:         if not state["has_keyword"]:
    1:             return "无关键词", f"{region}找到{period}期，但没匹配到严格抓取词"
>>>>>>         if state["has_multiple_values"]:
>>>>>>             return "数据不完整", f"{region}找到{period}期和严格抓取词，但数量校验失败"
>>>>>>         if not state["has_valid_value"]:
>>>>>>             return "数据不完整", f"{region}找到{period}期和严格抓取词，但没提取到有效合数"
>>>>>>         return "未找到目标", f"{region}里没找到{period}期的严格杀合内容"
       
>>>>>>     if not any(has_locator for _, has_locator in parts):
>>>>>>         return "无正文定位", f"{region}找到{period}期，但没匹配到正文定位词"
       
>>>>>>     quantity_failed = False
>>>>>>     for line, _ in parts:
>>>>>>         values = extract_values(line, allow_weak)
>>>>>>         if len(values) > 1:
>>>>>>             quantity_failed = True
       
>>>>>>     if quantity_failed:
>>>>>>         return "数据不完整", f"{region}找到{period}期、正文定位词和严格抓取词，但数量校验失败"
       
>>>>>>     return "数据不完整", f"{region}找到{period}期、正文定位词和严格抓取词，但没提取到有效合数"
       
       
    1: def classify_failure(period: int, detail: str, error: str | None) -> FailureInfo:
    7:     normalized_error = (error or "").strip()
    7:     normalized_detail = (detail or "").strip()
       
    7:     if normalized_error:
    3:         if "未抓到可参与重复检测的数据" in normalized_error or "未找到完整数据" in normalized_error:
>>>>>>             return FailureInfo("未找到目标", normalized_error)
    3:         if "curl exit" in normalized_error:
>>>>>>             return FailureInfo("请求失败", f"curl 连接失败: {normalized_error}")
    3:         if "net::ERR_CONNECTION_CLOSED" in normalized_error or "net::ERR_CONNECTION_RESET" in normalized_error:
>>>>>>             return FailureInfo("请求失败", "浏览器访问时连接被远端关闭或重置")
    3:         request_markers = (
                   "Timeout",
                   "ConnectionError",
                   "CalledProcessError",
                   "curl exit",
                   "HTTP Error",
                   "HTTPError",
                   "TooManyRedirects",
                   "SSLError",
                   "ProxyError",
                   "ChunkedEncodingError",
                   "RequestException",
                   "HTTPConnectionPool",
                   "HTTPSConnectionPool",
                   "net::ERR_",
               )
    6:         category = "请求失败" if any(marker in normalized_error for marker in request_markers) else "执行失败"
    3:         return FailureInfo(category, normalized_error)
       
    4:     if normalized_detail == "未执行":
    1:         return FailureInfo("未执行", "站点任务未执行")
       
    3:     if "没找到作者锚点" in normalized_detail:
    1:         return FailureInfo("锚点缺失", normalized_detail)
    2:     if "澳门综合杀表格里没找到" in normalized_detail or "绝杀①段①合表格里没找到" in normalized_detail:
>>>>>>         return FailureInfo("专属缺期", normalized_detail)
    2:     if re.search(r"里没找到\d{1,4}期", normalized_detail):
    1:         return FailureInfo("无当期", normalized_detail)
    1:     if "没匹配到严格抓取词" in normalized_detail:
>>>>>>         return FailureInfo("无关键词", normalized_detail)
    1:     if "没匹配到正文定位词" in normalized_detail:
>>>>>>         return FailureInfo("无正文定位", normalized_detail)
    1:     if "作者块下没找到" in normalized_detail:
>>>>>>         return FailureInfo("作者块无当期", normalized_detail)
       
    1:     if normalized_detail.startswith("页面里没找到"):
>>>>>>         return FailureInfo("无当期", normalized_detail)
    1:     if "数量校验失败" in normalized_detail:
    1:         return FailureInfo("数据不完整", normalized_detail)
>>>>>>     if "没提取到有效合数" in normalized_detail:
>>>>>>         return FailureInfo("数据不完整", normalized_detail)
>>>>>>     if normalized_detail:
>>>>>>         return FailureInfo("未找到目标", normalized_detail)
       
>>>>>>     return FailureInfo("未找到目标", f"页面里没找到{period}期的严格杀合内容")
       
       
    1: def failure_stage(category: str) -> str:
    8:     if category in {"请求失败", "执行失败"}:
    2:         return "网络请求"
    6:     if category in {"锚点缺失", "无正文定位", "记录ID缺失/不一致"}:
    1:         return "目标定位"
    5:     if category in {"数据不完整", "候选冲突", "区块冲突", "来源冲突"}:
    1:         return "数据校验"
    4:     if category in {"缓存不可用", "缓存冲突"}:
>>>>>>         return "缓存校验"
    4:     if category in {"重复拒收", "判重未完成"}:
>>>>>>         return "重复检测"
    4:     if category == "未执行":
    1:         return "任务执行"
    3:     return "指定期数校验"
       
       
    7: def format_failure_result(
    1:     period: int,
    1:     site: Site,
    1:     detail: str,
    1:     error: str | None,
    2:     category: str | None = None,
    1: ) -> str:
    3:     if category and not error:
    2:         failure = FailureInfo(category, detail.strip() or f"页面里没找到{period}期的严格杀合内容")
           else:
    1:         failure = classify_failure(period, detail, error)
    3:     reason = " ".join(failure.reason.split())
    3:     site_prefix = f"{site.name} "
    3:     if reason.startswith(site_prefix):
    1:         reason = reason[len(site_prefix) :].lstrip()
    3:     return (
    9:         f"失败 {site.name} {site.url} 方向: {site.pick} 期数: {period} "
    6:         f"阶段: {failure_stage(failure.category)} 原因: {reason}"
           )
       
       
  135: __all__ = [
   43:     name
   67:     for name, value in globals().items()
  176:     if callable(value) and getattr(value, "__module__", None) == __name__
       ]

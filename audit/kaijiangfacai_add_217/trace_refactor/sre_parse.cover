       #
       # Secret Labs' Regular Expression Engine
       #
       # convert re-style regular expression to sre pattern
       #
       # Copyright (c) 1998-2001 by Secret Labs AB.  All rights reserved.
       #
       # See the sre.py file for information on usage and redistribution.
       #
       
>>>>>> """Internal support module for sre"""
       
       # XXX: show string offset and offending character for all errors
       
>>>>>> from sre_constants import *
       
>>>>>> SPECIAL_CHARS = ".\\[{()*+?^$|"
>>>>>> REPEAT_CHARS = "*+?{"
       
>>>>>> DIGITS = frozenset("0123456789")
       
>>>>>> OCTDIGITS = frozenset("01234567")
>>>>>> HEXDIGITS = frozenset("0123456789abcdefABCDEF")
>>>>>> ASCIILETTERS = frozenset("abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ")
       
>>>>>> WHITESPACE = frozenset(" \t\n\r\v\f")
       
>>>>>> _REPEATCODES = frozenset({MIN_REPEAT, MAX_REPEAT})
>>>>>> _UNITCODES = frozenset({ANY, RANGE, IN, LITERAL, NOT_LITERAL, CATEGORY})
       
>>>>>> ESCAPES = {
>>>>>>     r"\a": (LITERAL, ord("\a")),
>>>>>>     r"\b": (LITERAL, ord("\b")),
>>>>>>     r"\f": (LITERAL, ord("\f")),
>>>>>>     r"\n": (LITERAL, ord("\n")),
>>>>>>     r"\r": (LITERAL, ord("\r")),
>>>>>>     r"\t": (LITERAL, ord("\t")),
>>>>>>     r"\v": (LITERAL, ord("\v")),
>>>>>>     r"\\": (LITERAL, ord("\\"))
       }
       
>>>>>> CATEGORIES = {
>>>>>>     r"\A": (AT, AT_BEGINNING_STRING), # start of string
>>>>>>     r"\b": (AT, AT_BOUNDARY),
>>>>>>     r"\B": (AT, AT_NON_BOUNDARY),
>>>>>>     r"\d": (IN, [(CATEGORY, CATEGORY_DIGIT)]),
>>>>>>     r"\D": (IN, [(CATEGORY, CATEGORY_NOT_DIGIT)]),
>>>>>>     r"\s": (IN, [(CATEGORY, CATEGORY_SPACE)]),
>>>>>>     r"\S": (IN, [(CATEGORY, CATEGORY_NOT_SPACE)]),
>>>>>>     r"\w": (IN, [(CATEGORY, CATEGORY_WORD)]),
>>>>>>     r"\W": (IN, [(CATEGORY, CATEGORY_NOT_WORD)]),
>>>>>>     r"\Z": (AT, AT_END_STRING), # end of string
       }
       
>>>>>> FLAGS = {
           # standard flags
>>>>>>     "i": SRE_FLAG_IGNORECASE,
>>>>>>     "L": SRE_FLAG_LOCALE,
>>>>>>     "m": SRE_FLAG_MULTILINE,
>>>>>>     "s": SRE_FLAG_DOTALL,
>>>>>>     "x": SRE_FLAG_VERBOSE,
           # extensions
>>>>>>     "a": SRE_FLAG_ASCII,
>>>>>>     "t": SRE_FLAG_TEMPLATE,
>>>>>>     "u": SRE_FLAG_UNICODE,
       }
       
>>>>>> TYPE_FLAGS = SRE_FLAG_ASCII | SRE_FLAG_LOCALE | SRE_FLAG_UNICODE
>>>>>> GLOBAL_FLAGS = SRE_FLAG_DEBUG | SRE_FLAG_TEMPLATE
       
>>>>>> class Verbose(Exception):
>>>>>>     pass
       
>>>>>> class State:
           # keeps track of state for parsing
>>>>>>     def __init__(self):
  209:         self.flags = 0
  209:         self.groupdict = {}
  209:         self.groupwidths = [None]  # group 0
  209:         self.lookbehindgroups = None
  209:         self.grouprefpos = {}
>>>>>>     @property
>>>>>>     def groups(self):
  743:         return len(self.groupwidths)
>>>>>>     def opengroup(self, name=None):
  147:         gid = self.groups
  147:         self.groupwidths.append(None)
  147:         if self.groups > MAXGROUPS:
>>>>>>             raise error("too many groups")
  147:         if name is not None:
   59:             ogid = self.groupdict.get(name, None)
   59:             if ogid is not None:
>>>>>>                 raise error("redefinition of group name %r as group %d; "
>>>>>>                             "was group %d" % (name, gid,  ogid))
   59:             self.groupdict[name] = gid
  147:         return gid
>>>>>>     def closegroup(self, gid, p):
  147:         self.groupwidths[gid] = p.getwidth()
>>>>>>     def checkgroup(self, gid):
>>>>>>         return gid < self.groups and self.groupwidths[gid] is not None
       
>>>>>>     def checklookbehindgroup(self, gid, source):
>>>>>>         if self.lookbehindgroups is not None:
>>>>>>             if not self.checkgroup(gid):
>>>>>>                 raise source.error('cannot refer to an open group')
>>>>>>             if gid >= self.lookbehindgroups:
>>>>>>                 raise source.error('cannot refer to group defined in the same '
                                          'lookbehind subpattern')
       
>>>>>> class SubPattern:
           # a subpattern, in intermediate form
>>>>>>     def __init__(self, state, data=None):
 5872:         self.state = state
 5872:         if data is None:
 5014:             data = []
 5872:         self.data = data
 5872:         self.width = None
       
>>>>>>     def dump(self, level=0):
>>>>>>         nl = True
>>>>>>         seqtypes = (tuple, list)
>>>>>>         for op, av in self.data:
>>>>>>             print(level*"  " + str(op), end='')
>>>>>>             if op is IN:
                       # member sublanguage
>>>>>>                 print()
>>>>>>                 for op, a in av:
>>>>>>                     print((level+1)*"  " + str(op), a)
>>>>>>             elif op is BRANCH:
>>>>>>                 print()
>>>>>>                 for i, a in enumerate(av[1]):
>>>>>>                     if i:
>>>>>>                         print(level*"  " + "OR")
>>>>>>                     a.dump(level+1)
>>>>>>             elif op is GROUPREF_EXISTS:
>>>>>>                 condgroup, item_yes, item_no = av
>>>>>>                 print('', condgroup)
>>>>>>                 item_yes.dump(level+1)
>>>>>>                 if item_no:
>>>>>>                     print(level*"  " + "ELSE")
>>>>>>                     item_no.dump(level+1)
>>>>>>             elif isinstance(av, seqtypes):
>>>>>>                 nl = False
>>>>>>                 for a in av:
>>>>>>                     if isinstance(a, SubPattern):
>>>>>>                         if not nl:
>>>>>>                             print()
>>>>>>                         a.dump(level+1)
>>>>>>                         nl = True
                           else:
>>>>>>                         if not nl:
>>>>>>                             print(' ', end='')
>>>>>>                         print(a, end='')
>>>>>>                         nl = False
>>>>>>                 if not nl:
>>>>>>                     print()
                   else:
>>>>>>                 print('', av)
>>>>>>     def __repr__(self):
>>>>>>         return repr(self.data)
>>>>>>     def __len__(self):
11846:         return len(self.data)
>>>>>>     def __delitem__(self, index):
    4:         del self.data[index]
>>>>>>     def __getitem__(self, index):
28316:         if isinstance(index, slice):
  858:             return SubPattern(self.state, self.data[index])
27458:         return self.data[index]
>>>>>>     def __setitem__(self, index, code):
 1182:         self.data[index] = code
>>>>>>     def insert(self, index, code):
>>>>>>         self.data.insert(index, code)
>>>>>>     def append(self, code):
 7527:         self.data.append(code)
>>>>>>     def getwidth(self):
               # determine the width (min, max) for this subpattern
 5255:         if self.width is not None:
  147:             return self.width
 5108:         lo = hi = 0
12628:         for op, av in self.data:
 7520:             if op is BRANCH:
  393:                 i = MAXREPEAT - 1
  393:                 j = 0
 4247:                 for av in av[1]:
 3854:                     l, h = av.getwidth()
 3854:                     i = min(i, l)
 3854:                     j = max(j, h)
  393:                 lo = lo + i
  393:                 hi = hi + j
 7127:             elif op is CALL:
>>>>>>                 i, j = av.getwidth()
>>>>>>                 lo = lo + i
>>>>>>                 hi = hi + j
 7127:             elif op is SUBPATTERN:
  159:                 i, j = av[-1].getwidth()
  159:                 lo = lo + i
  159:                 hi = hi + j
 6968:             elif op in _REPEATCODES:
  854:                 i, j = av[2].getwidth()
  854:                 lo = lo + i * av[0]
  854:                 hi = hi + j * av[1]
 6114:             elif op in _UNITCODES:
 5679:                 lo = lo + 1
 5679:                 hi = hi + 1
  435:             elif op is GROUPREF:
>>>>>>                 i, j = self.state.groupwidths[av]
>>>>>>                 lo = lo + i
>>>>>>                 hi = hi + j
  435:             elif op is GROUPREF_EXISTS:
>>>>>>                 i, j = av[1].getwidth()
>>>>>>                 if av[2] is not None:
>>>>>>                     l, h = av[2].getwidth()
>>>>>>                     i = min(i, l)
>>>>>>                     j = max(j, h)
                       else:
>>>>>>                     i = 0
>>>>>>                 lo = lo + i
>>>>>>                 hi = hi + j
  435:             elif op is SUCCESS:
>>>>>>                 break
 5108:         self.width = min(lo, MAXREPEAT - 1), min(hi, MAXREPEAT)
 5108:         return self.width
       
>>>>>> class Tokenizer:
>>>>>>     def __init__(self, string):
  208:         self.istext = isinstance(string, str)
  208:         self.string = string
  208:         if not self.istext:
   10:             string = str(string, 'latin1')
  208:         self.decoded_string = string
  208:         self.index = 0
  208:         self.next = None
  208:         self.__next()
>>>>>>     def __next(self):
19914:         index = self.index
19914:         try:
19914:             char = self.decoded_string[index]
  208:         except IndexError:
  208:             self.next = None
  208:             return
19706:         if char == "\\":
 1964:             index += 1
 1964:             try:
 1964:                 char += self.decoded_string[index]
>>>>>>             except IndexError:
>>>>>>                 raise error("bad escape (end of pattern)",
>>>>>>                             self.string, len(self.string) - 1) from None
19706:         self.index = index + 1
19706:         self.next = char
>>>>>>     def match(self, char):
10079:         if char == self.next:
 6106:             self.__next()
 6106:             return True
 3973:         return False
>>>>>>     def get(self):
12435:         this = self.next
12435:         self.__next()
12435:         return this
>>>>>>     def getwhile(self, n, charset):
  409:         result = ''
 1227:         for _ in range(n):
  818:             c = self.next
  818:             if c not in charset:
>>>>>>                 break
  818:             result += c
  818:             self.__next()
  409:         return result
>>>>>>     def getuntil(self, terminator, name):
   59:         result = ''
  345:         while True:
  345:             c = self.next
  345:             self.__next()
  345:             if c is None:
>>>>>>                 if not result:
>>>>>>                     raise self.error("missing " + name)
>>>>>>                 raise self.error("missing %s, unterminated name" % terminator,
>>>>>>                                  len(result))
  345:             if c == terminator:
   59:                 if not result:
>>>>>>                     raise self.error("missing " + name, 1)
   59:                 break
  286:             result += c
   59:         return result
>>>>>>     @property
>>>>>>     def pos(self):
>>>>>>         return self.index - len(self.next or '')
>>>>>>     def tell(self):
 3712:         return self.index - len(self.next or '')
>>>>>>     def seek(self, index):
    2:         self.index = index
    2:         self.__next()
       
>>>>>>     def error(self, msg, offset=0):
>>>>>>         return error(msg, self.string, self.tell() - offset)
       
>>>>>> def _class_escape(source, escape):
           # handle escape code inside character class
 1010:     code = ESCAPES.get(escape)
 1010:     if code:
  520:         return code
  490:     code = CATEGORIES.get(escape)
  490:     if code and code[0] is IN:
   17:         return code
  473:     try:
  473:         c = escape[1:2]
  473:         if c == "x":
                   # hexadecimal escape (exactly two digits)
  405:             escape += source.getwhile(2, HEXDIGITS)
  405:             if len(escape) != 4:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
  405:             return LITERAL, int(escape[2:], 16)
   68:         elif c == "u" and source.istext:
                   # unicode escape (exactly four digits)
>>>>>>             escape += source.getwhile(4, HEXDIGITS)
>>>>>>             if len(escape) != 6:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
>>>>>>             return LITERAL, int(escape[2:], 16)
   68:         elif c == "U" and source.istext:
                   # unicode escape (exactly eight digits)
>>>>>>             escape += source.getwhile(8, HEXDIGITS)
>>>>>>             if len(escape) != 10:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
>>>>>>             c = int(escape[2:], 16)
>>>>>>             chr(c) # raise ValueError for invalid code
>>>>>>             return LITERAL, c
   68:         elif c == "N" and source.istext:
>>>>>>             import unicodedata
                   # named unicode escape e.g. \N{EM DASH}
>>>>>>             if not source.match('{'):
>>>>>>                 raise source.error("missing {")
>>>>>>             charname = source.getuntil('}', 'character name')
>>>>>>             try:
>>>>>>                 c = ord(unicodedata.lookup(charname))
>>>>>>             except (KeyError, TypeError):
>>>>>>                 raise source.error("undefined character name %r" % charname,
>>>>>>                                    len(charname) + len(r'\N{}'))
>>>>>>             return LITERAL, c
   68:         elif c in OCTDIGITS:
                   # octal escape (up to three digits)
    4:             escape += source.getwhile(2, OCTDIGITS)
    4:             c = int(escape[1:], 8)
    4:             if c > 0o377:
>>>>>>                 raise source.error('octal escape value %s outside of '
>>>>>>                                    'range 0-0o377' % escape, len(escape))
    4:             return LITERAL, c
   64:         elif c in DIGITS:
>>>>>>             raise ValueError
   64:         if len(escape) == 2:
   64:             if c in ASCIILETTERS:
>>>>>>                 raise source.error('bad escape %s' % escape, len(escape))
   64:             return LITERAL, ord(escape[1])
>>>>>>     except ValueError:
>>>>>>         pass
>>>>>>     raise source.error("bad escape %s" % escape, len(escape))
       
>>>>>> def _escape(source, escape, state):
           # handle escape code in expression
  954:     code = CATEGORIES.get(escape)
  954:     if code:
  311:         return code
  643:     code = ESCAPES.get(escape)
  643:     if code:
  425:         return code
  218:     try:
  218:         c = escape[1:2]
  218:         if c == "x":
                   # hexadecimal escape
>>>>>>             escape += source.getwhile(2, HEXDIGITS)
>>>>>>             if len(escape) != 4:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
>>>>>>             return LITERAL, int(escape[2:], 16)
  218:         elif c == "u" and source.istext:
                   # unicode escape (exactly four digits)
>>>>>>             escape += source.getwhile(4, HEXDIGITS)
>>>>>>             if len(escape) != 6:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
>>>>>>             return LITERAL, int(escape[2:], 16)
  218:         elif c == "U" and source.istext:
                   # unicode escape (exactly eight digits)
>>>>>>             escape += source.getwhile(8, HEXDIGITS)
>>>>>>             if len(escape) != 10:
>>>>>>                 raise source.error("incomplete escape %s" % escape, len(escape))
>>>>>>             c = int(escape[2:], 16)
>>>>>>             chr(c) # raise ValueError for invalid code
>>>>>>             return LITERAL, c
  218:         elif c == "N" and source.istext:
>>>>>>             import unicodedata
                   # named unicode escape e.g. \N{EM DASH}
>>>>>>             if not source.match('{'):
>>>>>>                 raise source.error("missing {")
>>>>>>             charname = source.getuntil('}', 'character name')
>>>>>>             try:
>>>>>>                 c = ord(unicodedata.lookup(charname))
>>>>>>             except (KeyError, TypeError):
>>>>>>                 raise source.error("undefined character name %r" % charname,
>>>>>>                                    len(charname) + len(r'\N{}'))
>>>>>>             return LITERAL, c
  218:         elif c == "0":
                   # octal escape
>>>>>>             escape += source.getwhile(2, OCTDIGITS)
>>>>>>             return LITERAL, int(escape[1:], 8)
  218:         elif c in DIGITS:
                   # octal escape *or* decimal group reference (sigh)
>>>>>>             if source.next in DIGITS:
>>>>>>                 escape += source.get()
>>>>>>                 if (escape[1] in OCTDIGITS and escape[2] in OCTDIGITS and
>>>>>>                     source.next in OCTDIGITS):
                           # got three octal digits; this is an octal escape
>>>>>>                     escape += source.get()
>>>>>>                     c = int(escape[1:], 8)
>>>>>>                     if c > 0o377:
>>>>>>                         raise source.error('octal escape value %s outside of '
>>>>>>                                            'range 0-0o377' % escape,
>>>>>>                                            len(escape))
>>>>>>                     return LITERAL, c
                   # not an octal escape, so this is a group reference
>>>>>>             group = int(escape[1:])
>>>>>>             if group < state.groups:
>>>>>>                 if not state.checkgroup(group):
>>>>>>                     raise source.error("cannot refer to an open group",
>>>>>>                                        len(escape))
>>>>>>                 state.checklookbehindgroup(group, source)
>>>>>>                 return GROUPREF, group
>>>>>>             raise source.error("invalid group reference %d" % group, len(escape) - 1)
  218:         if len(escape) == 2:
  218:             if c in ASCIILETTERS:
>>>>>>                 raise source.error("bad escape %s" % escape, len(escape))
  218:             return LITERAL, ord(escape[1])
>>>>>>     except ValueError:
>>>>>>         pass
>>>>>>     raise source.error("bad escape %s" % escape, len(escape))
       
>>>>>> def _uniq(items):
  755:     return list(dict.fromkeys(items))
       
>>>>>> def _parse_sub(source, state, verbose, nested):
           # parse an alternation: a|b|c
       
 1152:     items = []
 1152:     itemsappend = items.append
 1152:     sourcematch = source.match
 1152:     start = source.tell()
 4618:     while True:
 9236:         itemsappend(_parse(source, state, verbose, nested + 1,
 4618:                            not nested and not items))
 4617:         if not sourcematch("|"):
 1151:             break
       
 1151:     if len(items) == 1:
  755:         return items[0]
       
  396:     subpattern = SubPattern(state)
       
           # check if all items share a common prefix
  398:     while True:
  398:         prefix = None
  804:         for item in items:
  802:             if not item:
    1:                 break
  801:             if prefix is None:
  398:                 prefix = item[0]
  403:             elif item[0] != prefix:
  395:                 break
               else:
                   # all subitems start with a common "prefix".
                   # move it out of the branch
    6:             for item in items:
    4:                 del item[0]
    2:             subpattern.append(prefix)
    2:             continue # check next one
  396:         break
       
           # check if the branch can be replaced by a character set
  396:     set = []
  488:     for item in items:
  486:         if len(item) != 1:
  222:             break
  264:         op, av = item[0]
  264:         if op is LITERAL:
    7:             set.append((op, av))
  257:         elif op is IN and av[0][0] is not NEGATE:
   85:             set.extend(av)
               else:
  172:             break
           else:
               # we can store this as a character set instead of a
               # branch (the compiler may optimize this even more)
    2:         subpattern.append((IN, _uniq(set)))
    2:         return subpattern
       
  394:     subpattern.append((BRANCH, (None, items)))
  394:     return subpattern
       
>>>>>> def _parse(source, state, verbose, nested, first=False):
           # parse a simple pattern
 4618:     subpattern = SubPattern(state)
       
           # precompute constants into local variables
 4618:     subpatternappend = subpattern.append
 4618:     sourceget = source.get
 4618:     sourcematch = source.match
 4618:     _len = len
 4618:     _ord = ord
       
12952:     while True:
       
12952:         this = source.next
12952:         if this is None:
  208:             break # end of pattern
12744:         if this in "|)":
 4409:             break # end of subpattern
 8335:         sourceget()
       
 8335:         if verbose:
                   # skip whitespace and comments
 2457:             if this in WHITESPACE:
  334:                 continue
 2123:             if this == "#":
  190:                 while True:
  190:                     this = sourceget()
  190:                     if this is None or this == "\n":
    9:                         break
    9:                 continue
       
 7992:         if this[0] == "\\":
  954:             code = _escape(source, this, state)
  954:             subpatternappend(code)
       
 7038:         elif this not in SPECIAL_CHARS:
 4295:             subpatternappend((LITERAL, _ord(this)))
       
 2743:         elif this == "[":
  753:             here = source.tell() - 1
                   # character set
  753:             set = []
  753:             setappend = set.append
       ##          if sourcematch(":"):
       ##              pass # handle character classes
  753:             if source.next == '[':
>>>>>>                 import warnings
>>>>>>                 warnings.warn(
>>>>>>                     'Possible nested set at position %d' % source.tell(),
>>>>>>                     FutureWarning, stacklevel=nested + 6
                       )
  753:             negate = sourcematch("^")
                   # check remaining characters
 2495:             while True:
 2495:                 this = sourceget()
 2495:                 if this is None:
>>>>>>                     raise source.error("unterminated character set",
>>>>>>                                        source.tell() - here)
 2495:                 if this == "]" and set:
  750:                     break
 1745:                 elif this[0] == "\\":
  848:                     code1 = _class_escape(source, this)
                       else:
  897:                     if set and this in '-&~|' and source.next == this:
>>>>>>                         import warnings
>>>>>>                         warnings.warn(
>>>>>>                             'Possible set %s at position %d' % (
>>>>>>                                 'difference' if this == '-' else
>>>>>>                                 'intersection' if this == '&' else
>>>>>>                                 'symmetric difference' if this == '~' else
>>>>>>                                 'union',
>>>>>>                                 source.tell() - 1),
>>>>>>                             FutureWarning, stacklevel=nested + 6
                               )
  897:                     code1 = LITERAL, _ord(this)
 1745:                 if sourcematch("-"):
                           # potential range
  359:                     that = sourceget()
  359:                     if that is None:
>>>>>>                         raise source.error("unterminated character set",
>>>>>>                                            source.tell() - here)
  359:                     if that == "]":
    3:                         if code1[0] is IN:
>>>>>>                             code1 = code1[1][0]
    3:                         setappend(code1)
    3:                         setappend((LITERAL, _ord("-")))
    3:                         break
  356:                     if that[0] == "\\":
  162:                         code2 = _class_escape(source, that)
                           else:
  194:                         if that == '-':
>>>>>>                             import warnings
>>>>>>                             warnings.warn(
>>>>>>                                 'Possible set difference at position %d' % (
>>>>>>                                     source.tell() - 2),
>>>>>>                                 FutureWarning, stacklevel=nested + 6
                                   )
  194:                         code2 = LITERAL, _ord(that)
  356:                     if code1[0] != LITERAL or code2[0] != LITERAL:
>>>>>>                         msg = "bad character range %s-%s" % (this, that)
>>>>>>                         raise source.error(msg, len(this) + 1 + len(that))
  356:                     lo = code1[1]
  356:                     hi = code2[1]
  356:                     if hi < lo:
>>>>>>                         msg = "bad character range %s-%s" % (this, that)
>>>>>>                         raise source.error(msg, len(this) + 1 + len(that))
  356:                     setappend((RANGE, (lo, hi)))
                       else:
 1386:                     if code1[0] is IN:
   17:                         code1 = code1[1][0]
 1386:                     setappend(code1)
       
  753:             set = _uniq(set)
                   # XXX: <fl> should move set optimization to compiler!
  753:             if _len(set) == 1 and set[0][0] is LITERAL:
                       # optimization
  222:                 if negate:
  104:                     subpatternappend((NOT_LITERAL, set[0][1]))
                       else:
  118:                     subpatternappend(set[0])
                   else:
  531:                 if negate:
  151:                     set.insert(0, (NEGATE, None))
                       # charmap optimization can't be added here because
                       # global flags still are not known
  531:                 subpatternappend((IN, set))
       
 1990:         elif this in REPEAT_CHARS:
                   # repeat previous item
  859:             here = source.tell()
  859:             if this == "?":
  162:                 min, max = 0, 1
  697:             elif this == "*":
  454:                 min, max = 0, MAXREPEAT
       
  243:             elif this == "+":
  165:                 min, max = 1, MAXREPEAT
   78:             elif this == "{":
   78:                 if source.next == "}":
>>>>>>                     subpatternappend((LITERAL, _ord(this)))
>>>>>>                     continue
       
   78:                 min, max = 0, MAXREPEAT
   78:                 lo = hi = ""
  155:                 while source.next in DIGITS:
   77:                     lo += sourceget()
   78:                 if sourcematch(","):
  136:                     while source.next in DIGITS:
   69:                         hi += sourceget()
                       else:
   11:                     hi = lo
   78:                 if not sourcematch("}"):
    1:                     subpatternappend((LITERAL, _ord(this)))
    1:                     source.seek(here)
    1:                     continue
       
   77:                 if lo:
   77:                     min = int(lo)
   77:                     if min >= MAXREPEAT:
>>>>>>                         raise OverflowError("the repetition number is too large")
   77:                 if hi:
   72:                     max = int(hi)
   72:                     if max >= MAXREPEAT:
>>>>>>                         raise OverflowError("the repetition number is too large")
   72:                     if max < min:
>>>>>>                         raise source.error("min repeat greater than max repeat",
>>>>>>                                            source.tell() - here)
                   else:
>>>>>>                 raise AssertionError("unsupported quantifier %r" % (char,))
                   # figure out which item to repeat
  858:             if subpattern:
  858:                 item = subpattern[-1:]
                   else:
>>>>>>                 item = None
  858:             if not item or item[0][0] is AT:
>>>>>>                 raise source.error("nothing to repeat",
>>>>>>                                    source.tell() - here + len(this))
  858:             if item[0][0] in _REPEATCODES:
>>>>>>                 raise source.error("multiple repeat",
>>>>>>                                    source.tell() - here + len(this))
  858:             if item[0][0] is SUBPATTERN:
  202:                 group, add_flags, del_flags, p = item[0][1]
  202:                 if group is None and not add_flags and not del_flags:
  195:                     item = p
  858:             if sourcematch("?"):
   20:                 subpattern[-1] = (MIN_REPEAT, (min, max, item))
                   else:
  838:                 subpattern[-1] = (MAX_REPEAT, (min, max, item))
       
 1131:         elif this == ".":
   26:             subpatternappend((ANY, None))
       
 1105:         elif this == "(":
  948:             start = source.tell() - 1
  948:             group = True
  948:             name = None
  948:             add_flags = 0
  948:             del_flags = 0
  948:             if sourcematch("?"):
                       # options
  860:                 char = sourceget()
  860:                 if char is None:
>>>>>>                     raise source.error("unexpected end of pattern")
  860:                 if char == "P":
                           # python extensions
   59:                     if sourcematch("<"):
                               # named group: skip forward to end of name
   59:                         name = source.getuntil(">", "group name")
   59:                         if not name.isidentifier():
>>>>>>                             msg = "bad character in group name %r" % name
>>>>>>                             raise source.error(msg, len(name) + 1)
>>>>>>                     elif sourcematch("="):
                               # named backreference
>>>>>>                         name = source.getuntil(")", "group name")
>>>>>>                         if not name.isidentifier():
>>>>>>                             msg = "bad character in group name %r" % name
>>>>>>                             raise source.error(msg, len(name) + 1)
>>>>>>                         gid = state.groupdict.get(name)
>>>>>>                         if gid is None:
>>>>>>                             msg = "unknown group name %r" % name
>>>>>>                             raise source.error(msg, len(name) + 1)
>>>>>>                         if not state.checkgroup(gid):
>>>>>>                             raise source.error("cannot refer to an open group",
>>>>>>                                                len(name) + 1)
>>>>>>                         state.checklookbehindgroup(gid, source)
>>>>>>                         subpatternappend((GROUPREF, gid))
>>>>>>                         continue
       
                           else:
>>>>>>                         char = sourceget()
>>>>>>                         if char is None:
>>>>>>                             raise source.error("unexpected end of pattern")
>>>>>>                         raise source.error("unknown extension ?P" + char,
>>>>>>                                            len(char) + 2)
  801:                 elif char == ":":
                           # non-capturing group
  519:                     group = None
  282:                 elif char == "#":
                           # comment
>>>>>>                     while True:
>>>>>>                         if source.next is None:
>>>>>>                             raise source.error("missing ), unterminated comment",
>>>>>>                                                source.tell() - start)
>>>>>>                         if sourceget() == ")":
>>>>>>                             break
>>>>>>                     continue
       
  282:                 elif char in "=!<":
                           # lookahead assertions
  265:                     dir = 1
  265:                     if char == "<":
   33:                         char = sourceget()
   33:                         if char is None:
>>>>>>                             raise source.error("unexpected end of pattern")
   33:                         if char not in "=!":
>>>>>>                             raise source.error("unknown extension ?<" + char,
>>>>>>                                                len(char) + 2)
   33:                         dir = -1 # lookbehind
   33:                         lookbehindgroups = state.lookbehindgroups
   33:                         if lookbehindgroups is None:
   33:                             state.lookbehindgroups = state.groups
  265:                     p = _parse_sub(source, state, verbose, nested + 1)
  265:                     if dir < 0:
   33:                         if lookbehindgroups is None:
   33:                             state.lookbehindgroups = None
  265:                     if not sourcematch(")"):
>>>>>>                         raise source.error("missing ), unterminated subpattern",
>>>>>>                                            source.tell() - start)
  265:                     if char == "=":
    7:                         subpatternappend((ASSERT, (dir, p)))
                           else:
  258:                         subpatternappend((ASSERT_NOT, (dir, p)))
  265:                     continue
       
   17:                 elif char == "(":
                           # conditional backreference group
>>>>>>                     condname = source.getuntil(")", "group name")
>>>>>>                     if condname.isidentifier():
>>>>>>                         condgroup = state.groupdict.get(condname)
>>>>>>                         if condgroup is None:
>>>>>>                             msg = "unknown group name %r" % condname
>>>>>>                             raise source.error(msg, len(condname) + 1)
                           else:
>>>>>>                         try:
>>>>>>                             condgroup = int(condname)
>>>>>>                             if condgroup < 0:
>>>>>>                                 raise ValueError
>>>>>>                         except ValueError:
>>>>>>                             msg = "bad character in group name %r" % condname
>>>>>>                             raise source.error(msg, len(condname) + 1) from None
>>>>>>                         if not condgroup:
>>>>>>                             raise source.error("bad group number",
>>>>>>                                                len(condname) + 1)
>>>>>>                         if condgroup >= MAXGROUPS:
>>>>>>                             msg = "invalid group reference %d" % condgroup
>>>>>>                             raise source.error(msg, len(condname) + 1)
>>>>>>                         if condgroup not in state.grouprefpos:
>>>>>>                             state.grouprefpos[condgroup] = (
>>>>>>                                 source.tell() - len(condname) - 1
                                   )
>>>>>>                     state.checklookbehindgroup(condgroup, source)
>>>>>>                     item_yes = _parse(source, state, verbose, nested + 1)
>>>>>>                     if source.match("|"):
>>>>>>                         item_no = _parse(source, state, verbose, nested + 1)
>>>>>>                         if source.next == "|":
>>>>>>                             raise source.error("conditional backref with more than two branches")
                           else:
>>>>>>                         item_no = None
>>>>>>                     if not source.match(")"):
>>>>>>                         raise source.error("missing ), unterminated subpattern",
>>>>>>                                            source.tell() - start)
>>>>>>                     subpatternappend((GROUPREF_EXISTS, (condgroup, item_yes, item_no)))
>>>>>>                     continue
       
   17:                 elif char in FLAGS or char == "-":
                           # flags
   17:                     flags = _parse_flags(source, state, char)
   17:                     if flags is None:  # global flags
    5:                         if not first or subpattern:
>>>>>>                             import warnings
>>>>>>                             warnings.warn(
>>>>>>                                 'Flags not at the start of the expression %r%s'
>>>>>>                                 ' but at position %d' % (
>>>>>>                                     source.string[:20],  # truncate long regexes
>>>>>>                                     ' (truncated)' if len(source.string) > 20 else '',
>>>>>>                                     start,
                                       ),
>>>>>>                                 DeprecationWarning, stacklevel=nested + 6
                                   )
    5:                         if (state.flags & SRE_FLAG_VERBOSE) and not verbose:
    1:                             raise Verbose
    4:                         continue
       
   12:                     add_flags, del_flags = flags
   12:                     group = None
                       else:
>>>>>>                     raise source.error("unknown extension ?" + char,
>>>>>>                                        len(char) + 1)
       
                   # parse group contents
  678:             if group is not None:
  147:                 try:
  147:                     group = state.opengroup(name)
>>>>>>                 except error as err:
>>>>>>                     raise source.error(err.msg, len(name) + 1) from None
 1168:             sub_verbose = ((verbose or (add_flags & SRE_FLAG_VERBOSE)) and
  490:                            not (del_flags & SRE_FLAG_VERBOSE))
  678:             p = _parse_sub(source, state, sub_verbose, nested + 1)
  678:             if not source.match(")"):
>>>>>>                 raise source.error("missing ), unterminated subpattern",
>>>>>>                                    source.tell() - start)
  678:             if group is not None:
  147:                 state.closegroup(group, p)
  678:             subpatternappend((SUBPATTERN, (group, add_flags, del_flags, p)))
       
  157:         elif this == "^":
   58:             subpatternappend((AT, AT_BEGINNING))
       
   99:         elif this == "$":
   99:             subpatternappend((AT, AT_END))
       
               else:
>>>>>>             raise AssertionError("unsupported special character %r" % (char,))
       
           # unpack non-capturing groups
11746:     for i in range(len(subpattern))[::-1]:
 7129:         op, av = subpattern[i]
 7129:         if op is SUBPATTERN:
  476:             group, add_flags, del_flags, p = av
  476:             if group is None and not add_flags and not del_flags:
  324:                 subpattern[i: i+1] = p
       
 4617:     return subpattern
       
>>>>>> def _parse_flags(source, state, char):
   17:     sourceget = source.get
   17:     add_flags = 0
   17:     del_flags = 0
   17:     if char != "-":
   17:         while True:
   17:             flag = FLAGS[char]
   17:             if source.istext:
   17:                 if char == 'L':
>>>>>>                     msg = "bad inline flags: cannot use 'L' flag with a str pattern"
>>>>>>                     raise source.error(msg)
                   else:
>>>>>>                 if char == 'u':
>>>>>>                     msg = "bad inline flags: cannot use 'u' flag with a bytes pattern"
>>>>>>                     raise source.error(msg)
   17:             add_flags |= flag
   17:             if (flag & TYPE_FLAGS) and (add_flags & TYPE_FLAGS) != flag:
>>>>>>                 msg = "bad inline flags: flags 'a', 'u' and 'L' are incompatible"
>>>>>>                 raise source.error(msg)
   17:             char = sourceget()
   17:             if char is None:
>>>>>>                 raise source.error("missing -, : or )")
   17:             if char in ")-:":
   17:                 break
>>>>>>             if char not in FLAGS:
>>>>>>                 msg = "unknown flag" if char.isalpha() else "missing -, : or )"
>>>>>>                 raise source.error(msg, len(char))
   17:     if char == ")":
    5:         state.flags |= add_flags
    5:         return None
   12:     if add_flags & GLOBAL_FLAGS:
>>>>>>         raise source.error("bad inline flags: cannot turn on global flag", 1)
   12:     if char == "-":
>>>>>>         char = sourceget()
>>>>>>         if char is None:
>>>>>>             raise source.error("missing flag")
>>>>>>         if char not in FLAGS:
>>>>>>             msg = "unknown flag" if char.isalpha() else "missing flag"
>>>>>>             raise source.error(msg, len(char))
>>>>>>         while True:
>>>>>>             flag = FLAGS[char]
>>>>>>             if flag & TYPE_FLAGS:
>>>>>>                 msg = "bad inline flags: cannot turn off flags 'a', 'u' and 'L'"
>>>>>>                 raise source.error(msg)
>>>>>>             del_flags |= flag
>>>>>>             char = sourceget()
>>>>>>             if char is None:
>>>>>>                 raise source.error("missing :")
>>>>>>             if char == ":":
>>>>>>                 break
>>>>>>             if char not in FLAGS:
>>>>>>                 msg = "unknown flag" if char.isalpha() else "missing :"
>>>>>>                 raise source.error(msg, len(char))
   12:     assert char == ":"
   12:     if del_flags & GLOBAL_FLAGS:
>>>>>>         raise source.error("bad inline flags: cannot turn off global flag", 1)
   12:     if add_flags & del_flags:
>>>>>>         raise source.error("bad inline flags: flag turned on and off", 1)
   12:     return add_flags, del_flags
       
>>>>>> def fix_flags(src, flags):
           # Check and fix flags according to the type of pattern (str or bytes)
  208:     if isinstance(src, str):
  198:         if flags & SRE_FLAG_LOCALE:
>>>>>>             raise ValueError("cannot use LOCALE flag with a str pattern")
  198:         if not flags & SRE_FLAG_ASCII:
  198:             flags |= SRE_FLAG_UNICODE
>>>>>>         elif flags & SRE_FLAG_UNICODE:
>>>>>>             raise ValueError("ASCII and UNICODE flags are incompatible")
           else:
   10:         if flags & SRE_FLAG_UNICODE:
>>>>>>             raise ValueError("cannot use UNICODE flag with a bytes pattern")
   10:         if flags & SRE_FLAG_LOCALE and flags & SRE_FLAG_ASCII:
>>>>>>             raise ValueError("ASCII and LOCALE flags are incompatible")
  208:     return flags
       
>>>>>> def parse(str, flags=0, state=None):
           # parse 're' pattern into list of (opcode, argument) tuples
       
  208:     source = Tokenizer(str)
       
  208:     if state is None:
  208:         state = State()
  208:     state.flags = flags
  208:     state.str = str
       
  208:     try:
  208:         p = _parse_sub(source, state, flags & SRE_FLAG_VERBOSE, 0)
    1:     except Verbose:
               # the VERBOSE flag was switched on inside the pattern.  to be
               # on the safe side, we'll parse the whole thing again...
    1:         state = State()
    1:         state.flags = flags | SRE_FLAG_VERBOSE
    1:         state.str = str
    1:         source.seek(0)
    1:         p = _parse_sub(source, state, True, 0)
       
  208:     p.state.flags = fix_flags(str, p.state.flags)
       
  208:     if source.next is not None:
>>>>>>         assert source.next == ")"
>>>>>>         raise source.error("unbalanced parenthesis")
       
  208:     for g in p.state.grouprefpos:
>>>>>>         if g >= p.state.groups:
>>>>>>             msg = "invalid group reference %d" % g
>>>>>>             raise error(msg, str, p.state.grouprefpos[g])
       
  208:     if flags & SRE_FLAG_DEBUG:
>>>>>>         p.dump()
       
  208:     return p
       
>>>>>> def parse_template(source, state):
           # parse 're' replacement string into list of literals and
           # group references
>>>>>>     s = Tokenizer(source)
>>>>>>     sget = s.get
>>>>>>     groups = []
>>>>>>     literals = []
>>>>>>     literal = []
>>>>>>     lappend = literal.append
>>>>>>     def addgroup(index, pos):
>>>>>>         if index > state.groups:
>>>>>>             raise s.error("invalid group reference %d" % index, pos)
>>>>>>         if literal:
>>>>>>             literals.append(''.join(literal))
>>>>>>             del literal[:]
>>>>>>         groups.append((len(literals), index))
>>>>>>         literals.append(None)
>>>>>>     groupindex = state.groupindex
>>>>>>     while True:
>>>>>>         this = sget()
>>>>>>         if this is None:
>>>>>>             break # end of replacement string
>>>>>>         if this[0] == "\\":
                   # group
>>>>>>             c = this[1]
>>>>>>             if c == "g":
>>>>>>                 name = ""
>>>>>>                 if not s.match("<"):
>>>>>>                     raise s.error("missing <")
>>>>>>                 name = s.getuntil(">", "group name")
>>>>>>                 if name.isidentifier():
>>>>>>                     try:
>>>>>>                         index = groupindex[name]
>>>>>>                     except KeyError:
>>>>>>                         raise IndexError("unknown group name %r" % name)
                       else:
>>>>>>                     try:
>>>>>>                         index = int(name)
>>>>>>                         if index < 0:
>>>>>>                             raise ValueError
>>>>>>                     except ValueError:
>>>>>>                         raise s.error("bad character in group name %r" % name,
>>>>>>                                       len(name) + 1) from None
>>>>>>                     if index >= MAXGROUPS:
>>>>>>                         raise s.error("invalid group reference %d" % index,
>>>>>>                                       len(name) + 1)
>>>>>>                 addgroup(index, len(name) + 1)
>>>>>>             elif c == "0":
>>>>>>                 if s.next in OCTDIGITS:
>>>>>>                     this += sget()
>>>>>>                     if s.next in OCTDIGITS:
>>>>>>                         this += sget()
>>>>>>                 lappend(chr(int(this[1:], 8) & 0xff))
>>>>>>             elif c in DIGITS:
>>>>>>                 isoctal = False
>>>>>>                 if s.next in DIGITS:
>>>>>>                     this += sget()
>>>>>>                     if (c in OCTDIGITS and this[2] in OCTDIGITS and
>>>>>>                         s.next in OCTDIGITS):
>>>>>>                         this += sget()
>>>>>>                         isoctal = True
>>>>>>                         c = int(this[1:], 8)
>>>>>>                         if c > 0o377:
>>>>>>                             raise s.error('octal escape value %s outside of '
>>>>>>                                           'range 0-0o377' % this, len(this))
>>>>>>                         lappend(chr(c))
>>>>>>                 if not isoctal:
>>>>>>                     addgroup(int(this[1:]), len(this) - 1)
                   else:
>>>>>>                 try:
>>>>>>                     this = chr(ESCAPES[this][1])
>>>>>>                 except KeyError:
>>>>>>                     if c in ASCIILETTERS:
>>>>>>                         raise s.error('bad escape %s' % this, len(this))
>>>>>>                 lappend(this)
               else:
>>>>>>             lappend(this)
>>>>>>     if literal:
>>>>>>         literals.append(''.join(literal))
>>>>>>     if not isinstance(source, str):
               # The tokenizer implicitly decodes bytes objects as latin-1, we must
               # therefore re-encode the final representation.
>>>>>>         literals = [None if s is None else s.encode('latin-1') for s in literals]
>>>>>>     return groups, literals
       
>>>>>> def expand_template(template, match):
>>>>>>     g = match.group
>>>>>>     empty = match.string[:0]
>>>>>>     groups, literals = template
>>>>>>     literals = literals[:]
>>>>>>     try:
>>>>>>         for index, group in groups:
>>>>>>             literals[index] = g(group) or empty
>>>>>>     except IndexError:
>>>>>>         raise error("invalid group reference %d" % index)
>>>>>>     return empty.join(literals)

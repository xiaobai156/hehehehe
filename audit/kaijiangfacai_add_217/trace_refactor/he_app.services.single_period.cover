    1: import re
       
    1: from bs4 import BeautifulSoup
       
    1: from he_app.domain.models import CandidateEvidence, FailureInfo, ParseResult, Site
    1: from he_app.domain.policies import normalize_digit_text, normalize_text
    1: from he_app.parsers.common import (
           build_candidate_parts,
           build_latest_candidate_parts,
           extract_values,
           has_kill_sum_keyword,
           split_period_segments,
       )
    1: from he_app.parsers.dedicated.history import BATCH_NEW_DEDICATED_SITE_IDS
    1: from he_app.parsers.dedicated.kaijiangfacai import (
           KAIJIANGFACAI_HEADERS,
           KAIJIANGFACAI_SITE_ID,
           KAIJIANGFACAI_TITLE_RE,
           extract_kaijiangfacai_kill_sum_period_values,
       )
    1: from he_app.parsers.dedicated.tables import site_rule
    1: from he_app.parsers.registry import evaluate_site_documents
    1: from he_app.validation.period import contains_exact_period, is_valid_sum_value
    1: from he_app.validation.record_boundary import record_id_from_url
       
       
    1: Evaluation = tuple[str | None, str, list[str], str | None]
       
       
    1: def _contains_sum_value(text: str, value: str) -> bool:
   11:     number = int(value[:2])
   11:     token = rf"0?{number}" if number < 10 else str(number)
   22:     return bool(
   22:         re.search(
   11:             rf"(?:合\s*{token}(?!\d)|(?<!\d){token}\s*合|[\[【(（]\s*{token}\s*[\]】)）])",
   11:             text,
               )
           )
       
       
    1: def _sum_number_pattern(value: str) -> str:
>>>>>>     number = int(value[:2])
>>>>>>     return rf"0?{number}" if number < 10 else str(number)
       
       
    1: def _dedicated_document_keyword(site: Site, period: int, values: list[str], text: str) -> str | None:
   22:     if site.site_id == "s093_a_909922_article_aspx_id_3694545" and len(values) == 1:
>>>>>>         value = _sum_number_pattern(values[0])
>>>>>>         row_re = re.compile(
>>>>>>             rf"(?<!\d){period}\s*期\s*杀\s*[:：]\s*[\[【]\s*{value}\s*合\s*[\]】]\s*[开開]"
               )
>>>>>>         if row_re.search(text):
>>>>>>             return "亮劍原创公式杀合"
       
   22:     if site.site_id == "s073_shuqhbq" and len(values) == 1:
>>>>>>         anchor_re = re.compile(r"(?:绝|絕)\s*(?:杀|殺)\s*①\s*段\s*①\s*合")
>>>>>>         anchor = anchor_re.search(text)
>>>>>>         if anchor is None:
>>>>>>             return None
>>>>>>         section = text[anchor.start():]
>>>>>>         next_anchor = re.search(r"(?:绝|絕)\s*(?:杀|殺)\s*三\s*尾", section[anchor.end() - anchor.start():])
>>>>>>         if next_anchor is not None:
>>>>>>             section = section[: anchor.end() - anchor.start() + next_anchor.start()]
>>>>>>         value = _sum_number_pattern(values[0])
>>>>>>         row_re = re.compile(
>>>>>>             rf"(?<!\d){period}\s*期\s*(?:杀|殺)\s*\[\s*{value}\s*合\s*\]\s*(?:开|開)"
               )
>>>>>>         return "绝杀①段①合" if row_re.search(section) else None
       
   22:     if site.site_id == "s085_kcvpleh" and len(values) == 2:
>>>>>>         anchor_re = re.compile(
>>>>>>             r"(?:澳门|澳門)?\s*女人味[^期]{0,50}(?:绝|絕)\s*(?:杀|殺)\s*一\s*尾[^期]{0,20}二\s*合"
               )
>>>>>>         anchor = anchor_re.search(text)
>>>>>>         if anchor is None:
>>>>>>             return None
>>>>>>         first = _sum_number_pattern(values[0])
>>>>>>         second = _sum_number_pattern(values[1])
>>>>>>         row_re = re.compile(
>>>>>>             rf"(?<!\d){period}\s*期\s*[;:]?\s*(?:绝|絕)\s*(?:杀|殺)"
                   rf"[^期]{{0,40}}?\[\s*\d{{1,2}}\s*尾\s*\][^期]{{0,20}}?"
>>>>>>             rf"\[\s*{first}\s*合\s*[-－]\s*{second}\s*合\s*\]\s*(?:开|開)"
               )
>>>>>>         return "女人味绝杀一尾二合" if row_re.search(text[anchor.start():]) else None
       
   22:     if site.site_id in BATCH_NEW_DEDICATED_SITE_IDS and len(values) == 1:
>>>>>>         value = _sum_number_pattern(values[0])
>>>>>>         abbreviated_row_re = re.compile(
>>>>>>             rf"(?<!\d){period}\s*期\s*[:：]?\s*杀\s*[:：]?\s*"
>>>>>>             rf"[\[【\(（][^期]{{0,20}}?{value}\s*合[^期]{{0,12}}?[\]】\)）][^期]{{0,20}}?[开開]"
               )
>>>>>>         if abbreviated_row_re.search(text):
>>>>>>             return "专属绝杀一合栏目"
       
   22:     if has_kill_sum_keyword(text, allow_weak=True):
   22:         return "绝杀一合"
>>>>>>     if "杀合" in text:
>>>>>>         return "杀合"
>>>>>>     return None
       
       
    1: def _authority_groups(documents: list[str]) -> list[list[str]]:
           """Keep each physical document as an independent candidate authority."""
   25:     return [[document] for document in documents]
       
       
    1: def _allowed_documents(site: Site, documents: list[str]) -> list[str]:
    8:     allowed_fetch_kinds = site_rule(site).allowed_fetch_kinds
    8:     if not allowed_fetch_kinds:
    8:         return documents
>>>>>>     return [
>>>>>>         document
>>>>>>         for document in documents
>>>>>>         if str(getattr(document, "fetch_kind", "legacy") or "legacy") in allowed_fetch_kinds
           ]
       
       
    1: def _target_values(documents: list[str], period: int, allow_weak: bool) -> tuple[set[str], list[str]]:
    9:     values: set[str] = set()
    9:     lines: list[str] = []
    9:     for line, _has_locator in build_candidate_parts(documents, period, allow_weak):
>>>>>>         extracted = extract_values(line, allow_weak)
>>>>>>         if len(extracted) != 1:
>>>>>>             continue
>>>>>>         values.add(extracted[0])
>>>>>>         lines.append(line)
    9:     return values, lines
       
       
    1: def _has_complete_records(documents: list[str], allow_weak: bool) -> bool:
    7:     return bool(build_latest_candidate_parts(documents, allow_weak))
       
       
    1: def _candidate_segments(document: str, period: int) -> list[tuple[str, int, int]]:
>>>>>>     soup = BeautifulSoup(document, "html.parser")
>>>>>>     text = soup.get_text(" ", strip=True) or document
>>>>>>     normalized_document = normalize_digit_text(normalize_text(text))
>>>>>>     chunks: list[str] = []
>>>>>>     for tag in soup.find_all(["p", "div", "td", "tr", "li", "font", "span", "b", "strong"]):
>>>>>>         tag_text = normalize_digit_text(normalize_text(tag.get_text(" ", strip=True)))
>>>>>>         if tag_text:
>>>>>>             chunks.append(tag_text)
>>>>>>     line_text = soup.get_text("\n", strip=True) if soup.find() else str(document)
>>>>>>     chunks.extend(
>>>>>>         normalize_digit_text(normalize_text(line))
>>>>>>         for line in line_text.splitlines()
>>>>>>         if normalize_text(line)
           )
       
>>>>>>     segments: list[tuple[str, int, int]] = []
>>>>>>     seen: set[str] = set()
>>>>>>     for chunk in chunks:
>>>>>>         candidates = split_period_segments(chunk, period)
>>>>>>         if not candidates and contains_exact_period(chunk, period):
>>>>>>             candidates = [chunk]
>>>>>>         for segment in candidates:
>>>>>>             if segment in seen or len(segment) > 500:
>>>>>>                 continue
>>>>>>             seen.add(segment)
>>>>>>             start = normalized_document.find(segment)
>>>>>>             segments.append((segment, start, start + len(segment)))
>>>>>>     return segments
       
       
    1: def _detail_is_self_contained(site: Site, period: int, values: list[str], detail: str) -> bool:
   11:     normalized = normalize_digit_text(normalize_text(detail))
   11:     if not contains_exact_period(normalized, period):
>>>>>>         return False
   11:     if _dedicated_document_keyword(site, period, values, normalized) is None:
>>>>>>         return False
   33:     return all(_contains_sum_value(normalized, value) for value in values)
       
       
    1: def _special_segment_is_bounded(site_id: str, text: str, start: int, end: int) -> bool:
>>>>>>     if site_id == "s058_vkjwinyt":
>>>>>>         anchor = re.search(r"澳门综合杀", text)
>>>>>>         if anchor is None:
>>>>>>             return False
>>>>>>         section = text[anchor.start() :]
>>>>>>         stop = re.search(r"澳门六合彩合数属性|澳彩合数属性|合数属性|其他栏目|上一篇|下一篇|Copyright", section)
>>>>>>         limit = anchor.start() + stop.start() if stop is not None else len(text)
>>>>>>         return anchor.start() <= start < end <= limit
       
>>>>>>     if site_id == "s073_shuqhbq":
>>>>>>         anchor = re.search(r"绝杀\s*①\s*段\s*①\s*合", text)
>>>>>>         if anchor is None:
>>>>>>             return False
>>>>>>         section = text[anchor.end() :]
>>>>>>         stop = re.search(r"绝杀\s*三\s*尾", section)
>>>>>>         limit = anchor.end() + stop.start() if stop is not None else len(text)
>>>>>>         return anchor.start() <= start < end <= limit
       
>>>>>>     if site_id == "s085_kcvpleh":
>>>>>>         anchor = re.search(r"(?:澳门|澳門)?\s*女人味", text)
>>>>>>         if anchor is None:
>>>>>>             return False
>>>>>>         section = text[anchor.start() :]
>>>>>>         stop = re.search(r"澳门六合彩合数属性|澳彩合数属性|合数属性|上一篇|下一篇|Copyright", section)
>>>>>>         limit = anchor.start() + stop.start() if stop is not None else len(text)
>>>>>>         return anchor.start() <= start < end <= limit
       
>>>>>>     return False
       
       
    5: def _kaijiangfacai_evidence_bounds(
    3:     document: str, period: int, value: str
    1: ) -> tuple[int, int] | None:
   11:     extracted = extract_kaijiangfacai_kill_sum_period_values([document])
   11:     if extracted.get(period) != value:
>>>>>>         return None
       
   11:     soup = BeautifulSoup(document, "html.parser")
   22:     normalized_document = normalize_digit_text(
   11:         normalize_text(soup.get_text(" ", strip=True) or document)
           )
   11:     expected_number = int(value[:2])
   11:     for title in soup.select(".list-title"):
   11:         title_text = normalize_text(title.get_text(" ", strip=True))
   11:         if KAIJIANGFACAI_TITLE_RE.fullmatch(title_text) is None:
>>>>>>             continue
   11:         table = title.find_next("table")
   11:         if table is None:
>>>>>>             continue
   11:         header = table.find("tr")
  165:         headers = tuple(
   66:             normalize_text(cell.get_text(" ", strip=True))
   11:             for cell in header.find_all(["th", "td"], recursive=False)
   11:         ) if header is not None else ()
   11:         if headers != KAIJIANGFACAI_HEADERS:
>>>>>>             continue
   69:         for row in table.find_all("tr"):
 1035:             cells = [
  414:                 normalize_digit_text(normalize_text(cell.get_text(" ", strip=True)))
   69:                 for cell in row.find_all(["th", "td"], recursive=False)
                   ]
   69:             if len(cells) != len(KAIJIANGFACAI_HEADERS):
>>>>>>                 continue
   69:             period_match = re.fullmatch(r"(\d{1,4})\s*期", cells[0])
   69:             value_match = re.fullmatch(r"(0?[1-9]|1[0-3])", cells[3])
                   if (
   69:                 period_match is None
   58:                 or value_match is None
   58:                 or int(period_match.group(1)) != period
   11:                 or int(value_match.group(1)) != expected_number
                   ):
   58:                 continue
   11:             title_start = normalized_document.find(normalize_text(title_text))
   11:             row_text = normalize_digit_text(normalize_text(row.get_text(" ", strip=True)))
   11:             row_start = normalized_document.find(row_text, max(title_start, 0))
   11:             if row_start >= 0:
   11:                 return row_start, row_start + len(row_text)
>>>>>>     return None
       
       
    7: def build_document_evidence(
    1:     site: Site,
    1:     period: int,
    1:     values: list[str],
    1:     detail: str,
    1:     documents: list[str],
    1: ) -> tuple[CandidateEvidence, ...]:
   11:     if not _detail_is_self_contained(site, period, values, detail):
>>>>>>         return ()
       
   11:     article_id = record_id_from_url(site.url)
   11:     synthetic_detail_sites = {"s058_vkjwinyt", "s073_shuqhbq", "s085_kcvpleh"}
   11:     evidence: list[CandidateEvidence] = []
   22:     for index, document in enumerate(documents):
   11:         if article_id is not None and article_id not in document:
>>>>>>             continue
   11:         text = BeautifulSoup(document, "html.parser").get_text(" ", strip=True) or document
   11:         normalized_document = normalize_digit_text(normalize_text(text))
   11:         keyword = _dedicated_document_keyword(site, period, values, normalized_document)
   11:         if keyword is None:
>>>>>>             continue
       
   11:         if site.site_id == KAIJIANGFACAI_SITE_ID and len(values) == 1:
   11:             bounds = _kaijiangfacai_evidence_bounds(document, period, values[0])
   11:             if bounds is None:
>>>>>>                 continue
   11:             start, end = bounds
   11:             source_url = str(getattr(document, "source_url", "") or site.url)
   11:             authority_id = str(getattr(document, "authority_id", "legacy") or "legacy")
   11:             document_id = str(getattr(document, "document_id", "") or f"document:{index}")
   22:             actual_position = len(
   11:                 re.findall(r"(?<!\d)\d{1,4}\s*期", normalized_document[:start])
                   )
   22:             evidence.append(
  198:                 CandidateEvidence(
   11:                     site_id=site.site_id,
   11:                     period=period,
   11:                     values=tuple(values),
   11:                     source_url=source_url,
   11:                     document_id=document_id,
   11:                     row_order=actual_position,
   11:                     raw_line=detail,
   11:                     article_id=article_id,
   11:                     keyword="开奖发财综合杀料表杀合列",
   11:                     fetch_kind=str(getattr(document, "fetch_kind", "legacy")),
   11:                     document_type=str(getattr(document, "document_type", "text")),
   11:                     parent_url=str(getattr(document, "parent_url", "") or site.url),
   11:                     authority_id=authority_id,
   11:                     block_id=f"{authority_id}:{start}:{end}",
   11:                     block_start=start,
   11:                     block_end=end,
   11:                     actual_position=actual_position,
                       )
                   )
   11:             continue
       
>>>>>>         matched_segment: tuple[str, int, int] | None = None
>>>>>>         for segment, start, end in _candidate_segments(document, period):
>>>>>>             if not all(_contains_sum_value(segment, value) for value in values):
>>>>>>                 continue
>>>>>>             if site.site_id not in synthetic_detail_sites:
>>>>>>                 if _dedicated_document_keyword(site, period, values, segment) is None:
>>>>>>                     continue
>>>>>>                 segment_values = extract_values(segment, allow_weak=True)
>>>>>>                 if segment_values and set(segment_values) != set(values):
>>>>>>                     continue
>>>>>>             elif not _special_segment_is_bounded(site.site_id, normalized_document, start, end):
>>>>>>                 continue
>>>>>>             matched_segment = (segment, start, end)
>>>>>>             break
>>>>>>         if matched_segment is None:
>>>>>>             continue
       
>>>>>>         segment, start, end = matched_segment
>>>>>>         source_url = str(getattr(document, "source_url", "") or site.url)
>>>>>>         authority_id = str(getattr(document, "authority_id", "legacy") or "legacy")
>>>>>>         document_id = str(getattr(document, "document_id", "") or f"document:{index}")
>>>>>>         actual_position = len(re.findall(r"(?<!\d)\d{1,4}\s*期", normalized_document[:max(start, 0)]))
>>>>>>         evidence.append(
>>>>>>             CandidateEvidence(
>>>>>>                 site_id=site.site_id,
>>>>>>                 period=period,
>>>>>>                 values=tuple(values),
>>>>>>                 source_url=source_url,
>>>>>>                 document_id=document_id,
>>>>>>                 row_order=actual_position,
>>>>>>                 raw_line=detail,
>>>>>>                 article_id=article_id,
>>>>>>                 keyword=keyword,
>>>>>>                 fetch_kind=str(getattr(document, "fetch_kind", "legacy")),
>>>>>>                 document_type=str(getattr(document, "document_type", "text")),
>>>>>>                 parent_url=str(getattr(document, "parent_url", "") or site.url),
>>>>>>                 authority_id=authority_id,
>>>>>>                 block_id=f"{authority_id}:{max(start, 0)}:{end}",
>>>>>>                 block_start=max(start, 0),
>>>>>>                 block_end=end,
>>>>>>                 actual_position=actual_position,
                   )
               )
   11:     return tuple(evidence)
       
       
    5: def _cross_authority_conflict(
    1:     site: Site,
    1:     period: int,
    1:     evaluations: list[tuple[list[str], Evaluation, set[str], list[str]]],
    1: ) -> FailureInfo | None:
    8:     signatures_by_authority: list[tuple[set[tuple[str, ...]], list[str]]] = []
   17:     for _documents, evaluation, _strict_values, _strict_lines in evaluations:
    9:         result, detail, result_values, _reason = evaluation
    9:         signatures: set[tuple[str, ...]] = set()
    9:         lines: list[str] = []
    9:         if result is not None and result_values:
    1:             signatures.add(tuple(result_values))
    1:             lines.append(detail)
    9:         if signatures:
    1:             signatures_by_authority.append((signatures, lines))
    8:     if len(signatures_by_authority) < 2:
    8:         return None
>>>>>>     unique_signatures = {
>>>>>>         signature
>>>>>>         for signatures, _lines in signatures_by_authority
>>>>>>         for signature in signatures
           }
>>>>>>     if len(unique_signatures) <= 1:
>>>>>>         return None
>>>>>>     unique_values = sorted(
>>>>>>         {
>>>>>>             value
>>>>>>             for signature in unique_signatures
>>>>>>             for value in signature
               },
>>>>>>         key=lambda value: int(value[:2]),
           )
>>>>>>     conflict_lines = [line for _signatures, lines in signatures_by_authority for line in lines]
>>>>>>     return FailureInfo(
>>>>>>         "候选冲突",
>>>>>>         f"{site.name} {period}期跨来源候选结果冲突: {' / '.join(unique_values)}；"
>>>>>>         f"候选: {' | '.join(conflict_lines[:5])}",
           )
       
       
    1: def parse_site_period(site: Site, period: int, documents: list[str]) -> ParseResult:
    8:     rule = site_rule(site)
    8:     documents = _allowed_documents(site, documents)
    8:     evaluations: list[tuple[list[str], Evaluation, set[str], list[str]]] = []
   17:     for authority_documents in _authority_groups(documents):
    9:         evaluation = evaluate_site_documents(site, period, authority_documents)
   18:         strict_values, strict_lines = _target_values(
    9:             authority_documents, period, rule.allow_weak_kill_sum_keyword
               )
    9:         evaluations.append((authority_documents, evaluation, strict_values, strict_lines))
       
    8:     conflict = _cross_authority_conflict(site, period, evaluations)
    8:     if conflict is not None:
>>>>>>         return ParseResult(False, failure=conflict)
       
    8:     selected: tuple[list[str], Evaluation, set[str], list[str]] | None = None
   16:     for item in evaluations:
    9:         if item[1][0] is not None:
    1:             selected = item
    1:             break
    8:     if selected is None:
   14:         for item in evaluations:
    8:             _authority_documents, evaluation, _strict_values, _strict_lines = item
    8:             _result, _detail, _values, reason = evaluation
    8:             if reason in {"方向范围外", "超出范围", "候选冲突", "数据不完整"}:
    1:                 selected = item
    1:                 break
    8:     if selected is None:
   13:         for item in evaluations:
    7:             authority_documents, _evaluation, _strict_values, _strict_lines = item
    7:             if _has_complete_records(authority_documents, rule.allow_weak_kill_sum_keyword):
>>>>>>                 selected = item
>>>>>>                 break
    8:     if selected is None and evaluations:
   19:         selected = next((item for item in evaluations if item[1][0] is not None), evaluations[-1])
    8:     if selected is None:
>>>>>>         return ParseResult(False, failure=FailureInfo("未找到目标", f"{site.name} 没有可解析文档"))
       
    8:     authority_documents, evaluation, _strict_values, _strict_lines = selected
    8:     result, detail, values, reason = evaluation
    8:     if result is None:
    7:         return ParseResult(False, failure=FailureInfo(reason or "未找到目标", detail))
    3:     if not values or any(not is_valid_sum_value(value) for value in values):
>>>>>>         return ParseResult(False, failure=FailureInfo("字段校验未通过", f"{site.name} 返回了无效合数"))
       
    1:     evidence = build_document_evidence(site, period, values, detail, authority_documents)
    1:     if not evidence:
>>>>>>         return ParseResult(
>>>>>>             False,
>>>>>>             failure=FailureInfo(
>>>>>>                 "证据校验未通过",
>>>>>>                 f"{site.name} 解析成功但无法在同一原始候选行验证期数、关键词和合数",
                   ),
               )
    1:     return ParseResult(True, value=result, evidence=evidence)
       
       
    1: def evaluate_site_period(site: Site, period: int, documents: list[str]) -> tuple[str | None, str, list[str], str | None]:
>>>>>>     parsed = parse_site_period(site, period, documents)
>>>>>>     if not parsed.success:
>>>>>>         assert parsed.failure is not None
>>>>>>         return None, parsed.failure.reason, [], parsed.failure.category
>>>>>>     first = parsed.evidence[0]
>>>>>>     return parsed.value, first.raw_line, list(first.values), None
